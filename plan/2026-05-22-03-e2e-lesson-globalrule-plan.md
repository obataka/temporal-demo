# E2E テスト教訓のドキュメント化 + グローバルルール追加

## Context
E2E グランドデモ3回失敗の反省として、2つの変更を行う:
1. **LESSON.md に Lesson 4 を追記**: 机上デバッグで検知可能だった3つの失敗を記録
2. **~/.claude/CLAUDE.md にグローバルルールを追加**: 同種の問題を未来のタスクで防ぐ

---

## 変更 1: LESSON.md に Lesson 4 を追記

ファイル: `LESSON.md`（プロジェクトルート）

既存「共通パターンのまとめ」テーブルに行4を追加 + Lesson 4 セクションを末尾に追加。

**Lesson 4 の骨子:**
- 現象: `sop_e2e_demo.py` 3回実行失敗（禁止用語混入 / "仮"誤マッチ / API クォータ枯渇）
- 各失敗が「実行前の机上デバッグで検知可能だった」ことを整理する
- 教訓: バリデーション関数をローカル実行してデータを通す・compound word に注意・クォータ確認

## 変更 2: ~/.claude/CLAUDE.md にグローバルルール追加

ファイル: `~/.claude/CLAUDE.md`

「Test Dependency Mocking」セクション直後（`---` 区切りの前）に新セクションを挿入:

- **テストデータの無害性確認**: 入力データにバリデーションルールが拒否する語句・フォーマットが含まれていないか確認
- **バリデーションロジックの副作用確認**: マルチバイト文字の substring 検索が複合語に誤マッチしないか確認
- **外部 API クォータの確認**: 呼び出し回数 × 実行予定回数がクォータ上限を超えないか確認

---

## 実行手順

1. `LESSON.md` に Lesson 4 を追記（Edit ツール）
2. `~/.claude/CLAUDE.md` にグローバルルールを挿入（Edit ツール）

## 検証

- `LESSON.md` に Lesson 4 と共通パターン行4が存在する
- `~/.claude/CLAUDE.md` に「E2E / 統合テスト 実行前チェックリスト」セクションが存在する

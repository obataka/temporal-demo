{
  "workflow_id": "sop-evidence-5a72bb3d",
  "topic": "Temporal SOP 生成 Activity — フェーズ別 Gemini 呼び出し実装",
  "source_file": "activities/sop_activity.py",
  "timestamp": "20260508_122756",
  "resilience_ok": true,
  "summary": {
    "total_attempts": 5,
    "total_feedbacks": 2,
    "total_tokens": 103339,
    "total_latency_ms": 265384,
    "elapsed_sec": 302
  },
  "history": [
    {
      "approved": false,
      "attempt": 0,
      "feedback": null,
      "latency_ms": 11604.0,
      "output": "## 標準作業手順書（SOP）アウトライン提案\n\n### 1. はじめに\n本SOPは、Temporal Activityとして動作する「SOP生成フェーズ別Gemini呼び出し」機能の目的、構造、および運用方法について記述します。ドキュメントアーキテクト、開発者、運用担当者を対象としています。\n\n### 2. システム概要\nこのActivityは、SOP生成ワークフローにおいて、指定されたフェーズ（章立て提案、詳細執筆、最終レビュー）に応じてGeminiモデルを呼び出す役割を担います。SOP生成プロセスの自動化をサポートする基盤コンポーネントです。\n\n### 3. 前提条件\n本Activityを実行するために必要な環境設定、依存ライブラリ、およびAPIキーの設定（`GEMINI_API_KEY`）について説明します。必要なPythonバージョンやTemporal Clientの要件もここに含めます。\n\n### 4. SOP生成フェーズの概念\nSOP生成プロセスにおける主要な3つのフェーズ（`outline`, `draft`, `review`）について、それぞれの役割と目的を解説します。また、リトライ時の処理についても触れます。\n\n### 5. `generate_sop_phase_activity` Activityの詳細\nTemporal Activityとして定義された`generate_sop_phase_activity`のインターフェース、つまり入力データ型（`SOPRequest`）と出力データ型（`LLMResult`）について説明します。Activityの全体的な処理フローもここで概説します。\n\n### 6. コア機能の実装\n#### 6.1. システム指示（System Instructions）の管理\n各フェーズおよびリトライ状況に応じてGeminiに与える「役割」を定義する`_SYSTEM`辞書の構造と、その利用方法について詳述します。これにより、LLMの応答が一貫性と目的に沿ったものになるように制御されます。\n#### 6.2. LLMコンテンツの動的構築\n`_build_contents`関数が、要求されたフェーズ、リトライ回数、および過去の出力やフィードバックに基づいて、Geminiに送信する具体的なプロンプトコンテンツをどのように生成するかを解説します。\n#### 6.3. Gemini API呼び出し\n`genai.Client`を利用したGeminiモデルへの実際のAPI呼び出しプロセスについて説明します。モデル指定（`gemini-2.5-flash`）、`system_instruction`の適用、および応答の処理方法を具体的に記述します。\n\n### 7. エラーハンドリングとリトライ機構\n`GEMINI_API_KEY`が未設定の場合のエラー処理や、Gemini API呼び出しにおける潜在的なエラーとその対応について説明します。TemporalのActivityリトライメカニズムがどのように機能し、エラーからの回復を支援するかについても触れます。\n\n### 8. 実行方法とデプロイ\nTemporal Workerへの本Activityの登録方法と、Temporal WorkflowからこのActivityを呼び出す際の具体的なコード例や考慮事項を説明します。\n\n### 9. 運用上の考慮事項\n本Activityを本番環境で運用する上での重要な考慮事項、例えばAPIキーのセキュアな管理、Gemini APIのレートリミット、コスト最適化、および将来的なモデルアップデートへの対応などについて記述します。\n\n### 10. 付録\n本SOPで参照される`core.models.SOPRequest`および`core.models.LLMResult`のデータ構造定義を含めます。関連するドキュメントや外部リソースへのリンクもここに記載します。",
      "phase": "outline",
      "phase_label": "フェーズ1: 章立て提案",
      "tokens": 3518
    },
    {
      "approved": true,
      "attempt": 1,
      "feedback": "各章にサブセクションを 2〜3 個追加してください。また「対象読者（初心者 / 上級者）」と「所要時間の目安」を各章の冒頭に明記してください。",
      "latency_ms": 20148.12,
      "output": "はい、承知いたしました。人間からのフィードバックに基づき、各章にサブセクションを追加し、「対象読者」と「所要時間の目安」を明記した改善版アウトラインを提案します。\n\n---\n\n## 標準作業手順書（SOP）アウトライン提案 - 改善版\n\n### 1. はじめに\n*   **対象読者:** 全ての関係者（ドキュメントアーキテクト、開発者、運用担当者）\n*   **所要時間の目安:** 5分\n本SOPは、Temporal Activityとして動作する「SOP生成フェーズ別Gemini呼び出し」機能の目的、構造、および運用方法について記述します。\n\n#### 1.1. ドキュメントの目的とスコープ\n本SOPが解決する課題と、対象とする機能範囲について明確にします。\n#### 1.2. 対象読者と前提知識\n本SOPが想定する読者層（役割）と、彼らが持つべき基本的な技術的知識レベルを説明します。\n#### 1.3. 用語集\nSOP内で使用される主要な専門用語（例: Temporal Activity, Workflow, LLM, Geminiなど）の定義を提供します。\n\n### 2. システム概要\n*   **対象読者:** 開発者、ドキュメントアーキテクト\n*   **所要時間の目安:** 10分\nこのActivityは、SOP生成ワークフローにおいて、指定されたフェーズ（章立て提案、詳細執筆、最終レビュー）に応じてGeminiモデルを呼び出す役割を担います。\n\n#### 2.1. `generate_sop_phase_activity`の役割\n本ActivityがSOP生成パイプライン全体の中でどのような機能的役割を果たすかを説明します。\n#### 2.2. SOP生成ワークフローにおける位置づけ\n上位のTemporal Workflowからどのように呼び出され、どのようなデータを受け渡し、結果を返すのかを概説します。\n#### 2.3. 主要な技術要素\n本Activityを構成する主要な技術（Python, Temporal, Gemini API）と、その選定理由に軽く触れます。\n\n### 3. 前提条件\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安:** 15分\n本Activityを実行するために必要な環境設定、依存ライブラリ、およびAPIキーの設定（`GEMINI_API_KEY`）について説明します。\n\n#### 3.1. 動作環境と依存ライブラリ\n必要なPythonバージョン、`temporalio`、`google-generativeai`などのライブラリとそのインストール方法を記述します。\n#### 3.2. 必須APIキーと環境変数\n`GEMINI_API_KEY`の設定方法とその重要性、およびその他の関連する環境変数について詳述します。\n#### 3.3. Temporal Clientのセットアップ要件\nTemporal WorkerとServiceへの接続要件、および必要な認証情報について説明します。\n\n### 4. SOP生成フェーズの概念\n*   **対象読者:** 開発者、ドキュメントアーキテクト\n*   **所要時間の目安:** 10分\nSOP生成プロセスにおける主要な3つのフェーズ（`outline`, `draft`, `review`）について、それぞれの役割と目的を解説します。\n\n#### 4.1. 各フェーズ（outline, draft, review）の役割と目的\n各フェーズがSOP生成のどの段階を担当し、どのような成果物を期待するかを具体的に説明します。\n#### 4.2. リトライ機構とフェーズ遷移\n`attempt`カウントを用いたリトライ時の処理（`_retry`サフィックスの利用）と、フェーズ間の論理的な遷移について説明します。\n#### 4.3. LLMに与える役割の切り替え\n`_SYSTEM`辞書によるLLMの「役割」定義が、どのようにフェーズとリトライ状況に応じて切り替わるかを解説します。\n\n### 5. `generate_sop_phase_activity` Activityの詳細\n*   **対象読者:** 開発者\n*   **所要時間の目安:** 20分\nTemporal Activityとして定義された`generate_sop_phase_activity`のインターフェース、つまり入力データ型（`SOPRequest`）と出力データ型（`LLMResult`）について説明します。\n\n#### 5.1. Activityインターフェース (`@activity.defn`)\nActivityのデコレータ、関数シグネチャ、およびTemporal Activityとしての特性（例: 再試行可能性、ハートビート）について説明します。\n#### 5.2. 入力データモデル `SOPRequest`\n`SOPRequest`クラスの構造（`topic`, `phase`, `source_code`, `outline`, `draft`, `previous_output`, `feedback`, `attempt`など）とその各フィールドの役割を解説します。\n#### 5.3. 出力データモデル `LLMResult`\n`LLMResult`クラスの構造（`text`, `model`, `input_tokens`, `output_tokens`, `total_tokens`, `latency_ms`など）と、LLMからの応答情報をどのように保持するかを説明します。\n\n### 6. コア機能の実装\n*   **対象読者:** 開発者\n*   **所要時間の目安:** 30分\n本Activityの中核をなす、システム指示の管理、LLMコンテンツの動的構築、およびGemini API呼び出しの実装について深掘りします。\n\n#### 6.1. システム指示（System Instructions）の管理 (`_SYSTEM`辞書)\n`_SYSTEM`辞書の設計思想、各フェーズ・リトライごとの具体的なプロンプト内容、およびLLMの振る舞いを制御する仕組みを詳述します。\n#### 6.2. LLMプロンプトコンテンツの動的生成 (`_build_contents`関数)\n`_build_contents`関数が`SOPRequest`オブジェクトからどのように適切なプロンプト（ソースコード、アウトライン、フィードバックなど）を構築するかをコード例を交えて解説します。\n#### 6.3. Gemini API呼び出しと応答処理\n`google.genai`クライアントの初期化、モデル指定（`_MODEL`）、`system_instruction`の適用、`client.models.generate_content`の呼び出し、および応答から`LLMResult`を構築するプロセスを説明します。\n\n### 7. エラーハンドリングとリトライ機構\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安:** 15分\n本Activityにおけるエラー処理の戦略と、Temporalの組み込みリトライメカニズムがどのように活用されるかを解説します。\n\n#### 7.1. 環境設定エラーの処理 (`GEMINI_API_KEY`未設定)\n`EnvironmentError`を発生させる条件と、その対処法について説明します。\n#### 7.2. Gemini API呼び出し時の潜在的エラーとその対策\nネットワークエラー、APIレート制限、LLMによる応答生成失敗など、Gemini API呼び出し時に発生しうる問題とその一般的な対応策（リトライ、指数バックオフなど）について説明します。\n#### 7.3. Temporal Activityリトライの設定と効果\nTemporal WorkflowからのActivity呼び出し時に設定できるリトライオプション（最大リトライ回数、バックオフ間隔など）が、本Activityの堅牢性をどのように高めるかを説明します。\n\n### 8. 実行方法とデプロイ\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安:** 20分\n本ActivityをTemporal環境で実行するための具体的な手順と、デプロイに関する考慮事項を説明します。\n\n#### 8.1. Temporal WorkerへのActivity登録\n`Worker`インスタンスの作成、タスクキューの指定、および`generate_sop_phase_activity`をWorkerに登録するPythonコード例を示します。\n#### 8.2. WorkflowからのActivity呼び出し例\nTemporal Workflowから`generate_sop_phase_activity`を呼び出す際の非同期実行（`await client.execute_activity`）や、入力パラメータの渡し方をコード例で示します。\n#### 8.3. デプロイメントとスケーリング\nコンテナ化（Docker）、Kubernetesなどの環境でのデプロイ方法と、SOP生成の負荷に応じたWorkerのスケーリング戦略について言及します。\n\n### 9. 運用上の考慮事項\n*   **対象読者:** 運用担当者、ドキュメントアーキテクト\n*   **所要時間の目安:** 15分\n本Activityを本番環境で運用する上での重要な側面、例えばセキュリティ、コスト、パフォーマンスなどについて記述します。\n\n#### 9.1. APIキーのセキュアな管理\n`GEMINI_API_KEY`を環境変数やシークレット管理サービス（例: AWS Secrets Manager, GCP Secret Manager）で安全に管理する方法を説明します。\n#### 9.2. コストとレートリミットの最適化\nGemini APIの利用料金体系と、コストを抑制するための戦略（例: モデル選択、プロンプトの最適化）およびAPIのレートリミットへの対応策を説明します。\n#### 9.3. ログ監視とパフォーマンスチューニング\nActivityの実行ログ、LLMの応答時間（`latency_ms`）、エラー率などの監視方法と、ボトルネック特定・パフォーマンス改善のためのアプローチについて述べます。\n\n### 10. 付録\n*   **対象読者:** 開発者\n*   **所要時間の目安:** 5分\n本SOPで参照される主要なデータ構造の完全な定義と、関連する外部リソースへのリンクを含めます。\n\n#### 10.1. `core.models.SOPRequest` データ構造定義\n`SOPRequest`クラスの全フィールド、型ヒント、および簡単な説明を含む完全なコードスニペットを提供します。\n#### 10.2. `core.models.LLMResult` データ構造定義\n`LLMResult`クラスの全フィールド、型ヒント、および簡単な説明を含む完全なコードスニペットを提供します。\n#### 10.3. 関連ドキュメントと外部リソース\nTemporal公式ドキュメント、Gemini APIリファレンス、およびSOP生成に関するベストプラクティスなど、参考となる外部リソースへのリンク集を提供します。",
      "phase": "outline",
      "phase_label": "フェーズ1: 章立て提案",
      "tokens": 6226
    },
    {
      "approved": false,
      "attempt": 0,
      "feedback": null,
      "latency_ms": 72317.5,
      "output": "## 標準作業手順書（SOP）- Temporal SOP 生成 Activity — フェーズ別 Gemini 呼び出し実装\n\n### 1. はじめに\n\n#### 1.1. ドキュメントの目的とスコープ\n本SOPは、Temporal Workflow内で動作するActivityである `generate_sop_phase_activity` の詳細を記述します。このActivityは、標準作業手順書（SOP）を自動生成するプロセスにおいて、特定のフェーズ（章立て提案、詳細執筆、最終レビュー）に応じてGoogle Geminiモデルを呼び出し、その応答を処理することを目的としています。これにより、SOP生成ワークフローの各段階でのLLM（大規模言語モデル）とのインタラクションを標準化し、信頼性と保守性を向上させます。\n\n本SOPは、`generate_sop_phase_activity` Activityの設計、実装、使用方法、および運用上の考慮事項に焦点を当てます。\n\n#### 1.2. 対象読者と前提知識\n*   **対象読者:**\n    *   **ドキュメントアーキテクト:** SOP生成プロセスの全体設計、LLMへの指示内容、および期待される出力形式に関心がある方。\n    *   **開発者:** 本Activityの実装、拡張、およびデバッグを行う方。\n    *   **運用担当者:** 本Activityを含むシステム全体のデプロイ、監視、およびトラブルシューティングを担当する方。\n*   **前提知識:**\n    *   Pythonプログラミングの基礎知識。\n    *   Temporal WorkflowおよびActivityの基本的な概念。\n    *   RESTful APIの利用経験、特にGoogle Gemini APIまたは類似のLLM APIに関する基礎知識。\n    *   Markdown形式でのドキュメント作成に関する理解。\n\n#### 1.3. 用語集\n*   **SOP (Standard Operating Procedure):** 標準作業手順書。特定の作業やプロセスを標準化するための詳細な手順書。\n*   **Temporal:** 分散システムの耐障害性とスケーラビリティを向上させるためのオープンソースのオーケストレーションプラットフォーム。\n*   **Workflow:** Temporalにおける、一連のビジネスロジックをオーケストレーションするコードの実行単位。長時間実行され、耐障害性を持つ。\n*   **Activity:** Temporal Workflowから呼び出される、具体的なタスク（例: 外部API呼び出し、データベース操作）を実行するコードの単位。Activityは冪等であるべきであり、リトライ可能。\n*   **LLM (Large Language Model):** 大規模言語モデル。人間のようなテキスト生成、理解、要約などを行うAIモデル。本SOPではGoogle Geminiモデルを指す。\n*   **Gemini API:** Googleが提供するLLM「Gemini」にプログラムからアクセスするためのAPI。\n*   **System Instructions (システム指示):** LLMに対して、その役割や振る舞いを指示するためのプロンプト。モデルの応答の質と方向性を決定する上で重要。\n*   **Prompt (プロンプト):** LLMに与える入力テキスト。\n*   **Task Queue:** Temporal WorkerがActivityタスクを受け取るためのキュー。\n*   **Retry Policy:** TemporalがActivityやWorkflowの失敗時に再試行する際のルール（最大試行回数、バックオフ間隔など）を定義するポリシー。\n\n---\n\n### 2. システム概要\n\n#### 2.1. `generate_sop_phase_activity`の役割\n`generate_sop_phase_activity` Activityは、SOP生成ワークフローの中核を担う機能であり、以下の役割を果たします。\n\n1.  **フェーズに応じたLLMの振る舞い制御:** SOP生成プロセスの各フェーズ（章立て提案、詳細執筆、最終レビュー）に応じて、Geminiモデルに異なる「役割」と指示を与えます。\n2.  **動的なプロンプト生成:** SOP生成に必要な情報（対象のソースコード、承認済みアウトライン、前回の出力、人間からのフィードバックなど）を組み合わせて、Geminiモデルに最適なプロンプトを動的に生成します。\n3.  **Gemini API呼び出し:** 構築されたプロンプトとシステム指示を用いて、Google Gemini APIを呼び出し、SOPの一部または全体を生成します。\n4.  **応答の構造化とメトリクス収集:** Geminiモデルからの応答を構造化された形式（`LLMResult`）で返し、同時にトークン消費量や処理レイテンシなどのメトリクスを収集します。\n\nこのActivityは、SOP生成ワークフローが、段階的かつ人間によるフィードバックを伴いながら、高品質なSOPを生成するための鍵となります。\n\n#### 2.2. SOP生成ワークフローにおける位置づけ\n本Activityは、SOP生成をオーケストレーションするTemporal Workflow（例: `SOPGenerationWorkflow`）から呼び出されます。ワークフローは以下のような流れで本Activityを利用することを想定しています。\n\n1.  **Outlineフェーズ:** Workflowが`generate_sop_phase_activity`を`phase=\"outline\"`で呼び出し、ソースコードを渡します。Activityはアウトラインを生成して返します。\n2.  **Human Review (Optional):** 生成されたアウトラインは人間によるレビューを受け、フィードバックが与えられる場合があります。\n3.  **Draftフェーズ:** Workflowは承認済みアウトラインとソースコード、そして必要に応じてフィードバックを伴って`generate_sop_phase_activity`を`phase=\"draft\"`で呼び出します。ActivityはSOPの草稿を生成します。\n4.  **Human Review (Optional):** 生成された草稿は人間によるレビューを受け、フィードバックが与えられる場合があります。\n5.  **Reviewフェーズ:** WorkflowはSOP草稿と必要に応じてフィードバックを伴って`generate_sop_phase_activity`を`phase=\"review\"`で呼び出します。Activityは改善点リストと最終版SOPを生成します。\n\nこのように、本ActivityはWorkflowの各段階でLLMとのインタラクションを抽象化し、Workflowのロジックを簡潔に保ちます。\n\n#### 2.3. 主要な技術要素\n本Activityを構成する主要な技術要素は以下の通りです。\n\n*   **Python:** Activityの実装言語。その豊富なライブラリエコシステムと開発の容易さから採用されています。\n*   **Temporal Python SDK (`temporalio`):** PythonアプリケーションをTemporal Workflow/Activityとして実行するためのSDK。耐障害性とスケーラビリティを実現します。\n*   **Google Gemini API Python Client (`google-generativeai`):** Google Geminiモデルとインタラクトするための公式Pythonクライアントライブラリ。\n*   **環境変数:** APIキーなどの機密情報を安全に管理するために使用されます。\n\nこれらの技術の組み合わせにより、堅牢で柔軟なLLM駆動のSOP生成システムが構築されています。\n\n---\n\n### 3. 前提条件\n\n#### 3.1. 動作環境と依存ライブラリ\n`generate_sop_phase_activity` Activityを実行するためには、以下の環境が整っている必要があります。\n\n*   **Pythonバージョン:** 3.9以上（Temporalio SDKの推奨および互換性を考慮）。\n*   **オペレーティングシステム:** Linux, macOS, Windowsなどの一般的なOSで動作します。\n*   **依存ライブラリ:** 以下のPythonライブラリが必要です。`pip` を使用してインストールします。\n\n    ```bash\n    pip install temporalio google-generativeai pydantic\n    ```\n    *   `temporalio`: Temporal Python SDK。\n    *   `google-generativeai`: Google Gemini APIにアクセスするためのクライアントライブラリ。\n    *   `pydantic`: データモデル (`SOPRequest`, `LLMResult`) の定義とバリデーションに使用。\n\n#### 3.2. 必須APIキーと環境変数\nGoogle Gemini APIを利用するためには、APIキーが必要です。このAPIキーは安全な方法でActivityが実行される環境に提供される必要があります。\n\n*   **`GEMINI_API_KEY`:** Google Cloud Projectで発行されたGemini APIキー。\n    *   **設定方法:** Activityが実行される環境の環境変数として設定します。\n\n        **Linux/macOS:**\n        ```bash\n        export GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n        ```\n\n        **Windows (PowerShell):**\n        ```powershell\n        $env:GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n        ```\n\n        **Docker/Kubernetes:**\n        コンテナの環境変数として設定するか、シークレット管理サービス（例: Kubernetes Secrets, AWS Secrets Manager, GCP Secret Manager）を使用して注入することを強く推奨します。\n\n    *   **重要性:** APIキーがない場合、`generate_sop_phase_activity` は `EnvironmentError` を発生させて実行を停止します。これはAPIアクセス認証の必須要件であり、セキュアな運用のためにも適切に管理される必要があります。\n\n#### 3.3. Temporal Clientのセットアップ要件\n本Activityが動作するTemporal Workerは、Temporal Serviceに接続できる必要があります。\n\n*   **Temporal Serviceへの接続:**\n    *   Workerアプリケーションは、Temporal Serviceのエンドポイント（通常は `localhost:7233` またはデプロイされたクラウド環境のURL）にアクセスできる必要があります。\n    *   接続には、`temporalio.client.Client` オブジェクトを初期化する際に適切な設定（`target_host`, `namespace` など）が必要です。\n*   **認証情報 (Optional):**\n    *   Temporal Serviceが認証/認可を必要とする場合（例: mTLS）、Workerは適切な証明書やAPIキーを提供する必要があります。これらの設定は通常、`temporalio.client.Client` の初期化時に行われます。\n*   **タスクキュー:**\n    *   Activityは特定のタスクキューに登録され、そのタスクキューをリッスンしているWorkerによって実行されます。WorkerはActivityが登録されているタスクキューを指定して起動される必要があります。\n\n---\n\n### 4. SOP生成フェーズの概念\n\nSOP生成プロセスは、段階的なアプローチを採用しており、`generate_sop_phase_activity` Activityはこのアプローチをサポートするために3つの主要なフェーズを定義しています。\n\n#### 4.1. 各フェーズ（outline, draft, review）の役割と目的\n\n1.  **`outline` フェーズ:**\n    *   **役割:** 与えられたソースコードを分析し、SOPの基本的な構造である章立て（アウトライン）を提案します。\n    *   **目的:** 広範なSOPのトピックに対して、構造化された枠組みを迅速に提供し、その後の詳細なコンテンツ作成の指針とします。このフェーズの出力は、人間による確認と承認の対象となり得ます。\n    *   **期待される成果物:** 各章のタイトルと簡潔な説明を含むMarkdown形式のアウトライン。\n\n2.  **`draft` フェーズ:**\n    *   **役割:** 承認されたアウトラインとソースコードに基づき、SOPの本文を詳細に執筆します。\n    *   **目的:** アウトラインで定義された各章を具体的に展開し、実装の意図、手順、注意点、コード例など、SOPとして必要な情報を盛り込んだ草稿を作成します。\n    *   **期待される成果物:** 詳細なコンテンツを含むMarkdown形式のSOP草稿。\n\n3.  **`review` フェーズ:**\n    *   **役割:** 提供されたSOP草稿を徹底的にレビューし、改善点リストと最終版SOPを提供します。\n    *   **目的:** 草稿の正確性、完全性、明確さ、実用性を評価し、品質の高い最終SOPを出力します。このフェーズでは、必要に応じて草稿の修正や加筆提案が行われます。\n    *   **期待される成果物:** 【改善点リスト】と【最終版SOP】を構造化したMarkdown形式のテキスト。\n\n#### 4.2. リトライ機構とフェーズ遷移\n各フェーズは、人間からのフィードバックに基づいて再試行される可能性があります。本Activityは、入力パラメータ `request.attempt` を利用してこのリトライ状況を識別します。\n\n*   **`attempt` カウント:** `request.attempt` は、現在の試行回数を表します。\n    *   `attempt = 0`: 初回の実行。\n    *   `attempt > 0`: リトライ実行。\n*   **`_retry` サフィックスの利用:** `_SYSTEM` 辞書（後述）では、`\"outline\"`, `\"draft\"`, `\"review\"` に加えて、それぞれに対応するリトライ用のキー (`\"outline_retry\"`, `\"draft_retry\"`, `\"review_retry\"`) が定義されています。\n    *   `request.attempt > 0` の場合、`f\"{request.phase}_retry\"` というキーが使用され、LLMに「前回のアウトライン/草稿に対してフィードバックがあるので、それを反映して改善してください」という指示が与えられます。\n*   **フェーズ間の論理的な遷移:** ワークフローレベルでは、`outline` → `draft` → `review` という順序でフェーズが進行します。各フェーズ内でのリトライは、同じフェーズキー (`outline`, `draft`, `review`) を使いつつ、`attempt` カウントを増やして本Activityを再呼び出しすることで実現されます。これにより、LLMは前回の出力とフィードバックを参照して、より良い出力を生成しようと試みます。\n\n#### 4.3. LLMに与える役割の切り替え\n`_SYSTEM` 辞書は、各フェーズおよびリトライ状況に応じてLLMに与える「役割（System Instruction）」を定義しています。\n\n```python\n_SYSTEM = {\n    \"outline\": \"あなたは経験豊富なドキュメントアーキテクトです。...\",\n    \"outline_retry\": \"あなたは経験豊富なドキュメントアーキテクトです。前回のアウトラインに対して人間からフィードバックが届いています。...\",\n    \"draft\": \"あなたは熟練した技術ライターです。...\",\n    \"draft_retry\": \"あなたは熟練した技術ライターです。前回のSOP草稿に対して人間からフィードバックが届いています。...\",\n    \"review\": \"あなたは品質保証の専門家です。...\",\n    \"review_retry\": \"あなたは品質保証の専門家です。前回のレビュー結果に対して人間からフィードバックが届いています。...\",\n}\n```\n*   **フェーズごとの役割:**\n    *   `outline` フェーズでは「ドキュメントアーキテクト」として構造設計に集中させます。\n    *   `draft` フェーズでは「技術ライター」として詳細なコンテンツ作成に集中させます。\n    *   `review` フェーズでは「品質保証の専門家」として批判的視点での評価に集中させます。\n*   **リトライ時の役割強化:** リトライ時 (`_retry` サフィックス付きのキー) の指示には、「人間からのフィードバックを最優先で反映する」という強調が加えられ、LLMが単に再生成するのではなく、具体的な指示に従って改善を行うよう促しています。\n\nこの動的な役割の切り替えにより、各フェーズでLLMの能力を最大限に引き出し、高品質なSOP生成に貢献します。\n\n---\n\n### 5. `generate_sop_phase_activity` Activityの詳細\n\n`generate_sop_phase_activity` は、Temporal Activityとして設計されており、明確な入力と出力を持ちます。\n\n#### 5.1. Activityインターフェース (`@activity.defn`)\n本Activityは `temporalio` ライブラリの `@activity.defn` デコレータを使用して定義されます。\n\n```python\nfrom temporalio import activity\nfrom core.models import SOPRequest, LLMResult\n\n@activity.defn\nasync def generate_sop_phase_activity(request: SOPRequest) -> LLMResult:\n    \"\"\"フェーズ別 SOP 生成 Activity。Gemini の system_instruction でフェーズを制御する。\"\"\"\n    # ... 実装 ...\n```\n\n*   **`@activity.defn`:** このデコレータは、指定された関数がTemporal Activityであることを示します。これにより、Temporal Workerがこの関数を発見し、Task Queueからタスクを受け取って実行できるようになります。\n*   **`async def`:** Activity関数は非同期関数として定義されています。これは、I/Oバウンドな操作（この場合はGemini API呼び出し）中にWorkerが他のActivityタスクを処理できるようにするためです。\n*   **関数シグネチャ:** `generate_sop_phase_activity(request: SOPRequest) -> LLMResult`\n    *   **入力:** `request` 引数は `SOPRequest` 型であり、SOP生成に必要な全ての情報を含みます。\n    *   **出力:** `LLMResult` 型のオブジェクトを返します。これはGeminiからの応答と関連するメトリクスをカプセル化したものです。\n*   **Temporal Activityとしての特性:**\n    *   **再試行可能性:** Temporal Workflowは、Activityの失敗時に組み込みのリトライメカニズムを提供します。本Activityはこのメカニズムを活用し、APIエラーなど一時的な問題が発生した場合に自動的に再試行されます。\n    *   **ハートビート (Heartbeat):** 長時間実行されるActivityは、進捗状況をTemporal Serviceに報告するためにハートビートを送信できます。このActivity自体はGemini API呼び出しのレイテンシに依存しますが、必要に応じて `activity.heartbeat()` を組み込むことで、WorkflowがActivityの進捗を監視し、タイムアウトを防ぐことができます（本SOPのコードには明示的なハートビートは含まれていませんが、必要に応じて追加可能です）。\n\n#### 5.2. 入力データモデル `SOPRequest`\n`SOPRequest` クラスは、`pydantic.BaseModel` を継承しており、Activityへの入力データを構造化してバリデーションします。\n\n```python\n# core/models.py (SOPRequestの定義を想定)\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass SOPRequest(BaseModel):\n    topic: str = Field(..., description=\"SOPが対象とするトピックまたは機能の簡潔な説明。\")\n    phase: str = Field(..., description=\"現在のSOP生成フェーズ ('outline', 'draft', 'review')。\")\n    source_code: Optional[str] = Field(None, description=\"ドキュメント化対象のソースコード。\")\n    outline: Optional[str] = Field(None, description=\"承認済みのアウトライン（draft/reviewフェーズで使用）。\")\n    draft: Optional[str] = Field(None, description=\"SOPの草稿（reviewフェーズで使用）。\")\n    previous_output: Optional[str] = Field(None, description=\"前回のLLM出力（リトライ時に使用）。\")\n    feedback: Optional[str] = Field(None, description=\"人間からのフィードバック（リトライ時に使用）。\")\n    attempt: int = Field(0, description=\"現在の試行回数（初回は0）。\")\n```\n\n*   **`topic` (str):** SOPが対象とするシステムや機能の簡潔な説明。LLMにSOPの主題を伝えるために使用されます。\n*   **`phase` (str):** 現在のSOP生成フェーズ。`\"outline\"`, `\"draft\"`, `\"review\"` のいずれかです。この値に基づいてLLMのシステム指示とプロンプトが構築されます。\n*   **`source_code` (Optional[str]):** ドキュメント化の対象となるソースコードの文字列。`outline` および `draft` フェーズでLLMに提供されます。\n*   **`outline` (Optional[str]):** 以前の `outline` フェーズで生成され、承認されたアウトライン。`draft` フェーズでSOP本文の構造の指針としてLLMに提供されます。\n*   **`draft` (Optional[str]):** 以前の `draft` フェーズで生成されたSOPの草稿。`review` フェーズでLLMによるレビュー対象となります。\n*   **`previous_output` (Optional[str]):** 前回のActivity実行でLLMから生成された出力。リトライ時 (`attempt > 0`) に、LLMが前回の結果を参照し、フィードバックを適用する際に使用されます。\n*   **`feedback` (Optional[str]):** 人間（または自動化された評価システム）からのフィードバックテキスト。リトライ時 (`attempt > 0`) に、LLMが改善を行うための具体的な指示として使用されます。\n*   **`attempt` (int):** 現在のActivityの試行回数。初回呼び出しは `0` で、リトライするごとにインクリメントされます。この値は、`_SYSTEM` 辞書から適切なシステム指示を選択するために使用されます。\n\nこれらのフィールドは、LLMに与える情報の粒度と関連性を制御し、各フェーズでのLLMの応答を最適化するために設計されています。\n\n#### 5.3. 出力データモデル `LLMResult`\n`LLMResult` クラスは、Gemini APIからの応答と、その呼び出しに関するメトリクスを構造化して格納します。\n\n```python\n# core/models.py (LLMResultの定義を想定)\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass LLMResult(BaseModel):\n    text: str = Field(..., description=\"LLMからの生成テキスト。\")\n    model: str = Field(..., description=\"使用されたLLMモデルの識別子。\")\n    input_tokens: int = Field(..., description=\"LLMへの入力に消費されたトークン数。\")\n    output_tokens: int = Field(..., description=\"LLMの出力に消費されたトークン数。\")\n    total_tokens: int = Field(..., description=\"合計トークン数 (入力 + 出力)。\")\n    latency_ms: float = Field(..., description=\"LLM API呼び出しのレイテンシ（ミリ秒）。\")\n    error: Optional[str] = Field(None, description=\"エラーが発生した場合のエラーメッセージ。\")\n```\n\n*   **`text` (str):** Geminiモデルが生成した主なテキスト応答。これはアウトライン、SOP草稿、またはレビュー結果となります。\n*   **`model` (str):** 応答を生成するために使用されたGeminiモデルのID（例: `\"gemini-2.5-flash\"`）。\n*   **`input_tokens` (int):** プロンプト（入力）に費やされたトークン数。Gemini APIの `usage_metadata.prompt_token_count` から取得されます。\n*   **`output_tokens` (int):** 生成された応答（出力）に費やされたトークン数。Gemini APIの `usage_metadata.candidates_token_count` から取得されます。\n*   **`total_tokens` (int):** `input_tokens` と `output_tokens` の合計。APIコストの計算に直接関係します。Gemini APIの `usage_metadata.total_token_count` から取得されます。\n*   **`latency_ms` (float):** Gemini APIへのリクエスト送信から応答受信までの時間（ミリ秒単位）。パフォーマンス監視に有用です。\n*   **`error` (Optional[str]):** エラーが発生した場合に、エラーメッセージを格納します。成功時には `None` となります。\n\nこの構造化された出力により、WorkflowはLLMの応答を容易にパースして後続の処理に利用できるだけでなく、コストやパフォーマンスに関する重要なメトリクスも収集できます。\n\n---\n\n### 6. コア機能の実装\n\n本Activityの中核は、LLMに対する指示の管理、動的なプロンプトコンテンツの構築、およびGemini APIの呼び出しと応答処理にあります。\n\n#### 6.1. システム指示（System Instructions）の管理 (`_SYSTEM`辞書)\n`_SYSTEM` 辞書は、各SOP生成フェーズおよびリトライの有無に応じて、Geminiモデルに与える「システム指示」を定義します。\n\n```python\n_SYSTEM = {\n    \"outline\": (\n        \"あなたは経験豊富なドキュメントアーキテクトです。\"\n        \"与えられたソースコードを分析し、標準作業手順書（SOP）の章立て（アウトライン）を提案してください。\"\n        \"各章のタイトルと1〜2行の説明を含め、明確で構造化されたアウトラインをMarkdownで出力してください。\"\n    ),\n    \"outline_retry\": (\n        \"あなたは経験豊富なドキュメントアーキテクトです。\"\n        \"前回のアウトラインに対して人間からフィードバックが届いています。\"\n        \"そのフィードバックを最優先で反映し、改善されたアウトラインをMarkdownで提供してください。\"\n    ),\n    # ... 他のフェーズの定義 ...\n}\n```\n\n*   **設計思想:**\n    *   **役割ベースのプロンプティング:** LLMに特定の専門家（ドキュメントアーキテクト、技術ライター、品質保証専門家）の役割を与えることで、その専門性に応じた思考プロセスと出力形式を促します。\n    *   **具体的な指示:** 各フェーズでLLMに何を期待するか（例: 「章立てを提案」「詳細を執筆」「レビューし改善点と最終版を提供」）を明確に記述します。\n    *   **出力形式の指定:** 「Markdownで出力してください」と明示することで、構造化された応答を期待します。\n    *   **リトライ時の指示強化:** リトライ (`_retry` サフィックス) 時には「フィードバックを最優先で反映する」という文言を加え、改善に対するLLMの集中を促します。\n*   **`system_instruction` の選択ロジック:**\n    `generate_sop_phase_activity` 関数内で、`request.phase` と `request.attempt` に基づいて適切な `system_instruction` が選択されます。\n\n    ```python\n    phase_key = f\"{request.phase}_retry\" if request.attempt > 0 else request.phase\n    system_instruction = _SYSTEM[phase_key]\n    ```\n    これにより、初回実行時は `request.phase` に対応するシステム指示が、リトライ時は `request.phase` に `_retry` を付加したシステム指示が選択されます。\n\nこの管理されたシステム指示により、LLMは各フェーズの目的に合致した、高品質で一貫性のある応答を生成することが可能になります。\n\n#### 6.2. LLMプロンプトコンテンツの動的生成 (`_build_contents`関数)\n`_build_contents` 関数は、`SOPRequest` オブジェクトからGeminiモデルに渡すための主要なプロンプトコンテンツを動的に構築します。\n\n```python\ndef _build_contents(request: SOPRequest) -> str:\n    phase = request.phase\n    is_retry = request.attempt > 0\n\n    if phase == \"outline\":\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## ソースコード\\n```python\\n{request.source_code}\\n```\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回のアウトライン（試行 #{request.attempt - 1}）\\n\"\n                f\"{request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善版アウトラインを提供してください。\"\n            )\n        return base + \"\\n\\n---\\n上記ソースコードの SOP アウトラインを提案してください。\"\n\n    elif phase == \"draft\":\n        # ... draftフェーズのロジック ...\n\n    else:  # review\n        # ... reviewフェーズのロジック ...\n```\n\n*   **関数の役割:** `_build_contents` は、`SOPRequest` の各フィールド（`topic`, `source_code`, `outline`, `draft`, `previous_output`, `feedback`）を組み合わせ、現在のフェーズとリトライ状況に応じた最適なプロンプトテキストを生成します。\n*   **フェーズごとのプロンプト構造:**\n    *   **`outline` フェーズ:**\n        *   初回 (`is_retry` が `False`): ドキュメント化対象のトピックとソースコードをLLMに提示し、「SOPアウトラインを提案してください」と指示します。\n        *   リトライ (`is_retry` が `True`): 上記に加え、前回のLLM出力 (`request.previous_output`) と人間からのフィードバック (`request.feedback`) を提示し、「フィードバックを反映した改善版アウトラインを提供してください」と明確に指示します。\n    *   **`draft` フェーズ:**\n        *   初回: ドキュメント化対象、承認済みアウトライン (`request.outline`)、およびソースコードを提示し、「詳細なSOP本文を執筆してください」と指示します。\n        *   リトライ: 上記に加え、前回の草稿とフィードバックを提示し、「フィードバックを反映した改善版草稿を提供してください」と指示します。\n    *   **`review` フェーズ:**\n        *   初回: ドキュメント化対象とSOP草稿 (`request.draft`) を提示し、「このSOP草稿をレビューし、【改善点リスト】と【最終版SOP】を提供してください」と指示します。\n        *   リトライ: 上記に加え、前回のレビュー結果とフィードバックを提示し、「フィードバックを反映した改善された最終版を提供してください」と指示します。\n*   **コード例（`draft` フェーズ）:**\n\n    ```python\n    elif phase == \"draft\":\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## 承認済みアウトライン\\n{request.outline}\\n\\n\"\n            f\"## ソースコード\\n```python\\n{request.source_code}\\n```\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回の草稿（試行 #{request.attempt - 1}）\\n\"\n                f\"{request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善版草稿を提供してください。\"\n            )\n        return base + \"\\n\\n---\\n上記アウトラインに基づき、詳細なSOP本文を執筆してください。\"\n    ```\n    この例から分かるように、各フェーズで必要な入力情報が異なるため、`_build_contents` 関数は非常に柔軟にプロンプトを構築します。Markdownのヘッダー (`##`) を使用して情報を構造化し、LLMが各セクションの内容を正確に理解できるように工夫されています。\n\n#### 6.3. Gemini API呼び出しと応答処理\n`generate_sop_phase_activity` 関数内で、`google.generativeai` クライアントを使用してGemini APIを呼び出し、その応答を処理します。\n\n```python\nasync def generate_sop_phase_activity(request: SOPRequest) -> LLMResult:\n    from google import genai\n    from google.genai import types\n\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n\n    client = genai.Client(api_key=api_key)\n\n    phase_key = f\"{request.phase}_retry\" if request.attempt > 0 else request.phase\n    system_instruction = _SYSTEM[phase_key]\n    contents = _build_contents(request)\n\n    start = time.monotonic()\n    response = await client.models.generate_content( # await を使用\n        model=_MODEL,\n        contents=contents,\n        config=types.GenerateContentConfig(system_instruction=system_instruction),\n    )\n    latency_ms = (time.monotonic() - start) * 1000\n\n    usage = response.usage_metadata\n    return LLMResult(\n        text=response.text,\n        model=_MODEL,\n        input_tokens=getattr(usage, \"prompt_token_count\", 0) or 0,\n        output_tokens=getattr(usage, \"candidates_token_count\", 0) or 0,\n        total_tokens=getattr(usage, \"total_token_count\", 0) or 0,\n        latency_ms=round(latency_ms, 2),\n    )\n```\n\n*   **`google.genai` クライアントの初期化:**\n    ```python\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n    client = genai.Client(api_key=api_key)\n    ```\n    環境変数 `GEMINI_API_KEY` からAPIキーを取得し、`genai.Client` を初期化します。キーが設定されていない場合は `EnvironmentError` を発生させます。\n*   **モデル指定 (`_MODEL`):**\n    ```python\n    _MODEL = \"gemini-2.5-flash\"\n    # ...\n    response = await client.models.generate_content(\n        model=_MODEL,\n        # ...\n    )\n    ```\n    事前に定義された `_MODEL` 変数 (`\"gemini-2.5-flash\"`) を使用して、呼び出すGeminiモデルを指定します。このモデルは高速かつコスト効率の良いテキスト生成に適しています。\n*   **`system_instruction` の適用:**\n    ```python\n    # ...\n    config=types.GenerateContentConfig(system_instruction=system_instruction),\n    ```\n    LLMに役割と振る舞いを指示する `system_instruction` は、`GenerateContentConfig` オブジェクトを通じてAPIリクエストに含められます。これはLLMの応答の質を大きく左右する重要な要素です。\n*   **`client.models.generate_content` の呼び出し:**\n    ```python\n    response = await client.models.generate_content(\n        model=_MODEL,\n        contents=contents,\n        config=types.GenerateContentConfig(system_instruction=system_instruction),\n    )\n    ```\n    `client.models.generate_content` メソッドを非同期で呼び出します。\n    *   `model`: 使用するGeminiモデル。\n    *   `contents`: `_build_contents` 関数で生成された主要なプロンプトテキスト。\n    *   `config`: `system_instruction` を含む追加設定。\n*   **応答処理と `LLMResult` の構築:**\n    ```python\n    latency_ms = (time.monotonic() - start) * 1000\n    usage = response.usage_metadata\n    return LLMResult(\n        text=response.text,\n        model=_MODEL,\n        input_tokens=getattr(usage, \"prompt_token_count\", 0) or 0,\n        output_tokens=getattr(usage, \"candidates_token_count\", 0) or 0,\n        total_tokens=getattr(usage, \"total_token_count\", 0) or 0,\n        latency_ms=round(latency_ms, 2),\n    )\n    ```\n    API応答から以下の情報を抽出します。\n    *   `response.text`: LLMが生成したテキスト。\n    *   `response.usage_metadata`: トークン数に関する情報（`prompt_token_count`, `candidates_token_count`, `total_token_count`）。`getattr()` と `or 0` を使用して、これらの属性が存在しない場合でも安全にデフォルト値（0）を取得します。\n    *   `time.monotonic()` を使用してAPI呼び出しの開始と終了時刻を記録し、レイテンシを計算します。\n    これらの情報を用いて `LLMResult` オブジェクトを構築し、Activityの戻り値として返します。\n\nこのセクションで説明されたコア機能の実装は、本ActivityがSOP生成プロセスの各フェーズでLLMを効果的に利用するための基盤を形成します。\n\n---\n\n### 7. エラーハンドリングとリトライ機構\n\n本Activityは、外部サービスであるGemini APIに依存するため、エラーハンドリングとリトライ機構は堅牢な運用に不可欠です。\n\n#### 7.1. 環境設定エラーの処理 (`GEMINI_API_KEY`未設定)\nGemini APIを呼び出すには、有効なAPIキーが環境変数 `GEMINI_API_KEY` に設定されている必要があります。\n\n```python\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n```\n\n*   **エラー発生条件:** `os.environ.get(\"GEMINI_API_KEY\")` が `None` または空文字列を返す場合、この条件が真となり `EnvironmentError` が発生します。\n*   **対処法:**\n    *   **デプロイ前:** Activityを含むWorkerをデプロイする前に、必ず `GEMINI_API_KEY` 環境変数が適切に設定されていることを確認してください。\n    *   **運用時:** CI/CDパイプラインやデプロイメントスクリプトで、この環境変数が安全かつ確実に設定されるように自動化を検討してください。Kubernetes Secretsなどのシークレット管理サービスを利用することで、APIキーの漏洩リスクを低減できます。\n\nこのエラーは通常、システムの初期設定ミスに起因するため、一度正しく設定されれば頻繁に発生するものではありません。\n\n#### 7.2. Gemini API呼び出し時の潜在的エラーとその対策\nGemini API呼び出し時には、以下のような様々なエラーが発生する可能性があります。\n\n1.  **ネットワークエラー:**\n    *   **原因:** APIエンドポイントへの接続問題、DNS解決失敗、タイムアウトなど。\n    *   **対策:**\n        *   **Temporalリトライ:** ActivityはTemporalの組み込みリトライポリシーによって自動的に再試行されます。ネットワークの一時的な問題は多くの場合、数回の再試行で解決します。\n        *   **タイムアウト設定:** `google-generativeai` クライアント自体にタイムアウト設定を追加することで、API呼び出しが長時間ブロックされるのを防げます。\n2.  **APIレート制限 (Rate Limit):**\n    *   **原因:** 短期間にAPIリクエストが集中し、Googleの許容リクエストレートを超過した場合。\n    *   **対策:**\n        *   **Temporalリトライとバックオフ:** TemporalのExponential Backoffリトライポリシーを設定することで、リクエスト間隔を徐々に長くしてレート制限を回避できます。\n        *   **トークンバケット/リーキーバケット:** Workerプロセス自体にクライアントサイドのレートリミッターを実装し、APIリクエストを制御することも有効です。\n        *   **スケーリング:** Workerインスタンスを増やすことで、全体としての処理能力を向上させつつ、個々のWorkerからのリクエストレートを管理できます。\n3.  **LLMによる応答生成失敗:**\n    *   **原因:** プロンプトが複雑すぎる、LLMが安全ポリシーに違反するコンテンツを検知した、内部エラーなどにより、LLMが有効な応答を生成できなかった場合。\n    *   **対策:**\n        *   **プロンプトの最適化:** プロンプトをより明確にし、曖昧さを排除し、LLMにとって理解しやすい形式にすることで、生成失敗のリスクを減らせます。\n        *   **モデルの選択:** より強力なモデル（例: `gemini-1.5-pro`）を使用することで、複雑なプロンプトに対するロバスト性を高めることができます。\n        *   **フォールバック:** 特定の回数リトライしても成功しない場合、エラーをWorkflowに返し、人間による介入や別の処理フローに切り替えることを検討します。\n\n本Activityのコードには明示的な `try-except` ブロックは含まれていませんが、Temporal Activityとしての性質上、未処理の例外はWorkflowに伝播し、設定されたリトライポリシーに従って処理されます。\n\n#### 7.3. Temporal Activityリトライの設定と効果\nTemporalの組み込みリトライメカニズムは、本Activityの堅牢性を大幅に向上させます。WorkflowからActivityを呼び出す際に、`start_to_close_timeout` と `retry_policy` を設定できます。\n\n**Workflowからの呼び出し例:**\n\n```python\nfrom temporalio.client import Client, WorkflowHandle\nfrom temporalio.worker import Worker\nfrom temporalio.common import RetryPolicy\n\n# ... Activity と Workflow の定義 ...\n\nasync def main():\n    client = await Client.connect(\"localhost:7233\")\n    # Workerの起動 (Activityを登録)\n    # ...\n\n    result = await client.execute_activity(\n        generate_sop_phase_activity,\n        SOPRequest(\n            topic=\"Temporal SOP 生成 Activity\",\n            phase=\"outline\",\n            source_code=\"...\",\n        ),\n        id=\"generate_outline_123\", # アクティビティIDは冪等性のため重要\n        task_queue=\"sop-generation-task-queue\",\n        start_to_close_timeout=timedelta(minutes=5), # アクティビティの実行時間制限\n        retry_policy=RetryPolicy(\n            initial_interval=timedelta(seconds=1),\n            backoff_coefficient=2.0,\n            maximum_interval=timedelta(minutes=1),\n            maximum_attempts=5, # 最大5回リトライ\n            non_retryable_error_types=[\"EnvironmentError\"], # このエラーはリトライしない\n        ),\n    )\n    # ... 結果処理 ...\n```\n\n*   **`start_to_close_timeout`:** Activityが開始されてから完了するまでの最大許容時間。この時間内に完了しない場合、Activityはタイムアウトと見なされ、リトライポリシーが適用されます。Gemini APIの応答時間によっては、適切な値を設定することが重要です。\n*   **`retry_policy`:**\n    *   **`initial_interval`:** 最初の再試行までの待機時間。\n    *   **`backoff_coefficient`:** 各再試行で待機時間を乗算する係数（例: 2.0で指数バックオフ）。\n    *   **`maximum_interval`:** 再試行間の最大待機時間。\n    *   **`maximum_attempts`:** 最大再試行回数。これを超えるとActivityは最終的に失敗としてWorkflowに報告されます。\n    *   **`non_retryable_error_types`:** 指定されたエラータイプが発生した場合は、リトライせず直ちにActivityを失敗とします。`EnvironmentError` (例: `GEMINI_API_KEY` 未設定) は通常、設定ミスでありリトライしても解決しないため、ここに含めるのが適切です。\n\nTemporalのリトライ機構により、Activity自体はビジネスロジックに集中でき、一時的な外部サービスの問題に対する回復性はプラットフォームによって提供されます。\n\n---\n\n### 8. 実行方法とデプロイ\n\n本ActivityをTemporal環境で実行するには、WorkerのセットアップとWorkflowからの呼び出しが必要です。\n\n#### 8.1. Temporal WorkerへのActivity登録\nActivityは、`temporalio.worker.Worker` インスタンスに登録され、特定のタスクキューをリッスンするWorkerによって実行されます。\n\n```python\nimport asyncio\nfrom temporalio.worker import Worker\nfrom temporalio.client import Client\n\nfrom my_activities import generate_sop_phase_activity # Activityのインポート\nfrom my_workflows import SOPGenerationWorkflow       # Workflowのインポート (参考)\n\nasync def run_worker():\n    client = await Client.connect(\"localhost:7233\") # Temporal Serviceに接続\n\n    worker = Worker(\n        client,\n        task_queue=\"sop-generation-task-queue\", # Activityがリッスンするタスクキュー名\n        activities=[generate_sop_phase_activity], # 登録するActivityのリスト\n        workflows=[SOPGenerationWorkflow],       # 関連するWorkflowも登録 (通常は同じWorkerで処理)\n    )\n    print(\"Starting Worker...\")\n    await worker.run() # Workerを起動し、タスクキューからのタスクを待機\n\nif __name__ == \"__main__\":\n    # GEMINI_API_KEY 環境変数を設定してから実行してください\n    # export GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n    asyncio.run(run_worker())\n```\n\n*   **`Client.connect()`:** Temporal Serviceへの接続を確立します。デフォルトでは `localhost:7233` に接続します。\n*   **`Worker` インスタンスの作成:**\n    *   `client`: 上記で作成したTemporal Client。\n    *   `task_queue`: このWorkerが処理するタスクキューの名前。ActivityとWorkflowは同じタスクキューに登録されます。\n    *   `activities`: このWorkerが実行できるActivity関数のリスト。`generate_sop_phase_activity` をここに追加します。\n    *   `workflows`: このWorkerが実行できるWorkflow関数のリスト（オプションですが、通常は関連するWorkflowも登録します）。\n*   **`worker.run()`:** Workerを起動し、指定されたタスクキューからActivityタスクやWorkflowタスクをポーリングして実行します。\n\n#### 8.2. WorkflowからのActivity呼び出し例\nWorkflowから `generate_sop_phase_activity` を呼び出す際は、`temporalio.workflow.execute_activity` を使用します。\n\n```python\nfrom temporalio import workflow\nfrom datetime import timedelta\n\nfrom my_activities import generate_sop_phase_activity\nfrom core.models import SOPRequest, LLMResult # データモデルのインポート\n\n@workflow.defn\nclass SOPGenerationWorkflow:\n    @workflow.run\n    async def run(self, topic: str, source_code: str) -> str:\n        # Outline フェーズ\n        outline_request = SOPRequest(\n            topic=topic,\n            phase=\"outline\",\n            source_code=source_code,\n            attempt=0, # 初回試行\n        )\n        outline_result: LLMResult = await workflow.execute_activity(\n            generate_sop_phase_activity,\n            outline_request,\n            start_to_close_timeout=timedelta(minutes=5),\n            retry_policy=workflow.RetryPolicy(maximum_attempts=3),\n            # Activity ID は Workflow の決定論的実行を保証するため重要\n            # ユニークかつ再試行で同じ ID になるように設計\n            id=f\"generate_outline-{workflow.uuid4()}\",\n        )\n        generated_outline = outline_result.text\n\n        workflow.logger.info(f\"Generated Outline:\\n{generated_outline}\")\n\n        # ここで人間によるレビューや承認を待つシグナルを待機するロジックを挟むことも可能\n        # 例: await workflow.wait_for_external_signal(\"human_approval_signal\")\n\n        # Draft フェーズ\n        draft_request = SOPRequest(\n            topic=topic,\n            phase=\"draft\",\n            source_code=source_code,\n            outline=generated_outline, # 承認済みアウトラインを渡す\n            attempt=0,\n        )\n        draft_result: LLMResult = await workflow.execute_activity(\n            generate_sop_phase_activity,\n            draft_request,\n            start_to_close_timeout=timedelta(minutes=15),\n            retry_policy=workflow.RetryPolicy(maximum_attempts=3),\n            id=f\"generate_draft-{workflow.uuid4()}\",\n        )\n        generated_draft = draft_result.text\n\n        workflow.logger.info(f\"Generated Draft:\\n{generated_draft}\")\n\n        # ... review フェーズの呼び出しも同様 ...\n\n        return generated_draft # 例として最終ドラフトを返す\n```\n\n*   **`await workflow.execute_activity()`:** WorkflowからActivityを非同期で呼び出します。\n    *   第一引数: 呼び出すActivity関数 (`generate_sop_phase_activity`)。\n    *   第二引数: Activityへの入力データ（`SOPRequest` オブジェクト）。\n    *   `start_to_close_timeout`: Activityの実行タイムアウト。LLMの応答時間に応じて適切な値を設定します。\n    *   `retry_policy`: Activity固有のリトライポリシー。Workflowレベルでデフォルト設定も可能ですが、Activityごとに調整することも重要です。\n    *   `id`: ActivityのユニークなID。Workflowが決定論的であるために重要です。Workflowリプレイ時に同じActivityが同じIDで実行されることを保証します。`workflow.uuid4()` などを使用してユニークなIDを生成し、フェーズ名などと組み合わせるのが一般的です。\n\n#### 8.3. デプロイメントとスケーリング\nSOP生成システムを本番環境にデプロイする際の考慮事項：\n\n*   **コンテナ化 (Docker):** WorkerアプリケーションをDockerコンテナとしてパッケージ化することを強く推奨します。これにより、環境依存性を排除し、どこでも一貫した動作を保証できます。`Dockerfile` にはPython環境と必要なライブラリのインストール手順、およびWorkerの起動コマンドを含めます。\n*   **コンテナオーケストレーション (Kubernetes):** DockerコンテナをKubernetesなどのコンテナオーケストレーションプラットフォームにデプロイすることで、Workerアプリケーションのデプロイ、管理、スケーリングが容易になります。\n    *   **Pod定義:** Workerコンテナ、環境変数（特に `GEMINI_API_KEY` をKubernetes Secretsとして）、リソース要求/制限を含むPod定義を作成します。\n    *   **Deployment:** 複数のWorker Podを管理し、ローリングアップデートなどを可能にするDeploymentリソースを使用します。\n    *   **Service:** 必要に応じて、Workerが外部からアクセスする必要がある場合にServiceを定義します（ただし、通常Temporal WorkerはPullモデルで動作するため不要なことが多い）。\n*   **スケーリング:**\n    *   SOP生成の負荷（Workflow実行数）に応じて、Workerのレプリカ数を増減させることでスケーリングします。\n    *   Kubernetesの場合、Horizontal Pod Autoscaler (HPA) を使用して、CPU使用率やカスタムメトリクス（例: タスクキューのバックログサイズ）に基づいてWorker Podを自動的にスケーリングできます。\n    *   Gemini APIのレート制限に注意し、WorkerのスケールアップがAPIリクエストレートの許容範囲内であることを確認してください。\n\n---\n\n### 9. 運用上の考慮事項\n\n本Activityを本番環境で運用する上で、セキュリティ、コスト、パフォーマンスは特に重要な側面です。\n\n#### 9.1. APIキーのセキュアな管理\n`GEMINI_API_KEY` は、Google Gemini APIへのフルアクセス権を持つ機密情報です。以下の方法で安全に管理する必要があります。\n\n*   **環境変数:** 開発環境や小規模なデプロイでは、環境変数として設定することが一般的です。ただし、OSのプロセスリストやログファイルから漏洩するリスクがないことを確認してください。\n*   **シークレット管理サービス:** 本番環境では、以下のいずれかの専用シークレット管理サービスを使用することを強く推奨します。\n    *   **AWS Secrets Manager:** AWS環境で利用。APIキーを暗号化して保存し、IAMロールに基づいてアクセスを許可します。\n    *   **GCP Secret Manager:** Google Cloud Platform環境で利用。同様にキーの暗号化とIAMによるアクセス制御を提供します。\n    *   **Kubernetes Secrets:** Kubernetesクラスター内でシークレットを管理します。Base64エンコードされるだけで暗号化はされないため、ディスクレベルの暗号化やRBACポリシーと組み合わせて利用します。\n*   **原則:**\n    *   **コードにハードコードしない:** 絶対にソースコード内にAPIキーを直接書き込まないでください。\n    *   **最小権限の原則:** APIキーには、そのActivityが必要とする最小限の権限のみを付与してください。\n    *   **ローテーション:** 定期的にAPIキーをローテーションし、万が一漏洩した場合の影響を最小限に抑えてください。\n\n#### 9.2. コストとレートリミットの最適化\nGemini APIの利用には料金が発生し、またAPI呼び出しにはレートリミットが設定されています。\n\n*   **コストの最適化:**\n    *   **モデル選択:** `_MODEL = \"gemini-2.5-flash\"` を使用しているのは、`gemini-1.5-pro` などのより高性能なモデルに比べてコスト効率が良いためです。SOP生成の要件（品質、速度）に応じて、適切なモデルを選択することが重要です。\n    *   **プロンプトの最適化:**\n        *   **簡潔性:** 不要な冗長な情報をプロンプトから削除し、トークン数を削減します。\n        *   **入力のフィルタリング:** 大量のソースコードをそのまま渡すのではなく、関連性の高い部分のみを抽出して渡すことを検討します。\n        *   **アウトラインの短縮:** `outline` や `draft` の情報量を必要最低限に抑えることで、入力トークンを削減します。\n    *   **キャッシング:** 同じ入力に対して同じSOPコンテンツを生成する場合、LLMの応答をキャッシュして再利用することでAPI呼び出しを減らし、コストを削減できます。\n*   **レートリミットへの対応:**\n    *   **リトライとバックオフ:** Temporalの `RetryPolicy` による指数バックオフは、レートリミットエラーに対する最初の防衛線です。\n    *   **Workerのスケーリング計画:** 大量のSOPを生成する場合、Workerの数を増やして並列処理を行うことになりますが、その結果としてAPIリクエストレートも増加します。Google CloudのモニタリングツールでAPI使用状況を監視し、必要に応じてレートリミットの引き上げをリクエストするか、クライアントサイドでのレート制御を実装してください。\n\n#### 9.3. ログ監視とパフォーマンスチューニング\nシステムの健全性と効率性を維持するためには、継続的な監視とパフォーマンスチューニングが不可欠です。\n\n*   **Activityの実行ログ:**\n    *   Temporal Workerのログは、Activityの開始、完了、および発生したエラーに関する情報を提供します。構造化ログ（JSON形式など）を採用することで、ログ分析ツールでの検索や集計が容易になります。\n    *   `workflow.logger.info()` を用いて、Activityの各フェーズの進捗、LLMからの主要な応答、エラーメッセージなどをログに出力します。\n*   **LLMの応答時間 (`latency_ms`):**\n    *   `LLMResult` に含まれる `latency_ms` は、Gemini API呼び出しのパフォーマンスを直接測定する重要なメトリクスです。\n    *   これらのデータを集計し、平均、中央値、99パーセンタイルなどのレイテンシメトリクスを監視することで、API応答のパフォーマンス傾向を把握し、潜在的なボトルネックを特定できます。\n*   **トークン消費量 (`input_tokens`, `output_tokens`, `total_tokens`):**\n    *   これらのメトリクスは、APIコストに直接影響します。長期的に監視することで、プロンプトの変更やモデルの選択がコストに与える影響を評価できます。\n*   **エラー率:**\n    *   Gemini API呼び出しの成功率とエラー率を監視します。エラー率の急激な上昇は、API側の問題、レート制限の超過、またはプロンプトの問題を示唆する可能性があります。\n*   **パフォーマンスチューニング:**\n    *   **プロンプトの最適化:** レイテンシやトークン消費量が問題となる場合、プロンプトの簡潔化、必要な情報の厳選、より効率的なモデルへの切り替えなどを検討します。\n    *   **非同期処理:** Activityは非同期で設計されていますが、`client.models.generate_content` 呼び出し以外の部分で同期的なブロッキングI/Oがないか確認します。\n    *   **Workerリソース:** WorkerのCPU、メモリ、ネットワークリソースが十分であることを確認します。リソースが不足していると、Activityの実行が遅延したり、エラーが発生したりする可能性があります。\n\nこれらの運用上の考慮事項は、本Activityを安定かつ効率的にSOP生成パイプラインの一部として機能させるために不可欠です。\n\n---\n\n### 10. 付録\n\n#### 10.1. `core.models.SOPRequest` データ構造定義\n\n```python\n# core/models.py\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass SOPRequest(BaseModel):\n    \"\"\"\n    SOP 生成 Activity への入力データモデル。\n    LLM に与えるコンテキストと現在の処理フェーズを定義します。\n    \"\"\"\n    topic: str = Field(\n        ...,\n        description=\"SOPが対象とするトピックまたは機能の簡潔な説明。\",\n        examples=[\"Temporal SOP 生成 Activity — フェーズ別 Gemini 呼び出し実装\"],\n    )\n    phase: str = Field(\n        ...,\n        description=\"現在のSOP生成フェーズ ('outline', 'draft', 'review')。\",\n        examples=[\"outline\", \"draft\", \"review\"],\n    )\n    source_code: Optional[str] = Field(\n        None,\n        description=\"ドキュメント化対象のソースコード。outline および draft フェーズで使用。\",\n        examples=[\"def my_func():\\n    pass\"],\n    )\n    outline: Optional[str] = Field(\n        None,\n        description=\"承認済みのアウトライン（draft および review フェーズで使用）。\",\n        examples=[\"## 1. はじめに\\n目的とスコープ。\"],\n    )\n    draft: Optional[str] = Field(\n        None,\n        description=\"SOPの草稿（review フェーズで使用）。\",\n        examples=[\"# SOPタイトル\\n本文...\"],\n    )\n    previous_output: Optional[str] = Field(\n        None,\n        description=\"前回のLLM出力。リトライ時にLLMが過去の応答を参照するために使用。\",\n        examples=[\"前回のアウトラインの出力テキスト\"],\n    )\n    feedback: Optional[str] = Field(\n        None,\n        description=\"人間からのフィードバック。リトライ時にLLMが改善を行うための指示として使用。\",\n        examples=[\"章立ての項目3をより具体的にしてください。\"],\n    )\n    attempt: int = Field(\n        0,\n        description=\"現在の試行回数（初回は0）。リトライ時にインクリメントされ、プロンプトの分岐に使用。\",\n        ge=0, # 0以上の整数\n    )\n```\n\n#### 10.2. `core.models.LLMResult` データ構造定義\n\n```python\n# core/models.py\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass LLMResult(BaseModel):\n    \"\"\"\n    LLM からの応答と関連するメトリクスを格納するデータモデル。\n    \"\"\"\n    text: str = Field(\n        ...,\n        description=\"LLMからの生成テキスト（例: アウトライン、SOP草稿、レビュー結果）。\",\n        examples=[\"# SOP\\n## 1. はじめに\\n...\"],\n    )\n    model: str = Field(\n        ...,\n        description=\"応答を生成するために使用されたLLMモデルの識別子。\",\n        examples=[\"gemini-2.5-flash\"],\n    )\n    input_tokens: int = Field(\n        ...,\n        description=\"LLMへの入力に消費されたトークン数。\",\n        ge=0, # 0以上の整数\n    )\n    output_tokens: int = Field(\n        ...,\n        description=\"LLMの出力に消費されたトークン数。\",\n        ge=0, # 0以上の整数\n    )\n    total_tokens: int = Field(\n        ...,\n        description=\"合計トークン数 (入力 + 出力)。\",\n        ge=0, # 0以上の整数\n    )\n    latency_ms: float = Field(\n        ...,\n        description=\"LLM API呼び出しのレイテンシ（ミリ秒）。\",\n        ge=0.0, # 0.0以上の浮動小数点数\n    )\n    error: Optional[str] = Field(\n        None,\n        description=\"エラーが発生した場合のエラーメッセージ。成功時には None。\",\n        examples=[\"API_KEY_NOT_SET\"],\n    )\n```\n\n#### 10.3. 関連ドキュメントと外部リソース\n\n*   **Temporal Python SDK ドキュメント:**\n    *   [Temporal Python SDK](https://python.temporal.io/index.html)\n    *   [Activities in Python](https://python.temporal.io/docs/activities/)\n*   **Google Gemini API ドキュメント:**\n    *   [Gemini API Overview](https://cloud.google.com/vertex-ai/docs/generative-ai/model-reference/gemini)\n    *   [Google AI Python SDK Quickstart](https://ai.google.dev/tutorials/python_quickstart)\n*   **Pydantic ドキュメント:**\n    *   [Pydantic Documentation](https://pydantic-docs.helpmanual.io/)\n*   **Markdown ガイド:**\n    *   [Markdown Guide](https://www.markdownguide.org/)",
      "phase": "draft",
      "phase_label": "フェーズ2: 詳細執筆",
      "tokens": 19081
    },
    {
      "approved": true,
      "attempt": 1,
      "feedback": "「エラーハンドリングと retry_policy の設計」章を追加し、実際に動作する Python コードスニペット（retry_policy の設定例）を含めてください。",
      "latency_ms": 80209.49,
      "output": "はい、承知いたしました。人間からのフィードバックに基づき、特に「エラーハンドリングとリトライ機構」の章を強化し、その設計思想と具体的な `retry_policy` の設定例について、より詳細で実践的な説明を加えて改善しました。\n\n---\n\n## 標準作業手順書（SOP）- Temporal SOP 生成 Activity — フェーズ別 Gemini 呼び出し実装\n\n### 1. はじめに\n*   **対象読者:** 全ての関係者（ドキュメントアーキテクト、開発者、運用担当者）\n*   **所要時間の目安:** 5分\n本SOPは、Temporal Activityとして動作する「SOP生成フェーズ別Gemini呼び出し」機能の目的、構造、および運用方法について記述します。\n\n#### 1.1. ドキュメントの目的とスコープ\n本SOPは、Temporal Workflow内で動作するActivityである `generate_sop_phase_activity` の詳細を記述します。このActivityは、標準作業手順書（SOP）を自動生成するプロセスにおいて、特定のフェーズ（章立て提案、詳細執筆、最終レビュー）に応じてGoogle Geminiモデルを呼び出し、その応答を処理することを目的としています。これにより、SOP生成ワークフローの各段階でのLLM（大規模言語モデル）とのインタラクションを標準化し、信頼性と保守性を向上させます。\n\n本SOPは、`generate_sop_phase_activity` Activityの設計、実装、使用方法、および運用上の考慮事項に焦点を当てます。\n\n#### 1.2. 対象読者と前提知識\n本SOPが想定する読者層（役割）と、彼らが持つべき基本的な技術的知識レベルを説明します。\n*   **対象読者:**\n    *   **ドキュメントアーキテクト:** SOP生成プロセスの全体設計、LLMへの指示内容、および期待される出力形式に関心がある方。\n    *   **開発者:** 本Activityの実装、拡張、およびデバッグを行う方。\n    *   **運用担当者:** 本Activityを含むシステム全体のデプロイ、監視、およびトラブルシューティングを担当する方。\n*   **前提知識:**\n    *   Pythonプログラミングの基礎知識。\n    *   Temporal WorkflowおよびActivityの基本的な概念。\n    *   RESTful APIの利用経験、特にGoogle Gemini APIまたは類似のLLM APIに関する基礎知識。\n    *   Markdown形式でのドキュメント作成に関する理解。\n\n#### 1.3. 用語集\nSOP内で使用される主要な専門用語（例: Temporal Activity, Workflow, LLM, Geminiなど）の定義を提供します。\n*   **SOP (Standard Operating Procedure):** 標準作業手順書。特定の作業やプロセスを標準化するための詳細な手順書。\n*   **Temporal:** 分散システムの耐障害性とスケーラビリティを向上させるためのオープンソースのオーケストレーションプラットフォーム。\n*   **Workflow:** Temporalにおける、一連のビジネスロジックをオーケストレーションするコードの実行単位。長時間実行され、耐障害性を持つ。\n*   **Activity:** Temporal Workflowから呼び出される、具体的なタスク（例: 外部API呼び出し、データベース操作）を実行するコードの単位。Activityは冪等であるべきであり、リトライ可能。\n*   **LLM (Large Language Model):** 大規模言語モデル。人間のようなテキスト生成、理解、要約などを行うAIモデル。本SOPではGoogle Geminiモデルを指す。\n*   **Gemini API:** Googleが提供するLLM「Gemini」にプログラムからアクセスするためのAPI。\n*   **System Instructions (システム指示):** LLMに対して、その役割や振る舞いを指示するためのプロンプト。モデルの応答の質と方向性を決定する上で重要。\n*   **Prompt (プロンプト):** LLMに与える入力テキスト。\n*   **Task Queue:** Temporal WorkerがActivityタスクを受け取るためのキュー。\n*   **Retry Policy:** TemporalがActivityやWorkflowの失敗時に再試行する際のルール（最大試行回数、バックオフ間隔など）を定義するポリシー。\n\n### 2. システム概要\n*   **対象読者:** 開発者、ドキュメントアーキテクト\n*   **所要時間の目安:** 10分\nこのActivityは、SOP生成ワークフローにおいて、指定されたフェーズ（章立て提案、詳細執筆、最終レビュー）に応じてGeminiモデルを呼び出す役割を担います。\n\n#### 2.1. `generate_sop_phase_activity`の役割\n本ActivityがSOP生成パイプライン全体の中でどのような機能的役割を果たすかを説明します。\n`generate_sop_phase_activity` Activityは、SOP生成ワークフローの中核を担う機能であり、以下の役割を果たします。\n\n1.  **フェーズに応じたLLMの振る舞い制御:** SOP生成プロセスの各フェーズ（章立て提案、詳細執筆、最終レビュー）に応じて、Geminiモデルに異なる「役割」と指示を与えます。\n2.  **動的なプロンプト生成:** SOP生成に必要な情報（対象のソースコード、承認済みアウトライン、前回の出力、人間からのフィードバックなど）を組み合わせて、Geminiモデルに最適なプロンプトを動的に生成します。\n3.  **Gemini API呼び出し:** 構築されたプロンプトとシステム指示を用いて、Google Gemini APIを呼び出し、SOPの一部または全体を生成します。\n4.  **応答の構造化とメトリクス収集:** Geminiモデルからの応答を構造化された形式（`LLMResult`）で返し、同時にトークン消費量や処理レイテンシなどのメトリクスを収集します。\n\nこのActivityは、SOP生成ワークフローが、段階的かつ人間によるフィードバックを伴いながら、高品質なSOPを生成するための鍵となります。\n\n#### 2.2. SOP生成ワークフローにおける位置づけ\n上位のTemporal Workflowからどのように呼び出され、どのようなデータを受け渡し、結果を返すのかを概説します。\n本Activityは、SOP生成をオーケストレーションするTemporal Workflow（例: `SOPGenerationWorkflow`）から呼び出されます。ワークフローは以下のような流れで本Activityを利用することを想定しています。\n\n1.  **Outlineフェーズ:** Workflowが`generate_sop_phase_activity`を`phase=\"outline\"`で呼び出し、ソースコードを渡します。Activityはアウトラインを生成して返します。\n2.  **Human Review (Optional):** 生成されたアウトラインは人間によるレビューを受け、フィードバックが与えられる場合があります。\n3.  **Draftフェーズ:** Workflowは承認済みアウトラインとソースコード、そして必要に応じてフィードバックを伴って`generate_sop_phase_activity`を`phase=\"draft\"`で呼び出します。ActivityはSOPの草稿を生成します。\n4.  **Human Review (Optional):** 生成された草稿は人間によるレビューを受け、フィードバックが与えられる場合があります。\n5.  **Reviewフェーズ:** WorkflowはSOP草稿と必要に応じてフィードバックを伴って`generate_sop_phase_activity`を`phase=\"review\"`で呼び出します。Activityは改善点リストと最終版SOPを生成します。\n\nこのように、本ActivityはWorkflowの各段階でLLMとのインタラクションを抽象化し、Workflowのロジックを簡潔に保ちます。\n\n#### 2.3. 主要な技術要素\n本Activityを構成する主要な技術（Python, Temporal, Gemini API）と、その選定理由に軽く触れます。\n本Activityを構成する主要な技術要素は以下の通りです。\n\n*   **Python:** Activityの実装言語。その豊富なライブラリエコシステムと開発の容易さから採用されています。\n*   **Temporal Python SDK (`temporalio`):** PythonアプリケーションをTemporal Workflow/Activityとして実行するためのSDK。耐障害性とスケーラビリティを実現します。\n*   **Google Gemini API Python Client (`google-generativeai`):** Google Geminiモデルとインタラクトするための公式Pythonクライアントライブラリ。\n*   **環境変数:** APIキーなどの機密情報を安全に管理するために使用されます。\n\nこれらの技術の組み合わせにより、堅牢で柔軟なLLM駆動のSOP生成システムが構築されています。\n\n### 3. 前提条件\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安:** 15分\n本Activityを実行するために必要な環境設定、依存ライブラリ、およびAPIキーの設定（`GEMINI_API_KEY`）について説明します。\n\n#### 3.1. 動作環境と依存ライブラリ\n必要なPythonバージョン、`temporalio`、`google-generativeai`などのライブラリとそのインストール方法を記述します。\n`generate_sop_phase_activity` Activityを実行するためには、以下の環境が整っている必要があります。\n\n*   **Pythonバージョン:** 3.9以上（Temporalio SDKの推奨および互換性を考慮）。\n*   **オペレーティングシステム:** Linux, macOS, Windowsなどの一般的なOSで動作します。\n*   **依存ライブラリ:** 以下のPythonライブラリが必要です。`pip` を使用してインストールします。\n\n    ```bash\n    pip install temporalio google-generativeai pydantic\n    ```\n    *   `temporalio`: Temporal Python SDK。\n    *   `google-generativeai`: Google Gemini APIにアクセスするためのクライアントライブラリ。\n    *   `pydantic`: データモデル (`SOPRequest`, `LLMResult`) の定義とバリデーションに使用。\n\n#### 3.2. 必須APIキーと環境変数\n`GEMINI_API_KEY`の設定方法とその重要性、およびその他の関連する環境変数について詳述します。\nGoogle Gemini APIを利用するためには、APIキーが必要です。このAPIキーは安全な方法でActivityが実行される環境に提供される必要があります。\n\n*   **`GEMINI_API_KEY`:** Google Cloud Projectで発行されたGemini APIキー。\n    *   **設定方法:** Activityが実行される環境の環境変数として設定します。\n\n        **Linux/macOS:**\n        ```bash\n        export GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n        ```\n\n        **Windows (PowerShell):**\n        ```powershell\n        $env:GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n        ```\n\n        **Docker/Kubernetes:**\n        コンテナの環境変数として設定するか、シークレット管理サービス（例: Kubernetes Secrets, AWS Secrets Manager, GCP Secret Manager）を使用して注入することを強く推奨します。\n\n    *   **重要性:** APIキーがない場合、`generate_sop_phase_activity` は `EnvironmentError` を発生させて実行を停止します。これはAPIアクセス認証の必須要件であり、セキュアな運用のためにも適切に管理される必要があります。\n\n#### 3.3. Temporal Clientのセットアップ要件\nTemporal WorkerとServiceへの接続要件、および必要な認証情報について説明します。\n本Activityが動作するTemporal Workerは、Temporal Serviceに接続できる必要があります。\n\n*   **Temporal Serviceへの接続:**\n    *   Workerアプリケーションは、Temporal Serviceのエンドポイント（通常は `localhost:7233` またはデプロイされたクラウド環境のURL）にアクセスできる必要があります。\n    *   接続には、`temporalio.client.Client` オブジェクトを初期化する際に適切な設定（`target_host`, `namespace` など）が必要です。\n*   **認証情報 (Optional):**\n    *   Temporal Serviceが認証/認可を必要とする場合（例: mTLS）、Workerは適切な証明書やAPIキーを提供する必要があります。これらの設定は通常、`temporalio.client.Client` の初期化時に行われます。\n*   **タスクキュー:**\n    *   Activityは特定のタスクキューに登録され、そのタスクキューをリッスンしているWorkerによって実行されます。WorkerはActivityが登録されているタスクキューを指定して起動される必要があります。\n\n### 4. SOP生成フェーズの概念\n*   **対象読者:** 開発者、ドキュメントアーキテクト\n*   **所要時間の目安:** 10分\nSOP生成プロセスにおける主要な3つのフェーズ（`outline`, `draft`, `review`）について、それぞれの役割と目的を解説します。\n\n#### 4.1. 各フェーズ（outline, draft, review）の役割と目的\n各フェーズがSOP生成のどの段階を担当し、どのような成果物を期待するかを具体的に説明します。\nSOP生成プロセスは、段階的なアプローチを採用しており、`generate_sop_phase_activity` Activityはこのアプローチをサポートするために3つの主要なフェーズを定義しています。\n\n1.  **`outline` フェーズ:**\n    *   **役割:** 与えられたソースコードを分析し、SOPの基本的な構造である章立て（アウトライン）を提案します。\n    *   **目的:** 広範なSOPのトピックに対して、構造化された枠組みを迅速に提供し、その後の詳細なコンテンツ作成の指針とします。このフェーズの出力は、人間による確認と承認の対象となり得ます。\n    *   **期待される成果物:** 各章のタイトルと簡潔な説明を含むMarkdown形式のアウトライン。\n\n2.  **`draft` フェーズ:**\n    *   **役割:** 承認されたアウトラインとソースコードに基づき、SOPの本文を詳細に執筆します。\n    *   **目的:** アウトラインで定義された各章を具体的に展開し、実装の意図、手順、注意点、コード例など、SOPとして必要な情報を盛り込んだ草稿を作成します。\n    *   **期待される成果物:** 詳細なコンテンツを含むMarkdown形式のSOP草稿。\n\n3.  **`review` フェーズ:**\n    *   **役割:** 提供されたSOP草稿を徹底的にレビューし、改善点リストと最終版SOPを提供します。\n    *   **目的:** 草稿の正確性、完全性、明確さ、実用性を評価し、品質の高い最終SOPを出力します。このフェーズでは、必要に応じて草稿の修正や加筆提案が行われます。\n    *   **期待される成果物:** 【改善点リスト】と【最終版SOP】を構造化したMarkdown形式のテキスト。\n\n#### 4.2. リトライ機構とフェーズ遷移\n`attempt`カウントを用いたリトライ時の処理（`_retry`サフィックスの利用）と、フェーズ間の論理的な遷移について説明します。\n各フェーズは、人間からのフィードバックに基づいて再試行される可能性があります。本Activityは、入力パラメータ `request.attempt` を利用してこのリトライ状況を識別します。\n\n*   **`attempt` カウント:** `request.attempt` は、現在の試行回数を表します。\n    *   `attempt = 0`: 初回の実行。\n    *   `attempt > 0`: リトライ実行。\n*   **`_retry` サフィックスの利用:** `_SYSTEM` 辞書（後述）では、`\"outline\"`, `\"draft\"`, `\"review\"` に加えて、それぞれに対応するリトライ用のキー (`\"outline_retry\"`, `\"draft_retry\"`, `\"review_retry\"`) が定義されています。\n    *   `request.attempt > 0` の場合、`f\"{request.phase}_retry\"` というキーが使用され、LLMに「前回のアウトライン/草稿に対してフィードバックがあるので、それを反映して改善してください」という指示が与えられます。\n*   **フェーズ間の論理的な遷移:** ワークフローレベルでは、`outline` → `draft` → `review` という順序でフェーズが進行します。各フェーズ内でのリトライは、同じフェーズキー (`outline`, `draft`, `review`) を使いつつ、`attempt` カウントを増やして本Activityを再呼び出しすることで実現されます。これにより、LLMは前回の出力とフィードバックを参照して、より良い出力を生成しようと試みます。\n\n#### 4.3. LLMに与える役割の切り替え\n`_SYSTEM`辞書によるLLMの「役割」定義が、どのようにフェーズとリトライ状況に応じて切り替わるかを解説します。\n`_SYSTEM` 辞書は、各フェーズおよびリトライ状況に応じてLLMに与える「役割（System Instruction）」を定義しています。\n\n```python\n_SYSTEM = {\n    \"outline\": \"あなたは経験豊富なドキュメントアーキテクトです。...\",\n    \"outline_retry\": \"あなたは経験豊富なドキュメントアーキテクトです。前回のアウトラインに対して人間からフィードバックが届いています。...\",\n    \"draft\": \"あなたは熟練した技術ライターです。...\",\n    \"draft_retry\": \"あなたは熟練した技術ライターです。前回のSOP草稿に対して人間からフィードバックが届いています。...\",\n    \"review\": \"あなたは品質保証の専門家です。...\",\n    \"review_retry\": \"あなたは品質保証の専門家です。前回のレビュー結果に対して人間からフィードバックが届いています。...\",\n}\n```\n*   **フェーズごとの役割:**\n    *   `outline` フェーズでは「ドキュメントアーキテクト」として構造設計に集中させます。\n    *   `draft` フェーズでは「技術ライター」として詳細なコンテンツ作成に集中させます。\n    *   `review` フェーズでは「品質保証の専門家」として批判的視点での評価に集中させます。\n*   **リトライ時の役割強化:** リトライ時 (`_retry` サフィックス付きのキー) の指示には、「人間からのフィードバックを最優先で反映する」という強調が加えられ、LLMが単に再生成するのではなく、具体的な指示に従って改善を行うよう促しています。\n\nこの動的な役割の切り替えにより、各フェーズでLLMの能力を最大限に引き出し、高品質なSOP生成に貢献します。\n\n### 5. `generate_sop_phase_activity` Activityの詳細\n*   **対象読者:** 開発者\n*   **所要時間の目安:** 20分\nTemporal Activityとして定義された`generate_sop_phase_activity`のインターフェース、つまり入力データ型（`SOPRequest`）と出力データ型（`LLMResult`）について説明します。\n\n#### 5.1. Activityインターフェース (`@activity.defn`)\nActivityのデコレータ、関数シグネチャ、およびTemporal Activityとしての特性（例: 再試行可能性、ハートビート）について説明します。\n本Activityは `temporalio` ライブラリの `@activity.defn` デコレータを使用して定義されます。\n\n```python\nfrom temporalio import activity\nfrom core.models import SOPRequest, LLMResult\n\n@activity.defn\nasync def generate_sop_phase_activity(request: SOPRequest) -> LLMResult:\n    \"\"\"フェーズ別 SOP 生成 Activity。Gemini の system_instruction でフェーズを制御する。\"\"\"\n    # ... 実装 ...\n```\n\n*   **`@activity.defn`:** このデコレータは、指定された関数がTemporal Activityであることを示します。これにより、Temporal Workerがこの関数を発見し、Task Queueからタスクを受け取って実行できるようになります。\n*   **`async def`:** Activity関数は非同期関数として定義されています。これは、I/Oバウンドな操作（この場合はGemini API呼び出し）中にWorkerが他のActivityタスクを処理できるようにするためです。\n*   **関数シグネチャ:** `generate_sop_phase_activity(request: SOPRequest) -> LLMResult`\n    *   **入力:** `request` 引数は `SOPRequest` 型であり、SOP生成に必要な全ての情報を含みます。\n    *   **出力:** `LLMResult` 型のオブジェクトを返します。これはGeminiからの応答と関連するメトリクスをカプセル化したものです。\n*   **Temporal Activityとしての特性:**\n    *   **再試行可能性:** Temporal Workflowは、Activityの失敗時に組み込みのリトライメカニズムを提供します。本Activityはこのメカニズムを活用し、APIエラーなど一時的な問題が発生した場合に自動的に再試行されます。\n    *   **ハートビート (Heartbeat):** 長時間実行されるActivityは、進捗状況をTemporal Serviceに報告するためにハートビートを送信できます。このActivity自体はGemini API呼び出しのレイテンシに依存しますが、必要に応じて `activity.heartbeat()` を組み込むことで、WorkflowがActivityの進捗を監視し、タイムアウトを防ぐことができます（本SOPのコードには明示的なハートビートは含まれていませんが、必要に応じて追加可能です）。\n\n#### 5.2. 入力データモデル `SOPRequest`\n`SOPRequest`クラスの構造（`topic`, `phase`, `source_code`, `outline`, `draft`, `previous_output`, `feedback`, `attempt`など）とその各フィールドの役割を解説します。\n`SOPRequest` クラスは、`pydantic.BaseModel` を継承しており、Activityへの入力データを構造化してバリデーションします。\n\n```python\n# core/models.py (SOPRequestの定義を想定)\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass SOPRequest(BaseModel):\n    topic: str = Field(..., description=\"SOPが対象とするトピックまたは機能の簡潔な説明。\")\n    phase: str = Field(..., description=\"現在のSOP生成フェーズ ('outline', 'draft', 'review')。\")\n    source_code: Optional[str] = Field(None, description=\"ドキュメント化対象のソースコード。\")\n    outline: Optional[str] = Field(None, description=\"承認済みのアウトライン（draft/reviewフェーズで使用）。\")\n    draft: Optional[str] = Field(None, description=\"SOPの草稿（reviewフェーズで使用）。\")\n    previous_output: Optional[str] = Field(None, description=\"前回のLLM出力（リトライ時に使用）。\")\n    feedback: Optional[str] = Field(None, description=\"人間からのフィードバック（リトライ時に使用）。\")\n    attempt: int = Field(0, description=\"現在の試行回数（初回は0）。\")\n```\n\n*   **`topic` (str):** SOPが対象とするシステムや機能の簡潔な説明。LLMにSOPの主題を伝えるために使用されます。\n*   **`phase` (str):** 現在のSOP生成フェーズ。`\"outline\"`, `\"draft\"`, `\"review\"` のいずれかです。この値に基づいてLLMのシステム指示とプロンプトが構築されます。\n*   **`source_code` (Optional[str]):** ドキュメント化の対象となるソースコードの文字列。`outline` および `draft` フェーズでLLMに提供されます。\n*   **`outline` (Optional[str]):** 以前の `outline` フェーズで生成され、承認されたアウトライン。`draft` フェーズでSOP本文の構造の指針としてLLMに提供されます。\n*   **`draft` (Optional[str]):** 以前の `draft` フェーズで生成されたSOPの草稿。`review` フェーズでLLMによるレビュー対象となります。\n*   **`previous_output` (Optional[str]):** 前回のActivity実行でLLMから生成された出力。リトライ時 (`attempt > 0`) に、LLMが前回の結果を参照し、フィードバックを適用する際に使用されます。\n*   **`feedback` (Optional[str]):** 人間（または自動化された評価システム）からのフィードバックテキスト。リトライ時 (`attempt > 0`) に、LLMが改善を行うための具体的な指示として使用されます。\n*   **`attempt` (int):** 現在のActivityの試行回数。初回呼び出しは `0` で、リトライするごとにインクリメントされます。この値は、`_SYSTEM` 辞書から適切なシステム指示を選択するために使用されます。\n\nこれらのフィールドは、LLMに与える情報の粒度と関連性を制御し、各フェーズでのLLMの応答を最適化するために設計されています。\n\n#### 5.3. 出力データモデル `LLMResult`\n`LLMResult`クラスの構造（`text`, `model`, `input_tokens`, `output_tokens`, `total_tokens`, `latency_ms`など）と、LLMからの応答情報をどのように保持するかを説明します。\n`LLMResult` クラスは、Gemini APIからの応答と、その呼び出しに関するメトリクスを構造化して格納します。\n\n```python\n# core/models.py (LLMResultの定義を想定)\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass LLMResult(BaseModel):\n    text: str = Field(..., description=\"LLMからの生成テキスト。\")\n    model: str = Field(..., description=\"使用されたLLMモデルの識別子。\")\n    input_tokens: int = Field(..., description=\"LLMへの入力に消費されたトークン数。\")\n    output_tokens: int = Field(..., description=\"LLMの出力に消費されたトークン数。\")\n    total_tokens: int = Field(..., description=\"合計トークン数 (入力 + 出力)。\")\n    latency_ms: float = Field(..., description=\"LLM API呼び出しのレイテンシ（ミリ秒）。\")\n    error: Optional[str] = Field(None, description=\"エラーが発生した場合のエラーメッセージ。\")\n```\n\n*   **`text` (str):** Geminiモデルが生成した主なテキスト応答。これはアウトライン、SOP草稿、またはレビュー結果となります。\n*   **`model` (str):** 応答を生成するために使用されたGeminiモデルのID（例: `\"gemini-2.5-flash\"`）。\n*   **`input_tokens` (int):** プロンプト（入力）に費やされたトークン数。Gemini APIの `usage_metadata.prompt_token_count` から取得されます。\n*   **`output_tokens` (int):** 生成された応答（出力）に費やされたトークン数。Gemini APIの `usage_metadata.candidates_token_count` から取得されます。\n*   **`total_tokens` (int):** `input_tokens` と `output_tokens` の合計。APIコストの計算に直接関係します。Gemini APIの `usage_metadata.total_token_count` から取得されます。\n*   **`latency_ms` (float):** Gemini APIへのリクエスト送信から応答受信までの時間（ミリ秒単位）。パフォーマンス監視に有用です。\n*   **`error` (Optional[str]):** エラーが発生した場合に、エラーメッセージを格納します。成功時には `None` となります。\n\nこの構造化された出力により、WorkflowはLLMの応答を容易にパースして後続の処理に利用できるだけでなく、コストやパフォーマンスに関する重要なメトリクスも収集できます。\n\n### 6. コア機能の実装\n*   **対象読者:** 開発者\n*   **所要時間の目安:** 30分\n本Activityの中核をなす、システム指示の管理、LLMコンテンツの動的構築、およびGemini API呼び出しの実装について深掘りします。\n\n#### 6.1. システム指示（System Instructions）の管理 (`_SYSTEM`辞書)\n`_SYSTEM` 辞書は、各SOP生成フェーズおよびリトライの有無に応じて、Geminiモデルに与える「システム指示」を定義します。\n\n```python\n_SYSTEM = {\n    \"outline\": (\n        \"あなたは経験豊富なドキュメントアーキテクトです。\"\n        \"与えられたソースコードを分析し、標準作業手順書（SOP）の章立て（アウトライン）を提案してください。\"\n        \"各章のタイトルと1〜2行の説明を含め、明確で構造化されたアウトラインをMarkdownで出力してください。\"\n    ),\n    \"outline_retry\": (\n        \"あなたは経験豊富なドキュメントアーキテクトです。\"\n        \"前回のアウトラインに対して人間からフィードバックが届いています。\"\n        \"そのフィードバックを最優先で反映し、改善されたアウトラインをMarkdownで提供してください。\"\n    ),\n    # ... 他のフェーズの定義 ...\n}\n```\n\n*   **設計思想:**\n    *   **役割ベースのプロンプティング:** LLMに特定の専門家（ドキュメントアーキテクト、技術ライター、品質保証専門家）の役割を与えることで、その専門性に応じた思考プロセスと出力形式を促します。\n    *   **具体的な指示:** 各フェーズでLLMに何を期待するか（例: 「章立てを提案」「詳細を執筆」「レビューし改善点と最終版を提供」）を明確に記述します。\n    *   **出力形式の指定:** 「Markdownで出力してください」と明示することで、構造化された応答を期待します。\n    *   **リトライ時の指示強化:** リトライ (`_retry` サフィックス) 時には「フィードバックを最優先で反映する」という文言を加え、改善に対するLLMの集中を促します。\n*   **`system_instruction` の選択ロジック:**\n    `generate_sop_phase_activity` 関数内で、`request.phase` と `request.attempt` に基づいて適切な `system_instruction` が選択されます。\n\n    ```python\n    phase_key = f\"{request.phase}_retry\" if request.attempt > 0 else request.phase\n    system_instruction = _SYSTEM[phase_key]\n    ```\n    これにより、初回実行時は `request.phase` に対応するシステム指示が、リトライ時は `request.phase` に `_retry` を付加したシステム指示が選択されます。\n\nこの管理されたシステム指示により、LLMは各フェーズの目的に合致した、高品質で一貫性のある応答を生成することが可能になります。\n\n#### 6.2. LLMプロンプトコンテンツの動的生成 (`_build_contents`関数)\n`_build_contents` 関数は、`SOPRequest` オブジェクトからGeminiモデルに渡すための主要なプロンプトコンテンツを動的に構築します。\n\n```python\ndef _build_contents(request: SOPRequest) -> str:\n    phase = request.phase\n    is_retry = request.attempt > 0\n\n    if phase == \"outline\":\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## ソースコード\\n```python\\n{request.source_code}\\n```\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回のアウトライン（試行 #{request.attempt - 1}）\\n\"\n                f\"{request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善版アウトラインを提供してください。\"\n            )\n        return base + \"\\n\\n---\\n上記ソースコードの SOP アウトラインを提案してください。\"\n\n    elif phase == \"draft\":\n        # ... draftフェーズのロジック ...\n\n    else:  # review\n        # ... reviewフェーズのロジック ...\n```\n\n*   **関数の役割:** `_build_contents` は、`SOPRequest` の各フィールド（`topic`, `source_code`, `outline`, `draft`, `previous_output`, `feedback`）を組み合わせ、現在のフェーズとリトライ状況に応じた最適なプロンプトテキストを生成します。\n*   **フェーズごとのプロンプト構造:**\n    *   **`outline` フェーズ:**\n        *   初回 (`is_retry` が `False`): ドキュメント化対象のトピックとソースコードをLLMに提示し、「SOPアウトラインを提案してください」と指示します。\n        *   リトライ (`is_retry` が `True`): 上記に加え、前回のLLM出力 (`request.previous_output`) と人間からのフィードバック (`request.feedback`) を提示し、「フィードバックを反映した改善版アウトラインを提供してください」と明確に指示します。\n    *   **`draft` フェーズ:**\n        *   初回: ドキュメント化対象、承認済みアウトライン (`request.outline`)、およびソースコードを提示し、「詳細なSOP本文を執筆してください」と指示します。\n        *   リトライ: 上記に加え、前回の草稿とフィードバックを提示し、「フィードバックを反映した改善版草稿を提供してください」と指示します。\n    *   **`review` フェーズ:**\n        *   初回: ドキュメント化対象とSOP草稿 (`request.draft`) を提示し、「このSOP草稿をレビューし、【改善点リスト】と【最終版SOP】を提供してください」と指示します。\n        *   リトライ: 上記に加え、前回のレビュー結果とフィードバックを提示し、「フィードバックを反映した改善された最終版を提供してください」と指示します。\n*   **コード例（`draft` フェーズ）:**\n\n    ```python\n    elif phase == \"draft\":\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## 承認済みアウトライン\\n{request.outline}\\n\\n\"\n            f\"## ソースコード\\n```python\\n{request.source_code}\\n```\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回の草稿（試行 #{request.attempt - 1}）\\n\"\n                f\"{request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善版草稿を提供してください。\"\n            )\n        return base + \"\\n\\n---\\n上記アウトラインに基づき、詳細なSOP本文を執筆してください。\"\n    ```\n    この例から分かるように、各フェーズで必要な入力情報が異なるため、`_build_contents` 関数は非常に柔軟にプロンプトを構築します。Markdownのヘッダー (`##`) を使用して情報を構造化し、LLMが各セクションの内容を正確に理解できるように工夫されています。\n\n#### 6.3. Gemini API呼び出しと応答処理\n`google.genai`クライアントの初期化、モデル指定（`_MODEL`）、`system_instruction`の適用、`client.models.generate_content`の呼び出し、および応答から`LLMResult`を構築するプロセスを説明します。\n`generate_sop_phase_activity` 関数内で、`google.generativeai` クライアントを使用してGemini APIを呼び出し、その応答を処理します。\n\n```python\nasync def generate_sop_phase_activity(request: SOPRequest) -> LLMResult:\n    from google import genai\n    from google.genai import types\n\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n\n    client = genai.Client(api_key=api_key)\n\n    phase_key = f\"{request.phase}_retry\" if request.attempt > 0 else request.phase\n    system_instruction = _SYSTEM[phase_key]\n    contents = _build_contents(request)\n\n    start = time.monotonic()\n    response = await client.models.generate_content( # await を使用\n        model=_MODEL,\n        contents=contents,\n        config=types.GenerateContentConfig(system_instruction=system_instruction),\n    )\n    latency_ms = (time.monotonic() - start) * 1000\n\n    usage = response.usage_metadata\n    return LLMResult(\n        text=response.text,\n        model=_MODEL,\n        input_tokens=getattr(usage, \"prompt_token_count\", 0) or 0,\n        output_tokens=getattr(usage, \"candidates_token_count\", 0) or 0,\n        total_tokens=getattr(usage, \"total_token_count\", 0) or 0,\n        latency_ms=round(latency_ms, 2),\n    )\n```\n\n*   **`google.genai` クライアントの初期化:**\n    ```python\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n    client = genai.Client(api_key=api_key)\n    ```\n    環境変数 `GEMINI_API_KEY` からAPIキーを取得し、`genai.Client` を初期化します。キーが設定されていない場合は `EnvironmentError` を発生させます。\n*   **モデル指定 (`_MODEL`):**\n    ```python\n    _MODEL = \"gemini-2.5-flash\"\n    # ...\n    response = await client.models.generate_content(\n        model=_MODEL,\n        # ...\n    )\n    ```\n    事前に定義された `_MODEL` 変数 (`\"gemini-2.5-flash\"`) を使用して、呼び出すGeminiモデルを指定します。このモデルは高速かつコスト効率の良いテキスト生成に適しています。\n*   **`system_instruction` の適用:**\n    ```python\n    # ...\n    config=types.GenerateContentConfig(system_instruction=system_instruction),\n    ```\n    LLMに役割と振る舞いを指示する `system_instruction` は、`GenerateContentConfig` オブジェクトを通じてAPIリクエストに含められます。これはLLMの応答の質を大きく左右する重要な要素です。\n*   **`client.models.generate_content` の呼び出し:**\n    ```python\n    response = await client.models.generate_content(\n        model=_MODEL,\n        contents=contents,\n        config=types.GenerateContentConfig(system_instruction=system_instruction),\n    )\n    ```\n    `client.models.generate_content` メソッドを非同期で呼び出します。\n    *   `model`: 使用するGeminiモデル。\n    *   `contents`: `_build_contents` 関数で生成された主要なプロンプトテキスト。\n    *   `config`: `system_instruction` を含む追加設定。\n*   **応答処理と `LLMResult` の構築:**\n    ```python\n    latency_ms = (time.monotonic() - start) * 1000\n    usage = response.usage_metadata\n    return LLMResult(\n        text=response.text,\n        model=_MODEL,\n        input_tokens=getattr(usage, \"prompt_token_count\", 0) or 0,\n        output_tokens=getattr(usage, \"candidates_token_count\", 0) or 0,\n        total_tokens=getattr(usage, \"total_token_count\", 0) or 0,\n        latency_ms=round(latency_ms, 2),\n    )\n    ```\n    API応答から以下の情報を抽出します。\n    *   `response.text`: LLMが生成したテキスト。\n    *   `response.usage_metadata`: トークン数に関する情報（`prompt_token_count`, `candidates_token_count`, `total_token_count`）。`getattr()` と `or 0` を使用して、これらの属性が存在しない場合でも安全にデフォルト値（0）を取得します。\n    *   `time.monotonic()` を使用してAPI呼び出しの開始と終了時刻を記録し、レイテンシを計算します。\n    これらの情報を用いて `LLMResult` オブジェクトを構築し、Activityの戻り値として返します。\n\nこのセクションで説明されたコア機能の実装は、本ActivityがSOP生成プロセスの各フェーズでLLMを効果的に利用するための基盤を形成します。\n\n### 7. エラーハンドリングとリトライ機構の設計\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安:** 15分\n本Activityは、外部サービスであるGemini APIに依存するため、エラーハンドリングとリトライ機構は堅牢な運用に不可欠です。本章では、エラー処理の戦略と、Temporalの組み込みリトライメカニズムをどのように活用し、設計するかについて解説します。\n\n#### 7.1. 環境設定エラーの処理 (`GEMINI_API_KEY`未設定)\nGemini APIを呼び出すには、有効なAPIキーが環境変数 `GEMINI_API_KEY` に設定されている必要があります。\n\n```python\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n```\n\n*   **エラー発生条件:** `os.environ.get(\"GEMINI_API_KEY\")` が `None` または空文字列を返す場合、この条件が真となり `EnvironmentError` が発生します。\n*   **設計上の考慮事項:**\n    *   この種のエラーは、環境の初期設定ミスに起因し、Activityを再試行しても解決することはありません。そのため、このようなエラーが発生した場合は、速やかにWorkflowを失敗させるべきです。\n    *   Temporalの `RetryPolicy` において、`EnvironmentError` を `non_retryable_error_types` に含めることで、無駄なリソース消費を防ぎ、運用担当者に迅速な介入を促すことができます。\n*   **対処法:**\n    *   **デプロイ前:** Activityを含むWorkerをデプロイする前に、必ず `GEMINI_API_KEY` 環境変数が適切に設定されていることを確認してください。\n    *   **運用時:** CI/CDパイプラインやデプロイメントスクリプトで、この環境変数が安全かつ確実に設定されるように自動化を検討してください。Kubernetes Secretsなどのシークレット管理サービスを利用することで、APIキーの漏洩リスクを低減できます。\n\n#### 7.2. Gemini API呼び出し時の潜在的エラーとその対策\nGemini API呼び出し時には、様々なエラーが発生する可能性があります。これらのエラーはActivityの未処理例外としてWorkflowに伝播し、Temporalのリトライポリシーによって処理されます。\n\n1.  **ネットワークエラー:**\n    *   **原因:** APIエンドポイントへの接続問題、DNS解決失敗、タイムアウトなど、一時的なインフラストストラクチャの問題。\n    *   **設計と対策:** これらは一時的な問題であるため、Temporalの組み込みリトライポリシーが非常に有効です。`initial_interval` と `backoff_coefficient` を適切に設定することで、自動的に再試行され、多くの場合は回復します。Activity自体に明示的な `try-except` を設けるよりも、Temporalの堅牢なメカニズムに任せるのが一般的です。\n2.  **APIレート制限 (Rate Limit):**\n    *   **原因:** 短期間にAPIリクエストが集中し、Googleの許容リクエストレートを超過した場合。\n    *   **設計と対策:** `google-generativeai` ライブラリは通常、レート制限エラーに対して特定の例外（例: `google.api_core.exceptions.ResourceExhausted`）を発生させます。Temporalの指数バックオフを含むリトライポリシーは、このような場合にリクエスト間隔を徐々に長くしてレート制限を回避するのに役立ちます。Workerプロセス自体にクライアントサイドのレートリミッターを実装し、APIリクエストを制御することも検討できます。\n3.  **LLMによる応答生成失敗:**\n    *   **原因:** プロンプトが複雑すぎる、LLMが安全ポリシーに違反するコンテンツを検知した、内部エラーなどにより、LLMが有効な応答を生成できなかった場合。\n    *   **設計と対策:**\n        *   **リトライ:** 一時的なLLM内部の問題であれば、リトライによって解決する可能性があります。\n        *   **プロンプトの最適化:** 継続的にこの問題が発生する場合、プロンプトの品質そのものが原因である可能性があります。プロンプトをより明確にし、曖昧さを排除し、LLMにとって理解しやすい形式にすることで、生成失敗のリスクを減らせます。\n        *   **フォールバック/人間による介入:** 特定の回数リトライしても成功しない場合、Activityは最終的に失敗します。Workflowは、この失敗を検知して人間による介入を要求するか、別の処理フロー（例: デフォルトコンテンツの生成）に切り替えることを検討すべきです。\n\n#### 7.3. Temporal Activityリトライポリシーの設計と適用\nTemporalの組み込みリトライメカニズムは、本Activityの堅牢性を大幅に向上させます。WorkflowからActivityを呼び出す際に、`start_to_close_timeout` と `retry_policy` を適切に設定することが、堅牢なエラーハンドリング設計の中核となります。\n\n**WorkflowからのActivity呼び出しと`RetryPolicy`の設定例:**\n\n```python\nfrom temporalio.client import Client, WorkflowHandle\nfrom temporalio.worker import Worker\nfrom temporalio.common import RetryPolicy\nfrom datetime import timedelta # timedelta をインポート\n\n# ... Activity と Workflow の定義 (省略) ...\n\nasync def start_sop_generation_workflow(client: Client, topic: str, source_code: str):\n    # Workflow実行の開始 (ここでは Workflow 自体の定義は省略)\n    # Workflow内部から generate_sop_phase_activity を呼び出す例を示す\n\n    # generate_sop_phase_activity のためのSOPRequestオブジェクト\n    outline_request = SOPRequest(\n        topic=topic,\n        phase=\"outline\",\n        source_code=source_code,\n        attempt=0, # 初回試行\n    )\n\n    # Activity の実行\n    try:\n        outline_result: LLMResult = await client.execute_activity(\n            generate_sop_phase_activity,\n            outline_request,\n            task_queue=\"sop-generation-task-queue\",\n            # Activityの実行時間制限とリトライポリシーの設計\n            start_to_close_timeout=timedelta(minutes=5), # Activityが完了するまでの最大時間\n            retry_policy=RetryPolicy(\n                initial_interval=timedelta(seconds=1),  # 最初の再試行までの待機時間\n                backoff_coefficient=2.0,                # 各再試行で待機時間を乗算する係数 (指数バックオフ)\n                maximum_interval=timedelta(minutes=1),  # 再試行間の最大待機時間\n                maximum_attempts=5,                     # 最大再試行回数。これを超えるとActivityは最終的に失敗\n                non_retryable_error_types=[\"EnvironmentError\", \"ValueError\"], # 特定のエラーはリトライしない\n            ),\n            # Activity ID は Workflow の決定論的実行を保証するため重要\n            # ユニークかつ再試行で同じ ID になるように設計することが推奨されます。\n            # 例: f\"generate_outline-{workflow.uuid4()}\" (Workflow内部から呼び出す場合)\n            id=\"generate-outline-for-my-sop-topic-v1\",\n        )\n        print(f\"SOP Outline Generated: {outline_result.text[:100]}...\")\n        return outline_result\n    except Exception as e:\n        print(f\"Activity failed after retries: {e}\")\n        raise # Workflow にエラーを再スロー\n\n# if __name__ == \"__main__\":\n#     # 通常は Worker が起動し、Workflow から Activity が呼び出されます。\n#     # このコードは Workflow 内部からの呼び出し方を具体的に示すためのものです。\n#     # 実際に実行する際は、Temporal Service と Worker が動作している必要があります。\n#     # client = await Client.connect(\"localhost:7233\")\n#     # await start_sop_generation_workflow(client, \"My Temporal Workflow\", \"...\")\n```\n\n*   **`start_to_close_timeout` の設計:**\n    *   Activityが開始されてから完了するまでの最大許容時間です。この時間内に完了しない場合、Activityはタイムアウトと見なされ、リトライポリシーが適用されます。\n    *   Gemini APIの応答時間、プロンプトの複雑さ、およびモデルの種類によって変動するため、適切な値を設定することが重要です。例えば、短い応答が期待される場合は数分、複雑な生成にはそれ以上の時間を設定します。\n*   **`RetryPolicy` の設計要素:**\n    *   **`initial_interval` (timedelta):** 最初の再試行までの待機時間。APIの一時的な障害やレート制限からの回復を考慮し、短すぎず長すぎない時間を設定します。\n    *   **`backoff_coefficient` (float):** 各再試行で待機時間を乗算する係数。`1.0` ならば固定間隔、`2.0` ならば指数バックオフとなります。外部APIのレート制限回避には指数バックオフ（`> 1.0`）が効果的です。\n    *   **`maximum_interval` (timedelta):** 再試行間の最大待機時間。指数バックオフが際限なく広がるのを防ぎ、リソースを過剰に占有しないように上限を設定します。\n    *   **`maximum_attempts` (int):** 最大再試行回数。これを超えるとActivityは最終的に失敗としてWorkflowに報告されます。無限ループを防ぎ、最終的な失敗をWorkflowに伝えるための重要なガードレールです。この回数は、一時的なエラーの頻度やビジネス要件に応じて決定します。\n    *   **`non_retryable_error_types` (List[str]):** 指定されたエラータイプが発生した場合は、リトライせず直ちにActivityを失敗とします。\n        *   `EnvironmentError` (例: `GEMINI_API_KEY` 未設定): 環境設定の根本的な問題であり、リトライしても解決しないため、ここに含めるのが適切です（前述の7.1節を参照）。\n        *   `ValueError` や `TypeError`: Activityへの入力データに問題がある場合など、ビジネスロジックやデータ構造の根本的な不整合を示すエラーも、リトライしても無意味なケースが多いため、ここに追加を検討します。これにより、無駄なリソース消費と時間の浪費を避け、迅速なデバッグと修正を促します。\n\nTemporalのリトライ機構により、Activity自体はビジネスロジックに集中でき、一時的な外部サービスの問題に対する回復性はプラットフォームによって提供されます。`RetryPolicy` を適切に設計することは、システムの安定性と運用効率を大きく左右します。\n\n### 8. 実行方法とデプロイ\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安:** 20分\n本ActivityをTemporal環境で実行するための具体的な手順と、デプロイに関する考慮事項を説明します。\n\n#### 8.1. Temporal WorkerへのActivity登録\n`Worker`インスタンスの作成、タスクキューの指定、および`generate_sop_phase_activity`をWorkerに登録するPythonコード例を示します。\nActivityは、`temporalio.worker.Worker` インスタンスに登録され、特定のタスクキューをリッスンするWorkerによって実行されます。\n\n```python\nimport asyncio\nfrom temporalio.worker import Worker\nfrom temporalio.client import Client\n\nfrom my_activities import generate_sop_phase_activity # Activityのインポート\nfrom my_workflows import SOPGenerationWorkflow       # Workflowのインポート (参考)\n\nasync def run_worker():\n    client = await Client.connect(\"localhost:7233\") # Temporal Serviceに接続\n\n    worker = Worker(\n        client,\n        task_queue=\"sop-generation-task-queue\", # Activityがリッスンするタスクキュー名\n        activities=[generate_sop_phase_activity], # 登録するActivityのリスト\n        workflows=[SOPGenerationWorkflow],       # 関連するWorkflowも登録 (通常は同じWorkerで処理)\n    )\n    print(\"Starting Worker...\")\n    await worker.run() # Workerを起動し、タスクキューからのタスクを待機\n\nif __name__ == \"__main__\":\n    # GEMINI_API_KEY 環境変数を設定してから実行してください\n    # export GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n    asyncio.run(run_worker())\n```\n\n*   **`Client.connect()`:** Temporal Serviceへの接続を確立します。デフォルトでは `localhost:7233` に接続します。\n*   **`Worker` インスタンスの作成:**\n    *   `client`: 上記で作成したTemporal Client。\n    *   `task_queue`: このWorkerが処理するタスクキューの名前。ActivityとWorkflowは同じタスクキューに登録されます。\n    *   `activities`: このWorkerが実行できるActivity関数のリスト。`generate_sop_phase_activity` をここに追加します。\n    *   `workflows`: このWorkerが実行できるWorkflow関数のリスト（オプションですが、通常は関連するWorkflowも登録します）。\n*   **`worker.run()`:** Workerを起動し、指定されたタスクキューからActivityタスクやWorkflowタスクをポーリングして実行します。\n\n#### 8.2. WorkflowからのActivity呼び出し例\nTemporal Workflowから`generate_sop_phase_activity`を呼び出す際の非同期実行（`await client.execute_activity`）や、入力パラメータの渡し方をコード例で示します。\nWorkflowから `generate_sop_phase_activity` を呼び出す際は、`temporalio.workflow.execute_activity` を使用します。\n\n```python\nfrom temporalio import workflow\nfrom datetime import timedelta\n\nfrom my_activities import generate_sop_phase_activity\nfrom core.models import SOPRequest, LLMResult # データモデルのインポート\n\n@workflow.defn\nclass SOPGenerationWorkflow:\n    @workflow.run\n    async def run(self, topic: str, source_code: str) -> str:\n        # Outline フェーズ\n        outline_request = SOPRequest(\n            topic=topic,\n            phase=\"outline\",\n            source_code=source_code,\n            attempt=0, # 初回試行\n        )\n        outline_result: LLMResult = await workflow.execute_activity(\n            generate_sop_phase_activity,\n            outline_request,\n            start_to_close_timeout=timedelta(minutes=5),\n            retry_policy=workflow.RetryPolicy(maximum_attempts=3),\n            # Activity ID は Workflow の決定論的実行を保証するため重要\n            # ユニークかつ再試行で同じ ID になるように設計\n            id=f\"generate_outline-{workflow.uuid4()}\",\n        )\n        generated_outline = outline_result.text\n\n        workflow.logger.info(f\"Generated Outline:\\n{generated_outline}\")\n\n        # ここで人間によるレビューや承認を待つシグナルを待機するロジックを挟むことも可能\n        # 例: await workflow.wait_for_external_signal(\"human_approval_signal\")\n\n        # Draft フェーズ\n        draft_request = SOPRequest(\n            topic=topic,\n            phase=\"draft\",\n            source_code=source_code,\n            outline=generated_outline, # 承認済みアウトラインを渡す\n            attempt=0,\n        )\n        draft_result: LLMResult = await workflow.execute_activity(\n            generate_sop_phase_activity,\n            draft_request,\n            start_to_close_timeout=timedelta(minutes=15),\n            retry_policy=workflow.RetryPolicy(maximum_attempts=3),\n            id=f\"generate_draft-{workflow.uuid4()}\",\n        )\n        generated_draft = draft_result.text\n\n        workflow.logger.info(f\"Generated Draft:\\n{generated_draft}\")\n\n        # ... review フェーズの呼び出しも同様 ...\n\n        return generated_draft # 例として最終ドラフトを返す\n```\n\n*   **`await workflow.execute_activity()`:** WorkflowからActivityを非同期で呼び出します。\n    *   第一引数: 呼び出すActivity関数 (`generate_sop_phase_activity`)。\n    *   第二引数: Activityへの入力データ（`SOPRequest` オブジェクト）。\n    *   `start_to_close_timeout`: Activityの実行タイムアウト。LLMの応答時間に応じて適切な値を設定します。\n    *   `retry_policy`: Activity固有のリトライポリシー。Workflowレベルでデフォルト設定も可能ですが、Activityごとに調整することも重要です。\n    *   `id`: ActivityのユニークなID。Workflowが決定論的であるために重要です。Workflowリプレイ時に同じActivityが同じIDで実行されることを保証します。`workflow.uuid4()` などを使用してユニークなIDを生成し、フェーズ名などと組み合わせるのが一般的です。\n\n#### 8.3. デプロイメントとスケーリング\nコンテナ化（Docker）、Kubernetesなどの環境でのデプロイ方法と、SOP生成の負荷に応じたWorkerのスケーリング戦略について言及します。\nSOP生成システムを本番環境にデプロイする際の考慮事項：\n\n*   **コンテナ化 (Docker):** WorkerアプリケーションをDockerコンテナとしてパッケージ化することを強く推奨します。これにより、環境依存性を排除し、どこでも一貫した動作を保証できます。`Dockerfile` にはPython環境と必要なライブラリのインストール手順、およびWorkerの起動コマンドを含めます。\n*   **コンテナオーケストレーション (Kubernetes):** DockerコンテナをKubernetesなどのコンテナオーケストレーションプラットフォームにデプロイすることで、Workerアプリケーションのデプロイ、管理、スケーリングが容易になります。\n    *   **Pod定義:** Workerコンテナ、環境変数（特に `GEMINI_API_KEY` をKubernetes Secretsとして）、リソース要求/制限を含むPod定義を作成します。\n    *   **Deployment:** 複数のWorker Podを管理し、ローリングアップデートなどを可能にするDeploymentリソースを使用します。\n    *   **Service:** 必要に応じて、Workerが外部からアクセスする必要がある場合にServiceを定義します（ただし、通常Temporal WorkerはPullモデルで動作するため不要なことが多い）。\n*   **スケーリング:**\n    *   SOP生成の負荷（Workflow実行数）に応じて、Workerのレプリカ数を増減させることでスケーリングします。\n    *   Kubernetesの場合、Horizontal Pod Autoscaler (HPA) を使用して、CPU使用率やカスタムメトリクス（例: タスクキューのバックログサイズ）に基づいてWorker Podを自動的にスケーリングできます。\n    *   Gemini APIのレート制限に注意し、WorkerのスケールアップがAPIリクエストレートの許容範囲内であることを確認してください。\n\n### 9. 運用上の考慮事項\n*   **対象読者:** 運用担当者、ドキュメントアーキテクト\n*   **所要時間の目安:** 15分\n本Activityを本番環境で運用する上での重要な側面、例えばセキュリティ、コスト、パフォーマンスなどについて記述します。\n\n#### 9.1. APIキーのセキュアな管理\n`GEMINI_API_KEY` は、Google Gemini APIへのフルアクセス権を持つ機密情報です。以下の方法で安全に管理する必要があります。\n\n*   **環境変数:** 開発環境や小規模なデプロイでは、環境変数として設定することが一般的です。ただし、OSのプロセスリストやログファイルから漏洩するリスクがないことを確認してください。\n*   **シークレット管理サービス:** 本番環境では、以下のいずれかの専用シークレット管理サービスを使用することを強く推奨します。\n    *   **AWS Secrets Manager:** AWS環境で利用。APIキーを暗号化して保存し、IAMロールに基づいてアクセスを許可します。\n    *   **GCP Secret Manager:** Google Cloud Platform環境で利用。同様にキーの暗号化とIAMによるアクセス制御を提供します。\n    *   **Kubernetes Secrets:** Kubernetesクラスター内でシークレットを管理します。Base64エンコードされるだけで暗号化はされないため、ディスクレベルの暗号化やRBACポリシーと組み合わせて利用します。\n*   **原則:**\n    *   **コードにハードコードしない:** 絶対にソースコード内にAPIキーを直接書き込まないでください。\n    *   **最小権限の原則:** APIキーには、そのActivityが必要とする最小限の権限のみを付与してください。\n    *   **ローテーション:** 定期的にAPIキーをローテーションし、万が一漏洩した場合の影響を最小限に抑えてください。\n\n#### 9.2. コストとレートリミットの最適化\nGemini APIの利用には料金が発生し、またAPI呼び出しにはレートリミットが設定されています。\n\n*   **コストの最適化:**\n    *   **モデル選択:** `_MODEL = \"gemini-2.5-flash\"` を使用しているのは、`gemini-1.5-pro` などのより高性能なモデルに比べてコスト効率が良いためです。SOP生成の要件（品質、速度）に応じて、適切なモデルを選択することが重要です。\n    *   **プロンプトの最適化:**\n        *   **簡潔性:** 不要な冗長な情報をプロンプトから削除し、トークン数を削減します。\n        *   **入力のフィルタリング:** 大量のソースコードをそのまま渡すのではなく、関連性の高い部分のみを抽出して渡すことを検討します。\n        *   **アウトラインの短縮:** `outline` や `draft` の情報量を必要最低限に抑えることで、入力トークンを削減します。\n    *   **キャッシング:** 同じ入力に対して同じSOPコンテンツを生成する場合、LLMの応答をキャッシュして再利用することでAPI呼び出しを減らし、コストを削減できます。\n*   **レートリミットへの対応:**\n    *   **リトライとバックオフ:** Temporalの `RetryPolicy` による指数バックオフは、レートリミットエラーに対する最初の防衛線です。\n    *   **Workerのスケーリング計画:** 大量のSOPを生成する場合、Workerの数を増やして並列処理を行うことになりますが、その結果としてAPIリクエストレートも増加します。Google CloudのモニタリングツールでAPI使用状況を監視し、必要に応じてレートリミットの引き上げをリクエストするか、クライアントサイドでのレート制御を実装してください。\n\n#### 9.3. ログ監視とパフォーマンスチューニング\nシステムの健全性と効率性を維持するためには、継続的な監視とパフォーマンスチューニングが不可欠です。\n\n*   **Activityの実行ログ:**\n    *   Temporal Workerのログは、Activityの開始、完了、および発生したエラーに関する情報を提供します。構造化ログ（JSON形式など）を採用することで、ログ分析ツールでの検索や集計が容易になります。\n    *   `workflow.logger.info()` を用いて、Activityの各フェーズの進捗、LLMからの主要な応答、エラーメッセージなどをログに出力します。\n*   **LLMの応答時間 (`latency_ms`):**\n    *   `LLMResult` に含まれる `latency_ms` は、Gemini API呼び出しのパフォーマンスを直接測定する重要なメトリクスです。\n    *   これらのデータを集計し、平均、中央値、99パーセンタイルなどのレイテンシメトリクスを監視することで、API応答のパフォーマンス傾向を把握し、潜在的なボトルネックを特定できます。\n*   **トークン消費量 (`input_tokens`, `output_tokens`, `total_tokens`):**\n    *   これらのメトリクスは、APIコストに直接影響します。長期的に監視することで、プロンプトの変更やモデルの選択がコストに与える影響を評価できます。\n*   **エラー率:**\n    *   Gemini API呼び出しの成功率とエラー率を監視します。エラー率の急激な上昇は、API側の問題、レート制限の超過、またはプロンプトの問題を示唆する可能性があります。\n*   **パフォーマンスチューニング:**\n    *   **プロンプトの最適化:** レイテンシやトークン消費量が問題となる場合、プロンプトの簡潔化、必要な情報の厳選、より効率的なモデルへの切り替えなどを検討します。\n    *   **非同期処理:** Activityは非同期で設計されていますが、`client.models.generate_content` 呼び出し以外の部分で同期的なブロッキングI/Oがないか確認します。\n    *   **Workerリソース:** WorkerのCPU、メモリ、ネットワークリソースが十分であることを確認します。リソースが不足していると、Activityの実行が遅延したり、エラーが発生したりする可能性があります。\n\nこれらの運用上の考慮事項は、本Activityを安定かつ効率的にSOP生成パイプラインの一部として機能させるために不可欠です。\n\n### 10. 付録\n*   **対象読者:** 開発者\n*   **所要時間の目安:** 5分\n本SOPで参照される主要なデータ構造の完全な定義と、関連する外部リソースへのリンクを含めます。\n\n#### 10.1. `core.models.SOPRequest` データ構造定義\n\n```python\n# core/models.py\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass SOPRequest(BaseModel):\n    \"\"\"\n    SOP 生成 Activity への入力データモデル。\n    LLM に与えるコンテキストと現在の処理フェーズを定義します。\n    \"\"\"\n    topic: str = Field(\n        ...,\n        description=\"SOPが対象とするトピックまたは機能の簡潔な説明。\",\n        examples=[\"Temporal SOP 生成 Activity — フェーズ別 Gemini 呼び出し実装\"],\n    )\n    phase: str = Field(\n        ...,\n        description=\"現在のSOP生成フェーズ ('outline', 'draft', 'review')。\",\n        examples=[\"outline\", \"draft\", \"review\"],\n    )\n    source_code: Optional[str] = Field(\n        None,\n        description=\"ドキュメント化対象のソースコード。outline および draft フェーズで使用。\",\n        examples=[\"def my_func():\\n    pass\"],\n    )\n    outline: Optional[str] = Field(\n        None,\n        description=\"承認済みのアウトライン（draft および review フェーズで使用）。\",\n        examples=[\"## 1. はじめに\\n目的とスコープ。\"],\n    )\n    draft: Optional[str] = Field(\n        None,\n        description=\"SOPの草稿（review フェーズで使用）。\",\n        examples=[\"# SOPタイトル\\n本文...\"],\n    )\n    previous_output: Optional[str] = Field(\n        None,\n        description=\"前回のLLM出力。リトライ時にLLMが過去の応答を参照するために使用。\",\n        examples=[\"前回のアウトラインの出力テキスト\"],\n    )\n    feedback: Optional[str] = Field(\n        None,\n        description=\"人間からのフィードバック。リトライ時にLLMが改善を行うための指示として使用。\",\n        examples=[\"章立ての項目3をより具体的にしてください。\"],\n    )\n    attempt: int = Field(\n        0,\n        description=\"現在の試行回数（初回は0）。リトライ時にインクリメントされ、プロンプトの分岐に使用。\",\n        ge=0, # 0以上の整数\n    )\n```\n\n#### 10.2. `core.models.LLMResult` データ構造定義\n\n```python\n# core/models.py\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass LLMResult(BaseModel):\n    \"\"\"\n    LLM からの応答と関連するメトリクスを格納するデータモデル。\n    \"\"\"\n    text: str = Field(\n        ...,\n        description=\"LLMからの生成テキスト（例: アウトライン、SOP草稿、レビュー結果）。\",\n        examples=[\"# SOP\\n## 1. はじめに\\n...\"],\n    )\n    model: str = Field(\n        ...,\n        description=\"応答を生成するために使用されたLLMモデルの識別子。\",\n        examples=[\"gemini-2.5-flash\"],\n    )\n    input_tokens: int = Field(\n        ...,\n        description=\"LLMへの入力に消費されたトークン数。\",\n        ge=0, # 0以上の整数\n    )\n    output_tokens: int = Field(\n        ...,\n        description=\"LLMの出力に消費されたトークン数。\",\n        ge=0, # 0以上の整数\n    )\n    total_tokens: int = Field(\n        ...,\n        description=\"合計トークン数 (入力 + 出力)。\",\n        ge=0, # 0以上の整数\n    )\n    latency_ms: float = Field(\n        ...,\n        description=\"LLM API呼び出しのレイテンシ（ミリ秒）。\",\n        ge=0.0, # 0.0以上の浮動小数点数\n    )\n    error: Optional[str] = Field(\n        None,\n        description=\"エラーが発生した場合のエラーメッセージ。成功時には None。\",\n        examples=[\"API_KEY_NOT_SET\"],\n    )\n```\n\n#### 10.3. 関連ドキュメントと外部リソース\n\n*   **Temporal Python SDK ドキュメント:**\n    *   [Temporal Python SDK](https://python.temporal.io/index.html)\n    *   [Activities in Python](https://python.temporal.io/docs/activities/)\n*   **Google Gemini API ドキュメント:**\n    *   [Gemini API Overview](https://cloud.google.com/vertex-ai/docs/generative-ai/model-reference/gemini)\n    *   [Google AI Python SDK Quickstart](https://ai.google.dev/tutorials/python_quickstart)\n*   **Pydantic ドキュメント:**\n    *   [Pydantic Documentation](https://pydantic-docs.helpmanual.io/)\n*   **Markdown ガイド:**\n    *   [Markdown Guide](https://www.markdownguide.org/)\n\n## ソースコード\n```python\n\"\"\"\nSOP 生成 Activity — フェーズ別 Gemini 呼び出し。\n\nフェーズ:\n    outline : 章立て提案（ソースコードを分析してアウトライン提案）\n    draft   : 詳細執筆（承認済みアウトラインをもとに本文執筆）\n    review  : 最終レビュー（草稿を徹底レビューして最終版を出力）\n\"\"\"\n\nimport os\nimport time\n\nfrom temporalio import activity\n\nfrom core.models import SOPRequest, LLMResult\n\n_MODEL = \"gemini-2.5-flash\"\n\n_SYSTEM = {\n    \"outline\": (\n        \"あなたは経験豊富なドキュメントアーキテクトです。\"\n        \"与えられたソースコードを分析し、標準作業手順書（SOP）の章立て（アウトライン）を提案してください。\"\n        \"各章のタイトルと1〜2行の説明を含め、明確で構造化されたアウトラインをMarkdownで出力してください。\"\n    ),\n    \"outline_retry\": (\n        \"あなたは経験豊富なドキュメントアーキテクトです。\"\n        \"前回のアウトラインに対して人間からフィードバックが届いています。\"\n        \"そのフィードバックを最優先で反映し、改善されたアウトラインをMarkdownで提供してください。\"\n    ),\n    \"draft\": (\n        \"あなたは熟練した技術ライターです。\"\n        \"承認されたアウトラインに基づき、ソースコードの詳細な標準作業手順書（SOP）本文をMarkdownで執筆してください。\"\n        \"各章を詳細に展開し、実装の意図・手順・注意点・コード例を含めてください。\"\n    ),\n    \"draft_retry\": (\n        \"あなたは熟練した技術ライターです。\"\n        \"前回のSOP草稿に対して人間からフィードバックが届いています。\"\n        \"そのフィードバックを最優先で反映し、改善された草稿をMarkdownで提供してください。\"\n    ),\n    \"review\": (\n        \"あなたは品質保証の専門家です。\"\n        \"提供されたSOP草稿を徹底的にレビューし、【改善点リスト】と【最終版SOP】をMarkdownで提供してください。\"\n        \"正確性・完全性・明確さ・実用性を重視してください。\"\n    ),\n    \"review_retry\": (\n        \"あなたは品質保証の専門家です。\"\n        \"前回のレビュー結果に対して人間からフィードバックが届いています。\"\n        \"そのフィードバックを最優先で反映し、改善された最終版をMarkdownで提供してください。\"\n    ),\n}\n\n\ndef _build_contents(request: SOPRequest) -> str:\n    phase = request.phase\n    is_retry = request.attempt > 0\n\n    if phase == \"outline\":\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## ソースコード\\n```python\\n{request.source_code}\\n```\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回のアウトライン（試行 #{request.attempt - 1}）\\n\"\n                f\"{request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善版アウトラインを提供してください。\"\n            )\n        return base + \"\\n\\n---\\n上記ソースコードの SOP アウトラインを提案してください。\"\n\n    elif phase == \"draft\":\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## 承認済みアウトライン\\n{request.outline}\\n\\n\"\n            f\"## ソースコード\\n```python\\n{request.source_code}\\n```\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回の草稿（試行 #{request.attempt - 1}）\\n\"\n                f\"{request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善版草稿を提供してください。\"\n            )\n        return base + \"\\n\\n---\\n上記アウトラインに基づき、詳細なSOP本文を執筆してください。\"\n\n    else:  # review\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## SOP草稿\\n{request.draft}\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回のレビュー結果（試行 #{request.attempt - 1}）\\n\"\n                f\"{request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善された最終版を提供してください。\"\n            )\n        return base + \"\\n\\n---\\nこのSOP草稿をレビューし、【改善点リスト】と【最終版SOP】を提供してください。\"\n\n\n@activity.defn\nasync def generate_sop_phase_activity(request: SOPRequest) -> LLMResult:\n    \"\"\"フェーズ別 SOP 生成 Activity。Gemini の system_instruction でフェーズを制御する。\"\"\"\n    from google import genai\n    from google.genai import types\n\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n\n    client = genai.Client(api_key=api_key)\n\n    phase_key = f\"{request.phase}_retry\" if request.attempt > 0 else request.phase\n    system_instruction = _SYSTEM[phase_key]\n    contents = _build_contents(request)\n\n    start = time.monotonic()\n    response = client.models.generate_content(\n        model=_MODEL,\n        contents=contents,\n        config=types.GenerateContentConfig(system_instruction=system_instruction),\n    )\n    latency_ms = (time.monotonic() - start) * 1000\n\n    usage = response.usage_metadata\n    return LLMResult(\n        text=response.text,\n        model=_MODEL,\n        input_tokens=getattr(usage, \"prompt_token_count\", 0) or 0,\n        output_tokens=getattr(usage, \"candidates_token_count\", 0) or 0,\n        total_tokens=getattr(usage, \"total_token_count\", 0) or 0,\n        latency_ms=round(latency_ms, 2),\n    )\n\n```",
      "phase": "draft",
      "phase_label": "フェーズ2: 詳細執筆",
      "tokens": 37151
    },
    {
      "approved": true,
      "attempt": 0,
      "feedback": null,
      "latency_ms": 81104.9,
      "output": "品質保証の専門家として、提供されたSOP草稿を徹底的にレビューしました。このSOP草稿は非常に詳細で網羅的であり、特にエラーハンドリングとリトライ機構に関する章が強化されており、実用性が高いと評価できます。\n\n以下に、【改善点リスト】と、それらの改善点を適用した【最終版SOP】を提供します。\n\n---\n\n## 【改善点リスト】\n\n### 1. SOP全体に関する改善\n\n*   **全体の所要時間の明記:** 「はじめに」セクションの冒頭に、SOP全体を読み通すのにかかる時間の目安を追加し、読者が全体像を把握しやすくします。\n*   **`_MODEL` 変数のSOP本文での明記:** `_MODEL = \"gemini-2.5-flash\"` の定義がソースコードにしかなく、SOP本文ではその存在が不明瞭なため、SOP本文（特に「2.3. 主要な技術要素」や「6.3. Gemini API呼び出しと応答処理」の冒頭）で明記し、その選定理由を補足します。\n\n### 2. 特定セクションに関する改善\n\n*   **セクション5.1. Activityインターフェース (`@activity.defn`)**\n    *   **`async def` の説明の修正:** `async def` の説明において、「I/Oバウンドな操作中にWorkerが他のActivityタスクを処理できるようにするため」という表現が、Pythonの非同期処理とTemporal Workerの挙動をより正確に反映するように修正します。具体的には、Workerが非同期I/O処理中に同じWorkerプロセス内で他の非同期タスクにコンテキストスイッチできる点や、Workerが複数のActivityを並行して実行できる点に言及します。\n    *   **Activity IDの説明の重複解消:** Activity IDに関する説明は、Workflowからの呼び出し側（セクション7.3. または8.2.）で詳しく行われるべき内容です。このセクションからは削除し、該当セクションへの参照を促す表現に修正します。\n\n*   **セクション6.3. Gemini API呼び出しと応答処理**\n    *   **`await` キーワードの追加:** ソースコードでは `await client.models.generate_content(...)` と非同期呼び出しになっているにも関わらず、SOP本文の説明では `response = client.models.generate_content(...)` と `await` が抜けています。この不一致を解消するため、SOP本文の該当箇所に `await` キーワードを追加し、非同期呼び出しであることを明確にします。\n    *   **`genai.Client` 初期化の補足:** `genai.Client` の初期化は同期的に行われますが、その後の `generate_content` メソッドが非同期で呼び出される点について、整合性を持たせるために補足説明を加えます。\n\n*   **セクション7.3. Temporal Activityリトライポリシーの設計と適用**\n    *   **`timedelta` のインポート追加:** 提示されているコード例で `timedelta` が使用されていますが、インポート文が欠落しているため、`from datetime import timedelta` を追加します。\n    *   **Activity IDの説明の重複解消:** 他のセクション（特に5.1と8.2）で重複して記述されているActivity IDの説明について、本セクションで最も詳細に説明されているため、他のセクションを簡略化または本セクションを参照するように修正します。\n\n*   **セクション8.2. WorkflowからのActivity呼び出し例**\n    *   **Activity IDの説明の重複解消:** 他のセクション（特に5.1と7.3）で重複して記述されているActivity IDの説明について、本セクションでも具体例を提示しつつ、重複表現を整理します。\n\n---\n\n## 【最終版SOP】\n\n## 標準作業手順書（SOP）- Temporal SOP 生成 Activity — フェーズ別 Gemini 呼び出し実装\n\n### 1. はじめに\n*   **対象読者:** 全ての関係者（ドキュメントアーキテクト、開発者、運用担当者）\n*   **所要時間の目安 (全体):** 60〜90分\n*   **所要時間の目安 (本セクション):** 5分\n\n本SOPは、Temporal Activityとして動作する「SOP生成フェーズ別Gemini呼び出し」機能の目的、構造、および運用方法について記述します。\n\n#### 1.1. ドキュメントの目的とスコープ\n本SOPは、Temporal Workflow内で動作するActivityである `generate_sop_phase_activity` の詳細を記述します。このActivityは、標準作業手順書（SOP）を自動生成するプロセスにおいて、特定のフェーズ（章立て提案、詳細執筆、最終レビュー）に応じてGoogle Geminiモデルを呼び出し、その応答を処理することを目的としています。これにより、SOP生成ワークフローの各段階でのLLM（大規模言語モデル）とのインタラクションを標準化し、信頼性と保守性を向上させます。\n\n本SOPは、`generate_sop_phase_activity` Activityの設計、実装、使用方法、および運用上の考慮事項に焦点を当てます。\n\n#### 1.2. 対象読者と前提知識\n本SOPが想定する読者層（役割）と、彼らが持つべき基本的な技術的知識レベルを説明します。\n*   **対象読者:**\n    *   **ドキュメントアーキテクト:** SOP生成プロセスの全体設計、LLMへの指示内容、および期待される出力形式に関心がある方。\n    *   **開発者:** 本Activityの実装、拡張、およびデバッグを行う方。\n    *   **運用担当者:** 本Activityを含むシステム全体のデプロイ、監視、およびトラブルシューティングを担当する方。\n*   **前提知識:**\n    *   Pythonプログラミングの基礎知識。\n    *   Temporal WorkflowおよびActivityの基本的な概念。\n    *   RESTful APIの利用経験、特にGoogle Gemini APIまたは類似のLLM APIに関する基礎知識。\n    *   Markdown形式でのドキュメント作成に関する理解。\n\n#### 1.3. 用語集\nSOP内で使用される主要な専門用語（例: Temporal Activity, Workflow, LLM, Geminiなど）の定義を提供します。\n*   **SOP (Standard Operating Procedure):** 標準作業手順書。特定の作業やプロセスを標準化するための詳細な手順書。\n*   **Temporal:** 分散システムの耐障害性とスケーラビリティを向上させるためのオープンソースのオーケストレーションプラットフォーム。\n*   **Workflow:** Temporalにおける、一連のビジネスロジックをオーケストレーションするコードの実行単位。長時間実行され、耐障害性を持つ。\n*   **Activity:** Temporal Workflowから呼び出される、具体的なタスク（例: 外部API呼び出し、データベース操作）を実行するコードの単位。Activityは冪等であるべきであり、リトライ可能。\n*   **LLM (Large Language Model):** 大規模言語モデル。人間のようなテキスト生成、理解、要約などを行うAIモデル。本SOPではGoogle Geminiモデルを指す。\n*   **Gemini API:** Googleが提供するLLM「Gemini」にプログラムからアクセスするためのAPI。\n*   **System Instructions (システム指示):** LLMに対して、その役割や振る舞いを指示するためのプロンプト。モデルの応答の質と方向性を決定する上で重要。\n*   **Prompt (プロンプト):** LLMに与える入力テキスト。\n*   **Task Queue:** Temporal WorkerがActivityタスクを受け取るためのキュー。\n*   **Retry Policy:** TemporalがActivityやWorkflowの失敗時に再試行する際のルール（最大試行回数、バックオフ間隔など）を定義するポリシー。\n\n### 2. システム概要\n*   **対象読者:** 開発者、ドキュメントアーキテクト\n*   **所要時間の目安 (本セクション):** 10分\nこのActivityは、SOP生成ワークフローにおいて、指定されたフェーズ（章立て提案、詳細執筆、最終レビュー）に応じてGeminiモデルを呼び出す役割を担います。\n\n#### 2.1. `generate_sop_phase_activity`の役割\n本ActivityがSOP生成パイプライン全体の中でどのような機能的役割を果たすかを説明します。\n`generate_sop_phase_activity` Activityは、SOP生成ワークフローの中核を担う機能であり、以下の役割を果たします。\n\n1.  **フェーズに応じたLLMの振る舞い制御:** SOP生成プロセスの各フェーズ（章立て提案、詳細執筆、最終レビュー）に応じて、Geminiモデルに異なる「役割」と指示を与えます。\n2.  **動的なプロンプト生成:** SOP生成に必要な情報（対象のソースコード、承認済みアウトライン、前回の出力、人間からのフィードバックなど）を組み合わせて、Geminiモデルに最適なプロンプトを動的に生成します。\n3.  **Gemini API呼び出し:** 構築されたプロンプトとシステム指示を用いて、Google Gemini APIを呼び出し、SOPの一部または全体を生成します。\n4.  **応答の構造化とメトリクス収集:** Geminiモデルからの応答を構造化された形式（`LLMResult`）で返し、同時にトークン消費量や処理レイテンシなどのメトリクスを収集します。\n\nこのActivityは、SOP生成ワークフローが、段階的かつ人間によるフィードバックを伴いながら、高品質なSOPを生成するための鍵となります。\n\n#### 2.2. SOP生成ワークフローにおける位置づけ\n上位のTemporal Workflowからどのように呼び出され、どのようなデータを受け渡し、結果を返すのかを概説します。\n本Activityは、SOP生成をオーケストレーションするTemporal Workflow（例: `SOPGenerationWorkflow`）から呼び出されます。ワークフローは以下のような流れで本Activityを利用することを想定しています。\n\n1.  **Outlineフェーズ:** Workflowが`generate_sop_phase_activity`を`phase=\"outline\"`で呼び出し、ソースコードを渡します。Activityはアウトラインを生成して返します。\n2.  **Human Review (Optional):** 生成されたアウトラインは人間によるレビューを受け、フィードバックが与えられる場合があります。\n3.  **Draftフェーズ:** Workflowは承認済みアウトラインとソースコード、そして必要に応じてフィードバックを伴って`generate_sop_phase_activity`を`phase=\"draft\"`で呼び出します。ActivityはSOPの草稿を生成します。\n4.  **Human Review (Optional):** 生成された草稿は人間によるレビューを受け、フィードバックが与えられる場合があります。\n5.  **Reviewフェーズ:** WorkflowはSOP草稿と必要に応じてフィードバックを伴って`generate_sop_phase_activity`を`phase=\"review\"`で呼び出します。Activityは改善点リストと最終版SOPを生成します。\n\nこのように、本ActivityはWorkflowの各段階でLLMとのインタラクションを抽象化し、Workflowのロジックを簡潔に保ちます。\n\n#### 2.3. 主要な技術要素\n本Activityを構成する主要な技術（Python, Temporal, Gemini API）と、その選定理由に軽く触れます。\n本Activityを構成する主要な技術要素は以下の通りです。\n\n*   **Python:** Activityの実装言語。その豊富なライブラリエコシステムと開発の容易さから採用されています。\n*   **Temporal Python SDK (`temporalio`):** PythonアプリケーションをTemporal Workflow/Activityとして実行するためのSDK。耐障害性とスケーラビリティを実現します。\n*   **Google Gemini API Python Client (`google-generativeai`):** Google Geminiモデルとインタラクトするための公式Pythonクライアントライブラリ。本Activityでは、高速かつコスト効率の良い `gemini-2.5-flash` モデルを使用します。\n*   **環境変数:** APIキーなどの機密情報を安全に管理するために使用されます。\n\nこれらの技術の組み合わせにより、堅牢で柔軟なLLM駆動のSOP生成システムが構築されています。\n\n### 3. 前提条件\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安 (本セクション):** 15分\n本Activityを実行するために必要な環境設定、依存ライブラリ、およびAPIキーの設定（`GEMINI_API_KEY`）について説明します。\n\n#### 3.1. 動作環境と依存ライブラリ\n必要なPythonバージョン、`temporalio`、`google-generativeai`などのライブラリとそのインストール方法を記述します。\n`generate_sop_phase_activity` Activityを実行するためには、以下の環境が整っている必要があります。\n\n*   **Pythonバージョン:** 3.9以上（Temporalio SDKの推奨および互換性を考慮）。\n*   **オペレーティングシステム:** Linux, macOS, Windowsなどの一般的なOSで動作します。\n*   **依存ライブラリ:** 以下のPythonライブラリが必要です。`pip` を使用してインストールします。\n\n    ```bash\n    pip install temporalio google-generativeai pydantic\n    ```\n    *   `temporalio`: Temporal Python SDK。\n    *   `google-generativeai`: Google Gemini APIにアクセスするためのクライアントライブラリ。\n    *   `pydantic`: データモデル (`SOPRequest`, `LLMResult`) の定義とバリデーションに使用。\n\n#### 3.2. 必須APIキーと環境変数\n`GEMINI_API_KEY`の設定方法とその重要性、およびその他の関連する環境変数について詳述します。\nGoogle Gemini APIを利用するためには、APIキーが必要です。このAPIキーは安全な方法でActivityが実行される環境に提供される必要があります。\n\n*   **`GEMINI_API_KEY`:** Google Cloud Projectで発行されたGemini APIキー。\n    *   **設定方法:** Activityが実行される環境の環境変数として設定します。\n\n        **Linux/macOS:**\n        ```bash\n        export GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n        ```\n\n        **Windows (PowerShell):**\n        ```powershell\n        $env:GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n        ```\n\n        **Docker/Kubernetes:**\n        コンテナの環境変数として設定するか、シークレット管理サービス（例: Kubernetes Secrets, AWS Secrets Manager, GCP Secret Manager）を使用して注入することを強く推奨します。\n\n    *   **重要性:** APIキーがない場合、`generate_sop_phase_activity` は `EnvironmentError` を発生させて実行を停止します。これはAPIアクセス認証の必須要件であり、セキュアな運用のためにも適切に管理される必要があります。\n\n#### 3.3. Temporal Clientのセットアップ要件\nTemporal WorkerとServiceへの接続要件、および必要な認証情報について説明します。\n本Activityが動作するTemporal Workerは、Temporal Serviceに接続できる必要があります。\n\n*   **Temporal Serviceへの接続:**\n    *   Workerアプリケーションは、Temporal Serviceのエンドポイント（通常は `localhost:7233` またはデプロイされたクラウド環境のURL）にアクセスできる必要があります。\n    *   接続には、`temporalio.client.Client` オブジェクトを初期化する際に適切な設定（`target_host`, `namespace` など）が必要です。\n*   **認証情報 (Optional):**\n    *   Temporal Serviceが認証/認可を必要とする場合（例: mTLS）、Workerは適切な証明書やAPIキーを提供する必要があります。これらの設定は通常、`temporalio.client.Client` の初期化時に行われます。\n*   **タスクキュー:**\n    *   Activityは特定のタスクキューに登録され、そのタスクキューをリッスンしているWorkerによって実行されます。WorkerはActivityが登録されているタスクキューを指定して起動される必要があります。\n\n### 4. SOP生成フェーズの概念\n*   **対象読者:** 開発者、ドキュメントアーキテクト\n*   **所要時間の目安 (本セクション):** 10分\nSOP生成プロセスにおける主要な3つのフェーズ（`outline`, `draft`, `review`）について、それぞれの役割と目的を解説します。\n\n#### 4.1. 各フェーズ（outline, draft, review）の役割と目的\n各フェーズがSOP生成のどの段階を担当し、どのような成果物を期待するかを具体的に説明します。\nSOP生成プロセスは、段階的なアプローチを採用しており、`generate_sop_phase_activity` Activityはこのアプローチをサポートするために3つの主要なフェーズを定義しています。\n\n1.  **`outline` フェーズ:**\n    *   **役割:** 与えられたソースコードを分析し、SOPの基本的な構造である章立て（アウトライン）を提案します。\n    *   **目的:** 広範なSOPのトピックに対して、構造化された枠組みを迅速に提供し、その後の詳細なコンテンツ作成の指針とします。このフェーズの出力は、人間による確認と承認の対象となり得ます。\n    *   **期待される成果物:** 各章のタイトルと簡潔な説明を含むMarkdown形式のアウトライン。\n\n2.  **`draft` フェーズ:**\n    *   **役割:** 承認されたアウトラインとソースコードに基づき、SOPの本文を詳細に執筆します。\n    *   **目的:** アウトラインで定義された各章を具体的に展開し、実装の意図、手順、注意点、コード例など、SOPとして必要な情報を盛り込んだ草稿を作成します。\n    *   **期待される成果物:** 詳細なコンテンツを含むMarkdown形式のSOP草稿。\n\n3.  **`review` フェーズ:**\n    *   **役割:** 提供されたSOP草稿を徹底的にレビューし、改善点リストと最終版SOPを提供します。\n    *   **目的:** 草稿の正確性、完全性、明確さ、実用性を評価し、品質の高い最終SOPを出力します。このフェーズでは、必要に応じて草稿の修正や加筆提案が行われます。\n    *   **期待される成果物:** 【改善点リスト】と【最終版SOP】を構造化したMarkdown形式のテキスト。\n\n#### 4.2. リトライ機構とフェーズ遷移\n`attempt`カウントを用いたリトライ時の処理（`_retry`サフィックスの利用）と、フェーズ間の論理的な遷移について説明します。\n各フェーズは、人間からのフィードバックに基づいて再試行される可能性があります。本Activityは、入力パラメータ `request.attempt` を利用してこのリトライ状況を識別します。\n\n*   **`attempt` カウント:** `request.attempt` は、現在の試行回数を表します。\n    *   `attempt = 0`: 初回の実行。\n    *   `attempt > 0`: リトライ実行。\n*   **`_retry` サフィックスの利用:** `_SYSTEM` 辞書（後述の6.1節で詳細）では、`\"outline\"`, `\"draft\"`, `\"review\"` に加えて、それぞれに対応するリトライ用のキー (`\"outline_retry\"`, `\"draft_retry\"`, `\"review_retry\"`) が定義されています。\n    *   `request.attempt > 0` の場合、`f\"{request.phase}_retry\"` というキーが使用され、LLMに「前回のアウトライン/草稿に対してフィードバックがあるので、それを反映して改善してください」という指示が与えられます。\n*   **フェーズ間の論理的な遷移:** ワークフローレベルでは、`outline` → `draft` → `review` という順序でフェーズが進行します。各フェーズ内でのリトライは、同じフェーズキー (`outline`, `draft`, `review`) を使いつつ、`attempt` カウントを増やして本Activityを再呼び出しすることで実現されます。これにより、LLMは前回の出力とフィードバックを参照して、より良い出力を生成しようと試みます。\n\n#### 4.3. LLMに与える役割の切り替え\n`_SYSTEM`辞書によるLLMの「役割」定義が、どのようにフェーズとリトライ状況に応じて切り替わるかを解説します。\n`_SYSTEM` 辞書は、各フェーズおよびリトライ状況に応じてLLMに与える「役割（System Instruction）」を定義しています。\n\n```python\n_SYSTEM = {\n    \"outline\": \"あなたは経験豊富なドキュメントアーキテクトです。...\",\n    \"outline_retry\": \"あなたは経験豊富なドキュメントアーキテクトです。前回のアウトラインに対して人間からフィードバックが届いています。...\",\n    \"draft\": \"あなたは熟練した技術ライターです。...\",\n    \"draft_retry\": \"あなたは熟練した技術ライターです。前回のSOP草稿に対して人間からフィードバックが届いています。...\",\n    \"review\": \"あなたは品質保証の専門家です。...\",\n    \"review_retry\": \"あなたは品質保証の専門家です。前回のレビュー結果に対して人間からフィードバックが届いています。...\",\n}\n```\n*   **フェーズごとの役割:**\n    *   `outline` フェーズでは「ドキュメントアーキテクト」として構造設計に集中させます。\n    *   `draft` フェーズでは「技術ライター」として詳細なコンテンツ作成に集中させます。\n    *   `review` フェーズでは「品質保証の専門家」として批判的視点での評価に集中させます。\n*   **リトライ時の役割強化:** リトライ時 (`_retry` サフィックス付きのキー) の指示には、「人間からのフィードバックを最優先で反映する」という強調が加えられ、LLMが単に再生成するのではなく、具体的な指示に従って改善を行うよう促しています。\n\nこの動的な役割の切り替えにより、各フェーズでLLMの能力を最大限に引き出し、高品質なSOP生成に貢献します。\n\n### 5. `generate_sop_phase_activity` Activityの詳細\n*   **対象読者:** 開発者\n*   **所要時間の目安 (本セクション):** 20分\nTemporal Activityとして定義された`generate_sop_phase_activity`のインターフェース、つまり入力データ型（`SOPRequest`）と出力データ型（`LLMResult`）について説明します。\n\n#### 5.1. Activityインターフェース (`@activity.defn`)\nActivityのデコレータ、関数シグネチャ、およびTemporal Activityとしての特性（例: 再試行可能性、ハートビート）について説明します。\n本Activityは `temporalio` ライブラリの `@activity.defn` デコレータを使用して定義されます。\n\n```python\nfrom temporalio import activity\nfrom core.models import SOPRequest, LLMResult\n\n@activity.defn\nasync def generate_sop_phase_activity(request: SOPRequest) -> LLMResult:\n    \"\"\"フェーズ別 SOP 生成 Activity。Gemini の system_instruction でフェーズを制御する。\"\"\"\n    # ... 実装 ...\n```\n\n*   **`@activity.defn`:** このデコレータは、指定された関数がTemporal Activityであることを示します。これにより、Temporal Workerがこの関数を発見し、Task Queueからタスクを受け取って実行できるようになります。\n*   **`async def`:** Activity関数は非同期関数として定義されています。これにより、ActivityがI/Oバウンドな操作（この場合はGemini API呼び出し）を実行している間に、Pythonの非同期ランタイムが同じWorkerプロセス内で他の非同期タスクにコンテキストスイッチできるようになります。結果として、Workerは単一スレッドでも複数のActivityタスクを並行して処理する効率を高めることができます。\n*   **関数シグネチャ:** `generate_sop_phase_activity(request: SOPRequest) -> LLMResult`\n    *   **入力:** `request` 引数は `SOPRequest` 型であり、SOP生成に必要な全ての情報を含みます。\n    *   **出力:** `LLMResult` 型のオブジェクトを返します。これはGeminiからの応答と関連するメトリクスをカプセル化したものです。\n*   **Temporal Activityとしての特性:**\n    *   **再試行可能性:** Temporal Workflowは、Activityの失敗時に組み込みのリトライメカニズムを提供します（詳細は7. エラーハンドリングとリトライ機構の設計を参照）。本Activityはこのメカニズムを活用し、APIエラーなど一時的な問題が発生した場合に自動的に再試行されます。\n    *   **ハートビート (Heartbeat):** 長時間実行されるActivityは、進捗状況をTemporal Serviceに報告するためにハートビートを送信できます。このActivity自体はGemini API呼び出しのレイテンシに依存しますが、必要に応じて `activity.heartbeat()` を組み込むことで、WorkflowがActivityの進捗を監視し、タイムアウトを防ぐことができます（本SOPのコードには明示的なハートビートは含まれていませんが、必要に応じて追加可能です）。\n\n#### 5.2. 入力データモデル `SOPRequest`\n`SOPRequest`クラスの構造（`topic`, `phase`, `source_code`, `outline`, `draft`, `previous_output`, `feedback`, `attempt`など）とその各フィールドの役割を解説します。\n`SOPRequest` クラスは、`pydantic.BaseModel` を継承しており、Activityへの入力データを構造化してバリデーションします。\n\n```python\n# core/models.py (SOPRequestの定義を想定)\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass SOPRequest(BaseModel):\n    topic: str = Field(..., description=\"SOPが対象とするトピックまたは機能の簡潔な説明。\")\n    phase: str = Field(..., description=\"現在のSOP生成フェーズ ('outline', 'draft', 'review')。\")\n    source_code: Optional[str] = Field(None, description=\"ドキュメント化対象のソースコード。\")\n    outline: Optional[str] = Field(None, description=\"承認済みのアウトライン（draft/reviewフェーズで使用）。\")\n    draft: Optional[str] = Field(None, description=\"SOPの草稿（reviewフェーズで使用）。\")\n    previous_output: Optional[str] = Field(None, description=\"前回のLLM出力（リトライ時に使用）。\")\n    feedback: Optional[str] = Field(None, description=\"人間からのフィードバック（リトライ時に使用）。\")\n    attempt: int = Field(0, description=\"現在の試行回数（初回は0）。\")\n```\n\n*   **`topic` (str):** SOPが対象とするシステムや機能の簡潔な説明。LLMにSOPの主題を伝えるために使用されます。\n*   **`phase` (str):** 現在のSOP生成フェーズ。`\"outline\"`, `\"draft\"`, `\"review\"` のいずれかです。この値に基づいてLLMのシステム指示とプロンプトが構築されます。\n*   **`source_code` (Optional[str]):** ドキュメント化の対象となるソースコードの文字列。`outline` および `draft` フェーズでLLMに提供されます。\n*   **`outline` (Optional[str]):** 以前の `outline` フェーズで生成され、承認されたアウトライン。`draft` フェーズでSOP本文の構造の指針としてLLMに提供されます。\n*   **`draft` (Optional[str]):** 以前の `draft` フェーズで生成されたSOPの草稿。`review` フェーズでLLMによるレビュー対象となります。\n*   **`previous_output` (Optional[str]):** 前回のActivity実行でLLMから生成された出力。リトライ時 (`attempt > 0`) に、LLMが前回の結果を参照し、フィードバックを適用する際に使用されます。\n*   **`feedback` (Optional[str]):** 人間（または自動化された評価システム）からのフィードバックテキスト。リトライ時 (`attempt > 0`) に、LLMが改善を行うための具体的な指示として使用されます。\n*   **`attempt` (int):** 現在のActivityの試行回数。初回呼び出しは `0` で、リトライするごとにインクリメントされます。この値は、`_SYSTEM` 辞書から適切なシステム指示を選択するために使用されます。\n\nこれらのフィールドは、LLMに与える情報の粒度と関連性を制御し、各フェーズでのLLMの応答を最適化するために設計されています。\n\n#### 5.3. 出力データモデル `LLMResult`\n`LLMResult`クラスの構造（`text`, `model`, `input_tokens`, `output_tokens`, `total_tokens`, `latency_ms`など）と、LLMからの応答情報をどのように保持するかを説明します。\n`LLMResult` クラスは、Gemini APIからの応答と、その呼び出しに関するメトリクスを構造化して格納します。\n\n```python\n# core/models.py (LLMResultの定義を想定)\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass LLMResult(BaseModel):\n    text: str = Field(..., description=\"LLMからの生成テキスト。\")\n    model: str = Field(..., description=\"使用されたLLMモデルの識別子。\")\n    input_tokens: int = Field(..., description=\"LLMへの入力に消費されたトークン数。\")\n    output_tokens: int = Field(..., description=\"LLMの出力に消費されたトークン数。\")\n    total_tokens: int = Field(..., description=\"合計トークン数 (入力 + 出力)。\")\n    latency_ms: float = Field(..., description=\"LLM API呼び出しのレイテンシ（ミリ秒）。\")\n    error: Optional[str] = Field(None, description=\"エラーが発生した場合のエラーメッセージ。\")\n```\n\n*   **`text` (str):** Geminiモデルが生成した主なテキスト応答。これはアウトライン、SOP草稿、またはレビュー結果となります。\n*   **`model` (str):** 応答を生成するために使用されたGeminiモデルのID（例: `\"gemini-2.5-flash\"`）。\n*   **`input_tokens` (int):** プロンプト（入力）に費やされたトークン数。Gemini APIの `usage_metadata.prompt_token_count` から取得されます。\n*   **`output_tokens` (int):** 生成された応答（出力）に費やされたトークン数。Gemini APIの `usage_metadata.candidates_token_count` から取得されます。\n*   **`total_tokens` (int):** `input_tokens` と `output_tokens` の合計。APIコストの計算に直接関係します。Gemini APIの `usage_metadata.total_token_count` から取得されます。\n*   **`latency_ms` (float):** Gemini APIへのリクエスト送信から応答受信までの時間（ミリ秒単位）。パフォーマンス監視に有用です。\n*   **`error` (Optional[str]):** エラーが発生した場合に、エラーメッセージを格納します。成功時には `None` となります。\n\nこの構造化された出力により、WorkflowはLLMの応答を容易にパースして後続の処理に利用できるだけでなく、コストやパフォーマンスに関する重要なメトリクスも収集できます。\n\n### 6. コア機能の実装\n*   **対象読者:** 開発者\n*   **所要時間の目安 (本セクション):** 30分\n本Activityの中核をなす、システム指示の管理、LLMコンテンツの動的構築、およびGemini API呼び出しの実装について深掘りします。\n\n#### 6.1. システム指示（System Instructions）の管理 (`_SYSTEM`辞書)\n`_SYSTEM` 辞書は、各SOP生成フェーズおよびリトライの有無に応じて、Geminiモデルに与える「システム指示」を定義します。\n\n```python\n_SYSTEM = {\n    \"outline\": (\n        \"あなたは経験豊富なドキュメントアーキテクトです。\"\n        \"与えられたソースコードを分析し、標準作業手順書（SOP）の章立て（アウトライン）を提案してください。\"\n        \"各章のタイトルと1〜2行の説明を含め、明確で構造化されたアウトラインをMarkdownで出力してください。\"\n    ),\n    \"outline_retry\": (\n        \"あなたは経験豊富なドキュメントアーキテクトです。\"\n        \"前回のアウトラインに対して人間からフィードバックが届いています。\"\n        \"そのフィードバックを最優先で反映し、改善されたアウトラインをMarkdownで提供してください。\"\n    ),\n    # ... 他のフェーズの定義 ...\n}\n```\n\n*   **設計思想:**\n    *   **役割ベースのプロンプティング:** LLMに特定の専門家（ドキュメントアーキテクト、技術ライター、品質保証専門家）の役割を与えることで、その専門性に応じた思考プロセスと出力形式を促します。\n    *   **具体的な指示:** 各フェーズでLLMに何を期待するか（例: 「章立てを提案」「詳細を執筆」「レビューし改善点と最終版を提供」）を明確に記述します。\n    *   **出力形式の指定:** 「Markdownで出力してください」と明示することで、構造化された応答を期待します。\n    *   **リトライ時の指示強化:** リトライ (`_retry` サフィックス) 時には「フィードバックを最優先で反映する」という文言を加え、改善に対するLLMの集中を促します。\n*   **`system_instruction` の選択ロジック:**\n    `generate_sop_phase_activity` 関数内で、`request.phase` と `request.attempt` に基づいて適切な `system_instruction` が選択されます。\n\n    ```python\n    phase_key = f\"{request.phase}_retry\" if request.attempt > 0 else request.phase\n    system_instruction = _SYSTEM[phase_key]\n    ```\n    これにより、初回実行時は `request.phase` に対応するシステム指示が、リトライ時は `request.phase` に `_retry` を付加したシステム指示が選択されます。\n\nこの管理されたシステム指示により、LLMは各フェーズの目的に合致した、高品質で一貫性のある応答を生成することが可能になります。\n\n#### 6.2. LLMプロンプトコンテンツの動的生成 (`_build_contents`関数)\n`_build_contents` 関数は、`SOPRequest` オブジェクトからGeminiモデルに渡すための主要なプロンプトコンテンツを動的に構築します。\n\n```python\ndef _build_contents(request: SOPRequest) -> str:\n    phase = request.phase\n    is_retry = request.attempt > 0\n\n    if phase == \"outline\":\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## ソースコード\\n```python\\n{request.source_code}\\n```\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回のアウトライン（試行 #{request.attempt - 1}）\\n\"\n                f\"{request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善版アウトラインを提供してください。\"\n            )\n        return base + \"\\n\\n---\\n上記ソースコードの SOP アウトラインを提案してください。\"\n\n    elif phase == \"draft\":\n        # ... draftフェーズのロジック ...\n\n    else:  # review\n        # ... reviewフェーズのロジック ...\n```\n\n*   **関数の役割:** `_build_contents` は、`SOPRequest` の各フィールド（`topic`, `source_code`, `outline`, `draft`, `previous_output`, `feedback`）を組み合わせ、現在のフェーズとリトライ状況に応じた最適なプロンプトテキストを生成します。\n*   **フェーズごとのプロンプト構造:**\n    *   **`outline` フェーズ:**\n        *   初回 (`is_retry` が `False`): ドキュメント化対象のトピックとソースコードをLLMに提示し、「SOPアウトラインを提案してください」と指示します。\n        *   リトライ (`is_retry` が `True`): 上記に加え、前回のLLM出力 (`request.previous_output`) と人間からのフィードバック (`request.feedback`) を提示し、「フィードバックを反映した改善版アウトラインを提供してください」と明確に指示します。\n    *   **`draft` フェーズ:**\n        *   初回: ドキュメント化対象、承認済みアウトライン (`request.outline`)、およびソースコードを提示し、「詳細なSOP本文を執筆してください」と指示します。\n        *   リトライ: 上記に加え、前回の草稿とフィードバックを提示し、「フィードバックを反映した改善版草稿を提供してください」と指示します。\n    *   **`review` フェーズ:**\n        *   初回: ドキュメント化対象とSOP草稿 (`request.draft`) を提示し、「このSOP草稿をレビューし、【改善点リスト】と【最終版SOP】を提供してください」と指示します。\n        *   リトライ: 上記に加え、前回のレビュー結果とフィードバックを提示し、「フィードバックを反映した改善された最終版を提供してください」と指示します。\n*   **コード例（`draft` フェーズ）:**\n\n    ```python\n    elif phase == \"draft\":\n        base = (\n            f\"## ドキュメント化対象\\n{request.topic}\\n\\n\"\n            f\"## 承認済みアウトライン\\n{request.outline}\\n\\n\"\n            f\"## ソースコード\\n```python\\n{request.source_code}\\n```\"\n        )\n        if is_retry:\n            return (\n                f\"{base}\\n\\n\"\n                f\"## 前回の草稿（試行 #{request.attempt - 1}）\\n\"\n                f\"## {request.previous_output}\\n\\n\"\n                f\"## 人間からのフィードバック\\n{request.feedback}\\n\\n\"\n                \"---\\nフィードバックを反映した改善版草稿を提供してください。\"\n            )\n        return base + \"\\n\\n---\\n上記アウトラインに基づき、詳細なSOP本文を執筆してください。\"\n    ```\n    この例から分かるように、各フェーズで必要な入力情報が異なるため、`_build_contents` 関数は非常に柔軟にプロンプトを構築します。Markdownのヘッダー (`##`) を使用して情報を構造化し、LLMが各セクションの内容を正確に理解できるように工夫されています。\n\n#### 6.3. Gemini API呼び出しと応答処理\n`google.genai`クライアントの初期化、モデル指定（`_MODEL`）、`system_instruction`の適用、`client.models.generate_content`の呼び出し、および応答から`LLMResult`を構築するプロセスを説明します。\n`generate_sop_phase_activity` 関数内で、`google.generativeai` クライアントを使用してGemini APIを呼び出し、その応答を処理します。\n\n```python\nasync def generate_sop_phase_activity(request: SOPRequest) -> LLMResult:\n    from google import genai\n    from google.genai import types\n\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n\n    # genai.Client の初期化自体は同期的に行われますが、\n    # その後の generate_content メソッドは非同期です。\n    client = genai.Client(api_key=api_key)\n\n    phase_key = f\"{request.phase}_retry\" if request.attempt > 0 else request.phase\n    system_instruction = _SYSTEM[phase_key]\n    contents = _build_contents(request)\n\n    start = time.monotonic()\n    # 非同期で generate_content を呼び出すため、await を使用します\n    response = await client.models.generate_content(\n        model=_MODEL,\n        contents=contents,\n        config=types.GenerateContentConfig(system_instruction=system_instruction),\n    )\n    latency_ms = (time.monotonic() - start) * 1000\n\n    usage = response.usage_metadata\n    return LLMResult(\n        text=response.text,\n        model=_MODEL,\n        input_tokens=getattr(usage, \"prompt_token_count\", 0) or 0,\n        output_tokens=getattr(usage, \"candidates_token_count\", 0) or 0,\n        total_tokens=getattr(usage, \"total_token_count\", 0) or 0,\n        latency_ms=round(latency_ms, 2),\n    )\n```\n\n*   **`google.genai` クライアントの初期化:**\n    ```python\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n    client = genai.Client(api_key=api_key)\n    ```\n    環境変数 `GEMINI_API_KEY` からAPIキーを取得し、`genai.Client` を初期化します。キーが設定されていない場合は `EnvironmentError` を発生させます。`genai.Client` 自体の初期化は同期的に行われますが、その後の `client.models.generate_content` メソッドは非同期で実行されます。\n*   **モデル指定 (`_MODEL`):**\n    ```python\n    _MODEL = \"gemini-2.5-flash\"\n    # ...\n    response = await client.models.generate_content(\n        model=_MODEL,\n        # ...\n    )\n    ```\n    事前に定義された `_MODEL` 変数 (`\"gemini-2.5-flash\"`) を使用して、呼び出すGeminiモデルを指定します。このモデルは高速かつコスト効率の良いテキスト生成に適しています。\n*   **`system_instruction` の適用:**\n    ```python\n    # ...\n    config=types.GenerateContentConfig(system_instruction=system_instruction),\n    ```\n    LLMに役割と振る舞いを指示する `system_instruction` は、`GenerateContentConfig` オブジェクトを通じてAPIリクエストに含められます。これはLLMの応答の質を大きく左右する重要な要素です。\n*   **`client.models.generate_content` の呼び出し:**\n    ```python\n    response = await client.models.generate_content( # await を使用\n        model=_MODEL,\n        contents=contents,\n        config=types.GenerateContentConfig(system_instruction=system_instruction),\n    )\n    ```\n    `client.models.generate_content` メソッドを非同期で呼び出します。\n    *   `model`: 使用するGeminiモデル。\n    *   `contents`: `_build_contents` 関数で生成された主要なプロンプトテキスト。\n    *   `config`: `system_instruction` を含む追加設定。\n*   **応答処理と `LLMResult` の構築:**\n    ```python\n    latency_ms = (time.monotonic() - start) * 1000\n    usage = response.usage_metadata\n    return LLMResult(\n        text=response.text,\n        model=_MODEL,\n        input_tokens=getattr(usage, \"prompt_token_count\", 0) or 0,\n        output_tokens=getattr(usage, \"candidates_token_count\", 0) or 0,\n        total_tokens=getattr(usage, \"total_token_count\", 0) or 0,\n        latency_ms=round(latency_ms, 2),\n    )\n    ```\n    API応答から以下の情報を抽出します。\n    *   `response.text`: LLMが生成したテキスト。\n    *   `response.usage_metadata`: トークン数に関する情報（`prompt_token_count`, `candidates_token_count`, `total_token_count`）。`getattr()` と `or 0` を使用して、これらの属性が存在しない場合でも安全にデフォルト値（0）を取得します。\n    *   `time.monotonic()` を使用してAPI呼び出しの開始と終了時刻を記録し、レイテンシを計算します。\n    これらの情報を用いて `LLMResult` オブジェクトを構築し、Activityの戻り値として返します。\n\nこのセクションで説明されたコア機能の実装は、本ActivityがSOP生成プロセスの各フェーズでLLMを効果的に利用するための基盤を形成します。\n\n### 7. エラーハンドリングとリトライ機構の設計\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安 (本セクション):** 15分\n本Activityは、外部サービスであるGemini APIに依存するため、エラーハンドリングとリトライ機構は堅牢な運用に不可欠です。本章では、エラー処理の戦略と、Temporalの組み込みリトライメカニズムをどのように活用し、設計するかについて解説します。\n\n#### 7.1. 環境設定エラーの処理 (`GEMINI_API_KEY`未設定)\nGemini APIを呼び出すには、有効なAPIキーが環境変数 `GEMINI_API_KEY` に設定されている必要があります。\n\n```python\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise EnvironmentError(\"GEMINI_API_KEY が設定されていません。\")\n```\n\n*   **エラー発生条件:** `os.environ.get(\"GEMINI_API_KEY\")` が `None` または空文字列を返す場合、この条件が真となり `EnvironmentError` が発生します。\n*   **設計上の考慮事項:**\n    *   この種のエラーは、環境の初期設定ミスに起因し、Activityを再試行しても解決することはありません。そのため、このようなエラーが発生した場合は、速やかにWorkflowを失敗させるべきです。\n    *   Temporalの `RetryPolicy` において、`EnvironmentError` を `non_retryable_error_types` に含めることで、無駄なリソース消費を防ぎ、運用担当者に迅速な介入を促すことができます。\n*   **対処法:**\n    *   **デプロイ前:** Activityを含むWorkerをデプロイする前に、必ず `GEMINI_API_KEY` 環境変数が適切に設定されていることを確認してください。\n    *   **運用時:** CI/CDパイプラインやデプロイメントスクリプトで、この環境変数が安全かつ確実に設定されるように自動化を検討してください。Kubernetes Secretsなどのシークレット管理サービスを利用することで、APIキーの漏洩リスクを低減できます。\n\n#### 7.2. Gemini API呼び出し時の潜在的エラーとその対策\nGemini API呼び出し時には、様々なエラーが発生する可能性があります。これらのエラーはActivityの未処理例外としてWorkflowに伝播し、Temporalのリトライポリシーによって処理されます。\n\n1.  **ネットワークエラー:**\n    *   **原因:** APIエンドポイントへの接続問題、DNS解決失敗、タイムアウトなど、一時的なインフラストストラクチャの問題。\n    *   **設計と対策:** これらは一時的な問題であるため、Temporalの組み込みリトライポリシーが非常に有効です。`initial_interval` と `backoff_coefficient` を適切に設定することで、自動的に再試行され、多くの場合は回復します。Activity自体に明示的な `try-except` を設けるよりも、Temporalの堅牢なメカニズムに任せるのが一般的です。\n2.  **APIレート制限 (Rate Limit):**\n    *   **原因:** 短期間にAPIリクエストが集中し、Googleの許容リクエストレートを超過した場合。\n    *   **設計と対策:** `google-generativeai` ライブラリは通常、レート制限エラーに対して特定の例外（例: `google.api_core.exceptions.ResourceExhausted`）を発生させます。Temporalの指数バックオフを含むリトライポリシーは、このような場合にリクエスト間隔を徐々に長くしてレート制限を回避するのに役立ちます。Workerプロセス自体にクライアントサイドのレートリミッターを実装し、APIリクエストを制御することも検討できます。\n3.  **LLMによる応答生成失敗:**\n    *   **原因:** プロンプトが複雑すぎる、LLMが安全ポリシーに違反するコンテンツを検知した、内部エラーなどにより、LLMが有効な応答を生成できなかった場合。\n    *   **設計と対策:**\n        *   **リトライ:** 一時的なLLM内部の問題であれば、リトライによって解決する可能性があります。\n        *   **プロンプトの最適化:** 継続的にこの問題が発生する場合、プロンプトの品質そのものが原因である可能性があります。プロンプトをより明確にし、曖昧さを排除し、LLMにとって理解しやすい形式にすることで、生成失敗のリスクを減らせます。\n        *   **フォールバック/人間による介入:** 特定の回数リトライしても成功しない場合、Activityは最終的に失敗します。Workflowは、この失敗を検知して人間による介入を要求するか、別の処理フロー（例: デフォルトコンテンツの生成）に切り替えることを検討すべきです。\n\n#### 7.3. Temporal Activityリトライポリシーの設計と適用\nTemporalの組み込みリトライメカニズムは、本Activityの堅牢性を大幅に向上させます。WorkflowからActivityを呼び出す際に、`start_to_close_timeout` と `retry_policy` を適切に設定することが、堅牢なエラーハンドリング設計の中核となります。\n\n**WorkflowからのActivity呼び出しと`RetryPolicy`の設定例:**\n\n```python\nfrom temporalio.client import Client, WorkflowHandle\nfrom temporalio.worker import Worker\nfrom temporalio.common import RetryPolicy\nfrom datetime import timedelta # timedelta をインポート\n\n# ... Activity と Workflow の定義 (省略) ...\n\nasync def start_sop_generation_workflow(client: Client, topic: str, source_code: str):\n    # Workflow実行の開始 (ここでは Workflow 自体の定義は省略)\n    # Workflow内部から generate_sop_phase_activity を呼び出す例を示す\n\n    # generate_sop_phase_activity のためのSOPRequestオブジェクト\n    outline_request = SOPRequest(\n        topic=topic,\n        phase=\"outline\",\n        source_code=source_code,\n        attempt=0, # 初回試行\n    )\n\n    # Activity の実行\n    try:\n        outline_result: LLMResult = await client.execute_activity(\n            generate_sop_phase_activity,\n            outline_request,\n            task_queue=\"sop-generation-task-queue\",\n            # Activityの実行時間制限とリトライポリシーの設計\n            start_to_close_timeout=timedelta(minutes=5), # Activityが完了するまでの最大時間\n            retry_policy=RetryPolicy(\n                initial_interval=timedelta(seconds=1),  # 最初の再試行までの待機時間\n                backoff_coefficient=2.0,                # 各再試行で待機時間を乗算する係数 (指数バックオフ)\n                maximum_interval=timedelta(minutes=1),  # 再試行間の最大待機時間\n                maximum_attempts=5,                     # 最大再試行回数。これを超えるとActivityは最終的に失敗\n                non_retryable_error_types=[\"EnvironmentError\", \"ValueError\"], # 特定のエラーはリトライしない\n            ),\n            # Activity ID は Workflow の決定論的実行を保証するため重要です。\n            # ユニークかつ再試行で同じ ID になるように設計することが推奨されます。\n            # 例: f\"generate-outline-{workflow.uuid4()}\" (Workflow内部から呼び出す場合)\n            id=\"generate-outline-for-my-sop-topic-v1\",\n        )\n        print(f\"SOP Outline Generated: {outline_result.text[:100]}...\")\n        return outline_result\n    except Exception as e:\n        print(f\"Activity failed after retries: {e}\")\n        raise # Workflow にエラーを再スロー\n\n# if __name__ == \"__main__\":\n#     # 通常は Worker が起動し、Workflow から Activity が呼び出されます。\n#     # このコードは Workflow 内部からの呼び出し方を具体的に示すためのものです。\n#     # 実際に実行する際は、Temporal Service と Worker が動作している必要があります。\n#     # client = await Client.connect(\"localhost:7233\")\n#     # await start_sop_generation_workflow(client, \"My Temporal Workflow\", \"...\")\n```\n\n*   **`start_to_close_timeout` の設計:**\n    *   Activityが開始されてから完了するまでの最大許容時間です。この時間内に完了しない場合、Activityはタイムアウトと見なされ、リトライポリシーが適用されます。\n    *   Gemini APIの応答時間、プロンプトの複雑さ、およびモデルの種類によって変動するため、適切な値を設定することが重要です。例えば、短い応答が期待される場合は数分、複雑な生成にはそれ以上の時間を設定します。\n*   **`RetryPolicy` の設計要素:**\n    *   **`initial_interval` (timedelta):** 最初の再試行までの待機時間。APIの一時的な障害やレート制限からの回復を考慮し、短すぎず長すぎない時間を設定します。\n    *   **`backoff_coefficient` (float):** 各再試行で待機時間を乗算する係数。`1.0` ならば固定間隔、`2.0` ならば指数バックオフとなります。外部APIのレート制限回避には指数バックオフ（`> 1.0`）が効果的です。\n    *   **`maximum_interval` (timedelta):** 再試行間の最大待機時間。指数バックオフが際限なく広がるのを防ぎ、リソースを過剰に占有しないように上限を設定します。\n    *   **`maximum_attempts` (int):** 最大再試行回数。これを超えるとActivityは最終的に失敗としてWorkflowに報告されます。無限ループを防ぎ、最終的な失敗をWorkflowに伝えるための重要なガードレールです。この回数は、一時的なエラーの頻度やビジネス要件に応じて決定します。\n    *   **`non_retryable_error_types` (List[str]):** 指定されたエラータイプが発生した場合は、リトライせず直ちにActivityを失敗とします。\n        *   `EnvironmentError` (例: `GEMINI_API_KEY` 未設定): 環境設定の根本的な問題であり、リトライしても解決しないため、ここに含めるのが適切です（前述の7.1節を参照）。\n        *   `ValueError` や `TypeError`: Activityへの入力データに問題がある場合など、ビジネスロジックやデータ構造の根本的な不整合を示すエラーも、リトライしても無意味なケースが多いため、ここに追加を検討します。これにより、無駄なリソース消費と時間の浪費を避け、迅速なデバッグと修正を促します。\n*   **Activity ID:** Activity IDは、WorkflowがActivityの実行を追跡し、リプレイ時に同じActivityが決定論的に実行されることを保証するために重要です。Workflow内部からActivityを呼び出す際には、`id` パラメータをユニークかつ再現可能な方法で設定することが推奨されます（例: Workflow IDとActivityフェーズ名を組み合わせるなど）。\n\nTemporalのリトライ機構により、Activity自体はビジネスロジックに集中でき、一時的な外部サービスの問題に対する回復性はプラットフォームによって提供されます。`RetryPolicy` を適切に設計することは、システムの安定性と運用効率を大きく左右します。\n\n### 8. 実行方法とデプロイ\n*   **対象読者:** 開発者、運用担当者\n*   **所要時間の目安 (本セクション):** 20分\n本ActivityをTemporal環境で実行するための具体的な手順と、デプロイに関する考慮事項を説明します。\n\n#### 8.1. Temporal WorkerへのActivity登録\n`Worker`インスタンスの作成、タスクキューの指定、および`generate_sop_phase_activity`をWorkerに登録するPythonコード例を示します。\nActivityは、`temporalio.worker.Worker` インスタンスに登録され、特定のタスクキューをリッスンするWorkerによって実行されます。\n\n```python\nimport asyncio\nfrom temporalio.worker import Worker\nfrom temporalio.client import Client\n\nfrom my_activities import generate_sop_phase_activity # Activityのインポート\nfrom my_workflows import SOPGenerationWorkflow       # Workflowのインポート (参考)\n\nasync def run_worker():\n    client = await Client.connect(\"localhost:7233\") # Temporal Serviceに接続\n\n    worker = Worker(\n        client,\n        task_queue=\"sop-generation-task-queue\", # Activityがリッスンするタスクキュー名\n        activities=[generate_sop_phase_activity], # 登録するActivityのリスト\n        workflows=[SOPGenerationWorkflow],       # 関連するWorkflowも登録 (通常は同じWorkerで処理)\n    )\n    print(\"Starting Worker...\")\n    await worker.run() # Workerを起動し、タスクキューからのタスクを待機\n\nif __name__ == \"__main__\":\n    # GEMINI_API_KEY 環境変数を設定してから実行してください\n    # export GEMINI_API_KEY=\"YOUR_GEMINI_API_KEY_HERE\"\n    asyncio.run(run_worker())\n```\n\n*   **`Client.connect()`:** Temporal Serviceへの接続を確立します。デフォルトでは `localhost:7233` に接続します。\n*   **`Worker` インスタンスの作成:**\n    *   `client`: 上記で作成したTemporal Client。\n    *   `task_queue`: このWorkerが処理するタスクキューの名前。ActivityとWorkflowは同じタスクキューに登録されます。\n    *   `activities`: このWorkerが実行できるActivity関数のリスト。`generate_sop_phase_activity` をここに追加します。\n    *   `workflows`: このWorkerが実行できるWorkflow関数のリスト（オプションですが、通常は関連するWorkflowも登録します）。\n*   **`worker.run()`:** Workerを起動し、指定されたタスクキューからActivityタスクやWorkflowタスクをポーリングして実行します。\n\n#### 8.2. WorkflowからのActivity呼び出し例\nTemporal Workflowから`generate_sop_phase_activity`を呼び出す際の非同期実行（`await workflow.execute_activity`）や、入力パラメータの渡し方をコード例で示します。\nWorkflowから `generate_sop_phase_activity` を呼び出す際は、`temporalio.workflow.execute_activity` を使用します。\n\n```python\nfrom temporalio import workflow\nfrom datetime import timedelta\n\nfrom my_activities import generate_sop_phase_activity\nfrom core.models import SOPRequest, LLMResult # データモデルのインポート\n\n@workflow.defn\nclass SOPGenerationWorkflow:\n    @workflow.run\n    async def run(self, topic: str, source_code: str) -> str:\n        # Outline フェーズ\n        outline_request = SOPRequest(\n            topic=topic,\n            phase=\"outline\",\n            source_code=source_code,\n            attempt=0, # 初回試行\n        )\n        outline_result: LLMResult = await workflow.execute_activity(\n            generate_sop_phase_activity,\n            outline_request,\n            start_to_close_timeout=timedelta(minutes=5),\n            retry_policy=workflow.RetryPolicy(maximum_attempts=3),\n            # Activity ID は Workflow の決定論的実行を保証するため重要です。\n            # ユニークかつ再試行で同じ ID になるように設計することが推奨されます。\n            # 例: `f\"generate-outline-{workflow.uuid4()}\"`\n            id=f\"generate-outline-{workflow.uuid4()}\",\n        )\n        generated_outline = outline_result.text\n\n        workflow.logger.info(f\"Generated Outline:\\n{generated_outline}\")\n\n        # ここで人間によるレビューや承認を待つシグナルを待機するロジックを挟むことも可能\n        # 例: await workflow.wait_for_external_signal(\"human_approval_signal\")\n\n        # Draft フェーズ\n        draft_request = SOPRequest(\n            topic=topic,\n            phase=\"draft\",\n            source_code=source_code,\n            outline=generated_outline, # 承認済みアウトラインを渡す\n            attempt=0,\n        )\n        draft_result: LLMResult = await workflow.execute_activity(\n            generate_sop_phase_activity,\n            draft_request,\n            start_to_close_timeout=timedelta(minutes=15),\n            retry_policy=workflow.RetryPolicy(maximum_attempts=3),\n            id=f\"generate-draft-{workflow.uuid4()}\",\n        )\n        generated_draft = draft_result.text\n\n        workflow.logger.info(f\"Generated Draft:\\n{generated_draft}\")\n\n        # ... review フェーズの呼び出しも同様 ...\n\n        return generated_draft # 例として最終ドラフトを返す\n```\n\n*   **`await workflow.execute_activity()`:** WorkflowからActivityを非同期で呼び出します。\n    *   第一引数: 呼び出すActivity関数 (`generate_sop_phase_activity`)。\n    *   第二引数: Activityへの入力データ（`SOPRequest` オブジェクト）。\n    *   `start_to_close_timeout`: Activityの実行タイムアウト。LLMの応答時間に応じて適切な値を設定します。\n    *   `retry_policy`: Activity固有のリトライポリシー。Workflowレベルでデフォルト設定も可能ですが、Activityごとに調整することも重要です。\n    *   `id`: ActivityのユニークなID。Workflowが決定論的であるために重要です。Workflowリプレイ時に同じActivityが同じIDで実行されることを保証します（詳細は7.3節も参照）。`workflow.uuid4()` などを使用してユニークなIDを生成し、フェーズ名などと組み合わせるのが一般的です。\n\n#### 8.3. デプロイメントとスケーリング\nコンテナ化（Docker）、Kubernetesなどの環境でのデプロイ方法と、SOP生成の負荷に応じたWorkerのスケーリング戦略について言及します。\nSOP生成システムを本番環境にデプロイする際の考慮事項：\n\n*   **コンテナ化 (Docker):** WorkerアプリケーションをDockerコンテナとしてパッケージ化することを強く推奨します。これにより、環境依存性を排除し、どこでも一貫した動作を保証できます。`Dockerfile` にはPython環境と必要なライブラリのインストール手順、およびWorkerの起動コマンドを含めます。\n*   **コンテナオーケストレーション (Kubernetes):** DockerコンテナをKubernetesなどのコンテナオーケストレーションプラットフォームにデプロイすることで、Workerアプリケーションのデプロイ、管理、スケーリングが容易になります。\n    *   **Pod定義:** Workerコンテナ、環境変数（特に `GEMINI_API_KEY` をKubernetes Secretsとして）、リソース要求/制限を含むPod定義を作成します。\n    *   **Deployment:** 複数のWorker Podを管理し、ローリングアップデートなどを可能にするDeploymentリソースを使用します。\n    *   **Service:** 必要に応じて、Workerが外部からアクセスする必要がある場合にServiceを定義します（ただし、通常Temporal WorkerはPullモデルで動作するため不要なことが多い）。\n*   **スケーリング:**\n    *   SOP生成の負荷（Workflow実行数）に応じて、Workerのレプリカ数を増減させることでスケーリングします。\n    *   Kubernetesの場合、Horizontal Pod Autoscaler (HPA) を使用して、CPU使用率やカスタムメトリクス（例: タスクキューのバックログサイズ）に基づいてWorker Podを自動的にスケーリングできます。\n    *   Gemini APIのレート制限に注意し、WorkerのスケールアップがAPIリクエストレートの許容範囲内であることを確認してください。\n\n### 9. 運用上の考慮事項\n*   **対象読者:** 運用担当者、ドキュメントアーキテクト\n*   **所要時間の目安 (本セクション):** 15分\n本Activityを本番環境で運用する上での重要な側面、例えばセキュリティ、コスト、パフォーマンスなどについて記述します。\n\n#### 9.1. APIキーのセキュアな管理\n`GEMINI_API_KEY` は、Google Gemini APIへのフルアクセス権を持つ機密情報です。以下の方法で安全に管理する必要があります。\n\n*   **環境変数:** 開発環境や小規模なデプロイでは、環境変数として設定することが一般的です。ただし、OSのプロセスリストやログファイルから漏洩するリスクがないことを確認してください。\n*   **シークレット管理サービス:** 本番環境では、以下のいずれかの専用シークレット管理サービスを使用することを強く推奨します。\n    *   **AWS Secrets Manager:** AWS環境で利用。APIキーを暗号化して保存し、IAMロールに基づいてアクセスを許可します。\n    *   **GCP Secret Manager:** Google Cloud Platform環境で利用。同様にキーの暗号化とIAMによるアクセス制御を提供します。\n    *   **Kubernetes Secrets:** Kubernetesクラスター内でシークレットを管理します。Base64エンコードされるだけで暗号化はされないため、ディスクレベルの暗号化やRBACポリシーと組み合わせて利用します。\n*   **原則:**\n    *   **コードにハードコードしない:** 絶対にソースコード内にAPIキーを直接書き込まないでください。\n    *   **最小権限の原則:** APIキーには、そのActivityが必要とする最小限の権限のみを付与してください。\n    *   **ローテーション:** 定期的にAPIキーをローテーションし、万が一漏洩した場合の影響を最小限に抑えてください。\n\n#### 9.2. コストとレートリミットの最適化\nGemini APIの利用には料金が発生し、またAPI呼び出しにはレートリミットが設定されています。\n\n*   **コストの最適化:**\n    *   **モデル選択:** `_MODEL = \"gemini-2.5-flash\"` を使用しているのは、`gemini-1.5-pro` などのより高性能なモデルに比べてコスト効率が良いためです。SOP生成の要件（品質、速度）に応じて、適切なモデルを選択することが重要です。\n    *   **プロンプトの最適化:**\n        *   **簡潔性:** 不要な冗長な情報をプロンプトから削除し、トークン数を削減します。\n        *   **入力のフィルタリング:** 大量のソースコードをそのまま渡すのではなく、関連性の高い部分のみを抽出して渡すことを検討します。\n        *   **アウトラインの短縮:** `outline` や `draft` の情報量を必要最低限に抑えることで、入力トークンを削減します。\n    *   **キャッシング:** 同じ入力に対して同じSOPコンテンツを生成する場合、LLMの応答をキャッシュして再利用することでAPI呼び出しを減らし、コストを削減できます。\n*   **レートリミットへの対応:**\n    *   **リトライとバックオフ:** Temporalの `RetryPolicy` による指数バックオフは、レートリミットエラーに対する最初の防衛線です。\n    *   **Workerのスケーリング計画:** 大量のSOPを生成する場合、Workerの数を増やして並列処理を行うことになりますが、その結果としてAPIリクエストレートも増加します。Google CloudのモニタリングツールでAPI使用状況を監視し、必要に応じてレートリミットの引き上げをリクエストするか、クライアントサイドでのレート制御を実装してください。\n\n#### 9.3. ログ監視とパフォーマンスチューニング\nシステムの健全性と効率性を維持するためには、継続的な監視とパフォーマンスチューニングが不可欠です。\n\n*   **Activityの実行ログ:**\n    *   Temporal Workerのログは、Activityの開始、完了、および発生したエラーに関する情報を提供します。構造化ログ（JSON形式など）を採用することで、ログ分析ツールでの検索や集計が容易になります。\n    *   `workflow.logger.info()` を用いて、Activityの各フェーズの進捗、LLMからの主要な応答、エラーメッセージなどをログに出力します。\n*   **LLMの応答時間 (`latency_ms`):**\n    *   `LLMResult` に含まれる `latency_ms` は、Gemini API呼び出しのパフォーマンスを直接測定する重要なメトリクスです。\n    *   これらのデータを集計し、平均、中央値、99パーセンタイルなどのレイテンシメトリクスを監視することで、API応答のパフォーマンス傾向を把握し、潜在的なボトルネックを特定できます。\n*   **トークン消費量 (`input_tokens`, `output_tokens`, `total_tokens`):**\n    *   これらのメトリクスは、APIコストに直接影響します。長期的に監視することで、プロンプトの変更やモデルの選択がコストに与える影響を評価できます。\n*   **エラー率:**\n    *   Gemini API呼び出しの成功率とエラー率を監視します。エラー率の急激な上昇は、API側の問題、レート制限の超過、またはプロンプトの問題を示唆する可能性があります。\n*   **パフォーマンスチューニング:**\n    *   **プロンプトの最適化:** レイテンシやトークン消費量が問題となる場合、プロンプトの簡潔化、必要な情報の厳選、より効率的なモデルへの切り替えなどを検討します。\n    *   **非同期処理:** Activityは非同期で設計されていますが、`client.models.generate_content` 呼び出し以外の部分で同期的なブロッキングI/Oがないか確認します。\n    *   **Workerリソース:** WorkerのCPU、メモリ、ネットワークリソースが十分であることを確認します。リソースが不足していると、Activityの実行が遅延したり、エラーが発生したりする可能性があります。\n\nこれらの運用上の考慮事項は、本Activityを安定かつ効率的にSOP生成パイプラインの一部として機能させるために不可欠です。\n\n### 10. 付録\n*   **対象読者:** 開発者\n*   **所要時間の目安 (本セクション):** 5分\n本SOPで参照される主要なデータ構造の完全な定義と、関連する外部リソースへのリンクを含めます。\n\n#### 10.1. `core.models.SOPRequest` データ構造定義\n\n```python\n# core/models.py\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass SOPRequest(BaseModel):\n    \"\"\"\n    SOP 生成 Activity への入力データモデル。\n    LLM に与えるコンテキストと現在の処理フェーズを定義します。\n    \"\"\"\n    topic: str = Field(\n        ...,\n        description=\"SOPが対象とするトピックまたは機能の簡潔な説明。\",\n        examples=[\"Temporal SOP 生成 Activity — フェーズ別 Gemini 呼び出し実装\"],\n    )\n    phase: str = Field(\n        ...,\n        description=\"現在のSOP生成フェーズ ('outline', 'draft', 'review')。\",\n        examples=[\"outline\", \"draft\", \"review\"],\n    )\n    source_code: Optional[str] = Field(\n        None,\n        description=\"ドキュメント化対象のソースコード。outline および draft フェーズで使用。\",\n        examples=[\"def my_func():\\n    pass\"],\n    )\n    outline: Optional[str] = Field(\n        None,\n        description=\"承認済みのアウトライン（draft および review フェーズで使用）。\",\n        examples=[\"## 1. はじめに\\n目的とスコープ。\"],\n    )\n    draft: Optional[str] = Field(\n        None,\n        description=\"SOPの草稿（review フェーズで使用）。\",\n        examples=[\"# SOPタイトル\\n本文...\"],\n    )\n    previous_output: Optional[str] = Field(\n        None,\n        description=\"前回のLLM出力。リトライ時にLLMが過去の応答を参照するために使用。\",\n        examples=[\"前回のアウトラインの出力テキスト\"],\n    )\n    feedback: Optional[str] = Field(\n        None,\n        description=\"人間からのフィードバック。リトライ時にLLMが改善を行うための指示として使用。\",\n        examples=[\"章立ての項目3をより具体的にしてください。\"],\n    )\n    attempt: int = Field(\n        0,\n        description=\"現在の試行回数（初回は0）。リトライ時にインクリメントされ、プロンプトの分岐に使用。\",\n        ge=0, # 0以上の整数\n    )\n```\n\n#### 10.2. `core.models.LLMResult` データ構造定義\n\n```python\n# core/models.py\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\nclass LLMResult(BaseModel):\n    \"\"\"\n    LLM からの応答と関連するメトリクスを格納するデータモデル。\n    \"\"\"\n    text: str = Field(\n        ...,\n        description=\"LLMからの生成テキスト（例: アウトライン、SOP草稿、レビュー結果）。\",\n        examples=[\"# SOP\\n## 1. はじめに\\n...\"],\n    )\n    model: str = Field(\n        ...,\n        description=\"応答を生成するために使用されたLLMモデルの識別子。\",\n        examples=[\"gemini-2.5-flash\"],\n    )\n    input_tokens: int = Field(\n        ...,\n        description=\"LLMへの入力に消費されたトークン数。\",\n        ge=0, # 0以上の整数\n    )\n    output_tokens: int = Field(\n        ...,\n        description=\"LLMの出力に消費されたトークン数。\",\n        ge=0, # 0以上の整数\n    )\n    total_tokens: int = Field(\n        ...,\n        description=\"合計トークン数 (入力 + 出力)。\",\n        ge=0, # 0以上の整数\n    )\n    latency_ms: float = Field(\n        ...,\n        description=\"LLM API呼び出しのレイテンシ（ミリ秒）。\",\n        ge=0.0, # 0.0以上の浮動小数点数\n    )\n    error: Optional[str] = Field(\n        None,\n        description=\"エラーが発生した場合のエラーメッセージ。成功時には None。\",\n        examples=[\"API_KEY_NOT_SET\"],\n    )\n```\n\n#### 10.3. 関連ドキュメントと外部リソース\n\n*   **Temporal Python SDK ドキュメント:**\n    *   [Temporal Python SDK](https://python.temporal.io/index.html)\n    *   [Activities in Python](https://python.temporal.io/docs/activities/)\n*   **Google Gemini API ドキュメント:**\n    *   [Gemini API Overview](https://cloud.google.com/vertex-ai/docs/generative-ai/model-reference/gemini)\n    *   [Google AI Python SDK Quickstart](https://ai.google.dev/tutorials/python_quickstart)\n*   **Pydantic ドキュメント:**\n    *   [Pydantic Documentation](https://pydantic-docs.helpmanual.io/)\n*   **Markdown ガイド:**\n    *   [Markdown Guide](https://www.markdownguide.org/)",
      "phase": "review",
      "phase_label": "フェーズ3: 最終レビュー",
      "tokens": 37363
    }
  ]
}
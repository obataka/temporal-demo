apiVersion: 1

providers:
  - name: LLM Metrics
    orgId: 1
    folder: LLM
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards

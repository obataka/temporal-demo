# X (Twitter) 投稿案

## パターン A：技術特化（アーキテクチャ重視）

```
Temporal + Prometheus + Grafana で「壊れないLLMエージェント」を作った。

ポイントは3行：
① RetryPolicy で API障害を自動吸収
② log_llm_interaction でメトリクスを透過的に収集
③ Gauge join で PromQL がモデル別コストを自動計算

docker compose up だけで全部動く。
```

**文字数:** 107文字
**狙い:** コードスニペット・箇条書き構成でエンジニアのブックマークを誘う。「3行で説明」形式は保存率が高い。

---

## パターン B：ビジネス特化（コスト・信頼性重視）

```
「今月LLMにいくら使った？」が答えられない会社、多すぎる。

Prometheus を1つ入れるだけで

gemini-2.5-flash: $0.00055 (86%)
mock-llm-v1:      $0.00004 (14%)

がリアルタイムで見える。
これからのAI開発に「可観測性」は必須です。
```

**文字数:** 116文字
**狙い:** 具体的な数字と「あるある」課題感でビジネス層・CTOへの拡散を狙う。

---

## パターン C：ストーリー特化（開発の集大成）

```
「AIが止まったら誰かが起きて直す」
→ そんな運用から卒業したくて作った。

Temporal に状態を永続化させ、
Grafana でコストを1円単位で可視化。

docker compose up だけで動く
"The Immortal AI Agent" 完成しました。

ブログに全設計を書きました👇
```

**文字数:** 122文字
**狙い:** 課題→解決の物語構造。「共感→興味→クリック」の導線を作り、ブログへの流入を最大化する。

---

## 補足：ハッシュタグ案

技術系の拡散には `#Temporal` `#LLMOps` `#Prometheus` `#Grafana` `#生成AI` あたりが有効。
ただし文字数制限の関係で本文内容を削らない範囲で付与すること。
